[{"title":"Helm 不是 controller：模板引擎 + release 状态机","slug":"posts/260706-helm-package-manager","date":"2026-07-06","tags":["devops","kubernetes"],"body":"Helm 常被说成「Kubernetes 的 apt / npm」——对，但这个类比藏了最关键的一点：**Helm 是一次性的（one-shot）**。`helm install` 把模板渲染成 YAML、提交给 apiserver、把这次结果记下来，然后就退出了。之后**没有任何进程持续盯着 drift**。这是理解 Helm 一切行为的锚点：它不是 controller、不是 GitOps，跟 Operator / ArgoCD 的持续 reconcile 是两码事。记住它，下面每个「反直觉」都顺理成章。\n\n## release 状态存在集群里，不在你本地\n\n`helm install` 之后，状态被写进 release namespace 的一个 Secret，名字是 `sh.helm.release.v1.<name>.v<revision>`。每 upgrade 一次多一个 revision Secret，`helm history` / `helm rollback` 就是读它们。内容是 `json.Marshal → gzip → base64`（Helm 3 默认 driver 从 Helm 2 的 ConfigMap 换成了 Secret）。想亲眼看：\n\n```bash\nkubectl get secret sh.helm.release.v1.my-pg.v3 -o jsonpath='{.data.release}' \\\n  | base64 -d | base64 -d | gunzip   # 两次 base64：先解 k8s Secret 层，再解 Helm 自己那层\n```\n\n两个非显然后果：① 这个 Secret 受 etcd ~1MB 上限约束，chart 塞太多东西（比如把大文件写进 ConfigMap 模板）会在 install 时炸；② 谁手滑删了它，Helm 就**失忆**——`helm list` 里 release 消失，但集群里的 Deployment 还在裸奔。换 driver 用 `HELM_DRIVER=secret|configmap|sql`（sql 是 beta，只支持 PostgreSQL）。\n\n## upgrade 是三方合并，不是覆盖\n\nHelm 3 的 `helm upgrade` 用 **3-way strategic merge patch**：同时看旧 manifest、**集群里的 live state**、新 manifest。Helm 2 只做两方，`kubectl edit` 的带外改动直接被无视。差别很实：\n\n- 你手动把 replicas 从 3 调到 0。Helm 2 rollback 觉得 manifest 没变，保持 0；Helm 3 发现「记录 3 / 现状 0 / 目标 3」不一致，拉回 3。\n- 服务网格给 Pod 注入了 sidecar。Helm 2 upgrade 会把它抹掉，Helm 3 保留 sidecar，同时改你要改的 image。\n\n## Tiller 之死：一个被删掉的攻击面\n\nHelm 2 有个集群内服务端 **Tiller**，默认以 `cluster-admin` 跑且**无认证**——任何能连上它的 Pod 都能操纵整个集群，是经典提权入口。Helm 3 直接砍掉，变成纯客户端，权限完全走你 kubeconfig 的 RBAC。所以网上任何出现 `helm init` / Tiller 的教程都是 Helm 2 时代的，别抄。\n\n## lookup 在 dry-run 会静默返回空\n\n有个常见模式：用 `lookup` 查 Secret 是否已存在，没有才生成随机密码，好让密码在 upgrade 间保持稳定。坑在于——`helm template` 和 `helm install --dry-run`（客户端）**根本不连 apiserver**，`lookup` 永远返回空。于是 diff 工具（helm-diff、ArgoCD）每次 render 都看到「密码变了」，天天误报。要真查集群得用 `--dry-run=server`。\n\n## 默认不等就绪；数组不合并只替换\n\n两个静默 footgun：\n\n- `helm install` 返回 ≠ 应用 ready，它只保证 manifest 提交成功。CI 里不加 `--wait` 的绿是假绿；再配 `--atomic` 让失败自动 rollback。\n- values 合并对 map 是深合并，对**数组是整体替换**（不是 append）。想往默认 list 里加一项，结果整个 list 被你那一项顶掉。多个 `-f` 后者盖前者，`--set` 优先级最高。\n\n## chart 存哪：OCI 已取代 index.yaml\n\n老式 repo 靠一个 `index.yaml` 索引（`helm repo add`）。3.8.0 起 **OCI 支持转正 GA 且默认开启**，底层是 ORAS——chart 像容器镜像一样 `helm push` / `pull` 到镜像仓库，没有 index.yaml。一个隐藏坑：OCI tag 不允许 `+`，Helm 会把 SemVer build metadata 里的 `+` 悄悄转成 `_`。\n\n一句话记牢：**Helm 只在你敲命令的那一刻工作**——渲染、提交、记账，然后什么都不管了。想要持续 reconcile 抹平 drift，那是 ArgoCD / Flux 的活。"},{"title":"DN42 —— 改 git repo 申请 ASN 的 BGP 沙盒","slug":"posts/260703-dn42-bgp-sandbox","date":"2026-07-03","tags":["network"],"body":"DN42 是一张「长得跟真实互联网一模一样」的 overlay 网络——同一套 BGP / whois / DNS，但发号不用求 RIR：fork 一个 git 仓库、改几个文件、提 PR，脚本校验通过就合并，你就正式拥有了自己的 ASN 和地址段。核心价值是**安全网**：你在里面扮演一家 mini-ISP 练 BGP，配错了也不会有大运营商打电话来骂。\n\n最反直觉的一点：它用 WireGuard 这类 VPN 隧道把成员互联，乍看像 Tor 那类 darknet，**目标却相反**——不做匿名，专做「去 NAT 的真实路由练习」。成员之间要的是能直连、能互相暴露服务，而不是藏起来。\n\n## 注册是 git PR，不是找管理员\n\n真实互联网上申请 ASN / IP 要走 APNIC 那套官僚流程；DN42 把它压成一次 PR——这是它整个设计的题眼。你要建的对象跟真实 whois 一一对应：\n\n- `mntner`（维护者，带签名 auth）、`person`（`nic-hdl` 以 `-DN42` 结尾）\n- `aut-num`（你的 ASN）、`inetnum` / `inet6num`（你的地址段）\n- `route` / `route6`——**仿照真实互联网的 RPKI/ROA**，没有它别人会因 ROA 校验失败而不收你的路由\n\n坑在校验：一个自动脚本会在合并前检查语法，格式错一个字符就打回，拖慢管理员审核。所以是「严格按语法填」而不是「大概填填」。\n\n## 号段为什么是私有的\n\n| 资源 | DN42 用的段 | 备注 |\n| ---- | ---- | ---- |\n| ASN | `4242420000–4242429999`（RFC 6996 私有段） | 新人建议在 `4242420000–4242423999` 里挑 |\n| IPv4 | `172.20.0.0/14` | 默认分 /27（32 个地址）够用 |\n| IPv6 | ULA `fd00::/8` | 默认分 /48 |\n\n全用**私有号段**是刻意的：这样 DN42 的路由永远不会泄进公网 BGP 表、也不会跟公网 IP 撞车。代价是这些地址空间跟 ChaosVPN（`172.31.0.0/16`）、NeoNetwork 等**兄弟网络重叠**——DN42 只敢用 `172.20.0.0/14` 而非整个 `172.16/12`，就是给这些互联网络留位置，它们之间是打通的。\n\nAS / ASN / BGP 选路的基础概念这里不重复，见同目录 CN2 那篇（那篇讲的是运营商的**真实** ASN 就是线路名，这篇是你自己**申请一个私有** ASN 来玩，正好两面）。\n\n## 加入四步\n\n1. **注册**：按上面建好对象，提 PR，合并即「持号」。\n2. **找 peer**：没有中心服务器，必须连另一个成员。新手直接连 **Kioubit**——它连接数最多、中心性最高，且有全自动 peering 向导（填 ASN + WireGuard 公钥 + 隧道 IP，几分钟出结果），所以大半个网都直连它。也可用官方 Peer Finder 挑低延迟的。\n3. **建隧道**：主流是 WireGuard（好配、够安全），也支持 OpenVPN / GRE / IPsec。\n4. **跑 BGP**：装 BIRD 或 FRR，跟 peer 起 eBGP 会话，把自己的段 announce 出去。\n\n门槛不高：一台 24/7 在线的 Linux VPS + 会配 WireGuard 和 BGP 就够。\n\n## dn42.us 不是官方入口\n\n官方主站是 **dn42.eu**（wiki + registry 说明都在这）。**dn42.us** 是成员 `xuu` 自建的一个节点门户，挂着他自己的 wiki / paste / git 镜像等服务——它本身就是「在 DN42 里对外提供服务」的活样板，别把它当成注册入口。"},{"title":"TurnKey Linux：预装好应用栈的 Debian 服务器镜像","slug":"posts/260622-turnkey-linux-appliances","date":"2026-06-22","tags":["linux","debian"],"body":"想快速起一台 WordPress / Nextcloud / GitLab，从零装 Debian → 配栈 → 加固 → 设备份要几十步。[TurnKey Linux](https://www.turnkeylinux.org/) 把这些提前做完，打包成 100+ 个开箱即用镜像（_appliance_）：下载 → 开机 → 向导设密码，几分钟能用。\n\n## 不是黑箱：底座就是一台纯 Debian\n\n除少数自研组件，底座是官方仓库里**未改动的** Debian 二进制包。所以全部 Debian 文档、`apt` 命令、排错经验都通用，维护起来跟一台普通 Debian 无异。当前主线 v18.x = Debian 12 (Bookworm)。\n\n每个镜像自带 TurnKey Core：\n\n- `di-live` 安装器 + Python 配置控制台（网络 / 重启等基础操作）\n- Webmin 网页管理：HTTPS `:12321`\n- **每日自动装安全补丁**\n- TKLBAM：一键加密备份（基于 Duplicity），文件 + 数据库 + 包状态一起存，可整机恢复 / 迁移\n\n代价是继承了 Debian **stable** 的反直觉默认：软件版本冻结，`apt upgrade` 只打安全补丁、不升应用大版本。想要新版应用得等官方出新的大版本镜像，别指望 `apt` 升上去。\n\n## 部署：VM / 容器 / 云怎么选\n\n同一份 rootfs 被 TKLDev 导出成多种格式，按平台挑：\n\n| 目标平台                | 用哪个格式                  | 怎么上                          |\n| ----------------------- | --------------------------- | ------------------------------- |\n| VirtualBox / VMware     | **OVA**（v14.0 起官方推荐） | 双击或导入向导，转换几分钟      |\n| Proxmox / KVM / Hyper-V | 通用 hybrid **ISO**         | 挂载启动，跑 `di-live` 装到磁盘 |\n| Proxmox（更省事）       | **LXC 模板**                | webUI 加 TurnKey channel 直接拉 |\n| OpenStack               | **qcow2**                   | 上传镜像                        |\n| 云                      | 预建 **AMI** 等             | 市场直接开                      |\n\n非 headless 构建（ISO/OVA）在**首次开机**跑向导设 root / 应用密码 + 网络；headless 构建（LXC/云/Docker）在**首次登录**初始化，随机 root 密码要去日志里读。之后应用走 `https://<ip>/`，系统管理走 Webmin `https://<ip>:12321`。\n\n> KVM/QEMU 不需要特殊格式，官方明确「直接用 ISO 就行」。\n\n## Docker 也能跑，但那是「胖容器」且官方已掉队\n\nDocker 镜像本质就是「文件系统 + 元数据」，从那份 rootfs 再导一个天经地义——但和 Docker 的哲学天生不合：\n\n- 标准容器跑**一个进程**（PID 1 = 应用）；TurnKey 是**一整套系统**（init + cron + ssh + Webmin + 应用 + 数据库），只能做成 _full-OS system container_，行为像 LXC。用 `/usr/sbin/start.sh` 一次拉起所有服务再 drop 到 shell，退出 shell 服务就停，日常得靠 SSH。\n- TurnKey 用 **systemd** 管服务，Docker 默认不支持 → 报 `System has not been booted with systemd as init system (PID 1)`。官网自己标注 Docker 文档 **\"does not work as intended\"**，社区反馈构建已坏。\n\n结论：想把 TurnKey 容器化，真正顺的是 **LXC（Proxmox）不是 Docker**；需要 Docker 生态时直接找单应用官方镜像（`nextcloud`、`gitlab/gitlab-ce`）。\n\n## TKLDev：整个库可复现的根子\n\n「全开源、可复现」不是口号——TKL 用一个叫 **TKLDev 的 appliance**（一台专门用来 build 的机器）产出库里全部镜像。每个 appliance 不是手工装出来的一坨，而是一个小 git 仓库里的**声明式配方**：\n\n| 组成         | 作用                                                                 |\n| ------------ | -------------------------------------------------------------------- |\n| `Makefile`   | 入口；声明继承链（你的 app → `common` → `core`），别的不用重写       |\n| `plan/`      | 要装的 Debian 包清单（支持 cpp 式条件），等于 `apt install` 的声明版 |\n| `conf.d/`    | 构建期跑的编号 shell 脚本，做配置（建库、设服务、加固）              |\n| `overlay/`   | 文件系统覆盖层，按目标路径摆好、构建时拷进根文件系统                 |\n| `removelist` | 要从镜像里删掉的文件                                                 |\n\n因为有继承，做一个自定义 appliance 往往只写几十行。构建：\n\n```bash\ncd core            # 或你自己的 product 仓库\nmake               # 从源码构建，产出 build/product.iso\nmake clean         # 清理重来\n```\n\n底层靠 `fab`（把装包 + 配置 + 打包串成可复现流水线）和 `deck`（基于 overlay 的分层快照，反复 build 时秒回滚）。要定制自己的加固 / 内部证书版镜像、又想像官方一样可复现可分发，就用它——思路和 Dockerfile / Nix / Packer 同源：把「怎么造一台机器」写成代码。\n\n源与文档：<https://www.turnkeylinux.org/> · [TKLDev 文档](https://www.turnkeylinux.org/docs/tkldev)"},{"title":"spec-kit vs. superpowers vs. gstack:三种 coding agent 工作流框架","slug":"posts/260620-spec-kit-vs-superpowers-vs-gstack","date":"2026-06-20","tags":["ai","claude-code","agent"],"body":"三个都是套在 coding agent(主要是 Claude Code) 外面的工作流框架，但把它们放一起比不是因为像，而是因为**它们赌的「正确性从哪来」是三个不同的答案**:spec-kit 赌**规格**,superpowers 赌**流程纪律**,gstack 赌**多视角评审**。选型不是比谁命令多，是认领你当前最缺哪一种。\n\n| 框架        | 出身                 | 赌注                   | 载体                              |\n| ----------- | -------------------- | ---------------------- | --------------------------------- |\n| spec-kit    | GitHub 官方          | 规格是可执行的唯一真理 | `specify` CLI + `/speckit.*` 命令 |\n| superpowers | Jesse Vincent (obra) | 流程纪律逼出质量       | 自动触发的 skills 插件            |\n| gstack      | Garry Tan (YC)       | 多角色评审补单视角盲区 | 23 个纯 Markdown slash command    |\n\n三者都是 MIT，都不止支持 Claude Code(各自能跨 7-30+ 个 agent),都不锁厂商——这些都不是差异点，差异全在下面。\n\n## spec-kit:规格是唯一真理来源\n\nGitHub 官方的 **Spec-Driven Development**。核心主张一句话：**规格是源码，代码是规格的编译产物**。你维护的是 `spec.md` 不是 `.py`;改需求改 spec，再 implement。产物 `spec.md → plan.md → tasks.md` 被当成 agent 无关的规格层——换 agent 不换规格。\n\n```bash\nuv tool install specify-cli --from git+https://github.com/github/spec-kit.git@vX.Y.Z\nspecify init my-project --integration copilot\n```\n\n然后在 agent 里按顺序走命令，每步落一个文件：\n\n```text\n/speckit.constitution   # 立项目宪法 (不可动的原则)\n/speckit.specify        # 写「做什么 / 为什么」→ spec.md\n/speckit.clarify        # 澄清模糊点 (可选，建议 plan 前跑)\n/speckit.plan           # 定技术栈方案 → plan.md\n/speckit.tasks          # 拆可执行任务 → tasks.md\n/speckit.implement      # 执行全部任务\n```\n\n最适合：需求先行、跨会话、多文件的功能开发，团队要一层标准化的规格。\n\n## superpowers:用流程纪律逼出质量\n\nJesse Vincent(GitHub `obra`) 的 **skills 框架**。它不给你命令表，给一套**自动触发**的 skills——agent 一旦识别到该用某 skill 就必须用，把「偷懒跳步」这条路堵死。内核是真正的红绿 TDD + 每个任务派新鲜子代理，两段式审查 (先查规格符合、再查代码质量)。\n\nClaude Code 装法一行：\n\n```bash\n/plugin install superpowers@claude-plugins-official\n```\n\n强制流水线 (skills 依次自动登场):\n\n```text\nbrainstorming               # 苏格拉底式追问，细化想法 → design doc\nusing-git-worktrees         # 设计批准后开隔离工作区\nwriting-plans               # 拆成 2-5 分钟的小任务，带确切路径 + 完整代码\nsubagent-driven-development # 每任务派新鲜子代理，两段式 review\ntest-driven-development     # RED → GREEN → REFACTOR，先写失败测试\nrequesting-code-review      # 对着 plan 审，critical 问题卡住不放行\n```\n\n最适合：在意测试纪律和执行严谨度，想防 agent「不写测试 / 静默改需求」。\n\n## gstack:把一个 agent 变成一支团队\n\nGarry Tan(YC 总裁) 的设置：**23 个专家 + 8 个 power tool**,全是纯 Markdown slash command，无专有 runtime。它赌**结构化 prompt 而非自建工具**才是对的抽象层——每个角色只约束 agent 这一阶段盯一件事，靠视角切换补单一视角的盲区。\n\n装法 (全局装 + `./setup`):\n\n```bash\ngit clone --single-branch --depth 1 https://github.com/garrytan/gstack.git ~/.claude/skills/gstack \\\n  && cd ~/.claude/skills/gstack && ./setup\n```\n\n一个 Sprint 里角色依次登场 (Think → Plan → Build → Review → Test → Ship → Reflect):\n\n```text\n/office-hours       # YC 办公时间：这产品该不该做\n/plan-ceo-review    # CEO:重想问题本身\n/plan-eng-review    # 工程经理：锁架构 + 测试策略\n/plan-design-review # 设计师：评设计维度，抓 AI slop\n/review             # Staff 工程师：挖生产级 bug\n/qa                 # QA:真开浏览器点流程，修完再验\n/cso                # 安全官:OWASP + STRIDE 审计\n/ship               # 发布工程师：测试 + PR + 覆盖率\n```\n\n外加 `/codex`(拉 OpenAI Codex 做跨模型二次 review)、`/careful` `/freeze` `/guard`(危险命令 / 目录锁护栏) 这类 power tool。最适合：一个人当全栈团队，要多视角决策 + 质量 / 安全 / 发布把关。\n\n## 组合与选型\n\n三者不互斥，常叠着用：**gstack 多角色起草 → superpowers 头脑风暴 + TDD 执行 → spec-kit 固化成 spec/plan/tasks**。但别为叠而叠，先认领你当前最缺的那一环。\n\n口诀：**缺规格标准化用 spec-kit，缺执行纪律用 superpowers，缺多视角把关用 gstack。**"},{"title":"gitignore 里斜杠的位置决定匹配范围","slug":"posts/260619-gitignore-slash-rules","date":"2026-06-19","tags":["git"],"body":"gitignore 的匹配范围全看斜杠在哪，记两条：\n\n- **结尾 `/`** = 只匹配目录;不带 = 文件、目录通吃。\n- **开头 / 中间有 `/`** = 锚定到仓库根目录;pattern 里完全没有斜杠 = 任意层级都匹配。\n\n## 结尾斜杠：对文件加 `/` 是静默 bug\n\n`.idea` 是目录，`.idea/` 和 `.idea` 效果一样——但 `/` 更精确，显式表达「我忽略的是目录」,哪天真有个叫 `.idea` 的文件不会被误伤。JetBrains / GitHub 模板都用 `.idea/`。\n\n反过来对文件就是坑：`.DS_Store` 是 macOS 生成的**文件**,写成 `.DS_Store/` 会**匹配不到任何东西**(没有叫这名的目录),规则静默失效、不报错。目标是文件时绝不能加 `/`。\n\n```bash\n.idea/         # 目录 → 带斜杠更严谨\n.DS_Store      # 文件 → 加斜杠就失效\n```\n\n## 开头斜杠:monorepo 的陷阱\n\n`node_modules/`(无开头斜杠) 匹配任意深度，等价于 `**/node_modules/`——所以 `**/` 在这里是多余的。`/node_modules/` 只锚定根目录那一个，子包里的会漏掉。\n\n```text\nnode_modules/                ← /node_modules/ 只命中这个\npackages/app/node_modules/   ← 只有无斜杠写法 / **/ 才命中\n```\n\nmonorepo(pnpm / turborepo 子包) 一定用能匹配嵌套的 `node_modules/`,别写 `/node_modules/`。\n\n`**/` 真正有用是「中间匹配多层」:`docs/**/tmp`(docs 下任意深度的 tmp)、`src/**/*.log`。\n\n`.DS_Store`、`.idea/` 这类跟项目无关的编辑器 / 系统噪音，放全局 ignore(`git config --global core.excludesFile`) 一次，所有仓库都干净。"},{"title":"nerd","slug":"posts/260617-nerd","date":"2026-06-17","tags":["english","jargon"],"body":"装 [Nerd Fonts](https://www.nerdfonts.com/) 时顺手查的词。最反常识的一点先记住：\n\n> **nerd 不是任何东西的首字母缩写，是一个被「造」出来的词。**\n\n很多人以为它是 acronym（比如流传的 \"Northern Electric Research and Development\"），全是 folk etymology（民间附会）。真实出处是 1950 年 Dr. Seuss（苏斯博士）童书 _If I Ran the Zoo_ 里他自造的一个生物名，次年被学生俚语借去当「书呆子」用，固定至今。\n\n## 词义随圈子翻转\n\n- 原意带贬义：书呆子、不擅社交、土气。\n- 科技圈现在基本是 **褒义甚至自豪的自称**：某领域的狂热钻研者。`a computer nerd` / `a math nerd`。说 \"I'm a nerd\" 是骄傲，不是骂人。\n\n`nerdfonts.com` 的命名就是这层自嘲幽默——「只有我们这些爱折腾终端的 nerd 才在意的字体」。\n\n## 近义词别混\n\n| 词        | 含义              | 色彩        |\n| --------- | ----------------- | ----------- |\n| **nerd**  | 技术 / 学术钻研者 | 中性 → 褒义 |\n| **geek**  | 极客，痴迷某领域  | 偏褒义      |\n| **dork**  | 笨拙、傻气        | 偏贬义      |\n| **dweeb** | 呆子              | 贬义        |\n\n口诀：**nerd / geek 在科技圈是夸，dork / dweeb 是损人。**"},{"title":"CN2 GIA / 9929 / CMIN2：三网回国线路分层","slug":"posts/260616-cn2-gia-9929-cmin2","date":"2026-06-16","updated":"2026-07-06","tags":["network"],"body":"回国线路好坏，本质是数据包**进国内后走「精品网」还是「普通骨干网」**——三网都能按 **普通线 / 优质线 / 顶级线** 三档理解（VPS、跨境专线圈的俗称）。看线的前提：**回程比去程重要**——下载占 90%+，回程决定体感。\n\n## AS 号就是线路名的真身\n\n_AS_（Autonomous System，自治系统）= 由同一机构统一管理、对外采用统一路由策略的一组网络。互联网是「网络的网络」，每个大网络就是一个 AS。\n\n- _ASN_（AS 号）是网络层的身份证，全球唯一；BGP 选路靠它：IP 像门牌号定位主机，ASN 像区域号——BGP 先决定送到哪个 AS，再在 AS 内细分到具体地址。\n- ASN 原本 16 位（1–65535），号段不够用扩成了 32 位——移动的 AS58453 / AS58807 就是 32 位号，所以位数明显比电信 AS4809 长。\n\n所谓 CN2 GIA、9929、CMIN2，本质就是 AS4809、AS9929、AS58807——运营商不同精品骨干网的「身份证号」。\n\n## 精品网 vs 普通骨干：差在哪\n\n普通骨干（电信 163、联通 169、移动 CMNET）是共享、超售的大网，国际带宽受限，晚高峰拥堵、抖动大。精品网是**独立轻负载链路**，绕开大量公共交换点（IX），**一跳入境、一跳落地**，所以延迟低、丢包少、高峰稳——代价是带宽小、价格贵。这就是 CN2 GIA 被当成回国「黄金标准」的根因。\n\n优质线是**半程精品**：只在出国段走精品网，国内段仍回普通骨干（电信 CN2 GT 典型——国内段走 163，出境才汇入 CN2），比纯普通线好，但高峰国内段仍会堵；顶级线**全程精品**、不降级，才是天花板。\n\n## 三网分层总表\n\n| 运营商 | 普通线（普通骨干） | 优质线（半程精品）             | 顶级线（全程精品）                     |\n| ------ | ------------------ | ------------------------------ | -------------------------------------- |\n| 电信   | 163（AS4134）      | CN2 GT（AS4809，国内仍走 163） | **CN2 GIA**（AS4809）                  |\n| 联通   | 169（AS4837）      | —（无 GT 式半程档）            | **9929 / CUII**（AS9929）              |\n| 移动   | CMNET（AS9808）    | CMI（AS58453）                 | **CMIN2**（AS58807，CMI 的 N2 精品网） |\n\n综合性能大致：**CN2 GIA ≈ 9929 ≥ CMIN2 > CN2 GT / CTGNet > 163 / 169 / CMNET**。\n\n商家宣传只说「CN2」是泛称，要追问是 GT 还是 GIA——两者差价常有 2–3 倍。\n\n选线一句话：电信用户选 CN2 GIA，联通用户选 9929，移动用户选 CMIN2；三网优化 VPS 就是按运营商把三条线分流，是目前回国线路的天花板。\n\n## 怎么自测拿到的是哪条线\n\n两个工具都跑在被测的境外 VPS 上：\n\n**[NodeQuality](https://nodequality.com/)**——一键综合测评，除硬件 / IP 质量外，其 `backroute_trace` 会自动判断三网回程各走哪条骨干，最省事：\n\n```bash\nbash <(curl -sL https://run.nodequality.com)\n```\n\n**[NextTrace](https://github.com/nxtrace/NTrace-core)**（`nexttrace`）——想手动细看回程路径用它，每跳直接标 AS 号 + 地理位置，比裸 `traceroute` 清楚。"},{"title":"xxd —— 字节级显微镜，看出 BOM / CRLF / 不可见字符","slug":"posts/260615-xxd-hex-dump","date":"2026-06-15","tags":["shell"],"body":"文本工具看不出问题 (多了空格？BOM?tab 还是空格？CRLF 还是 LF?) 时，用 `xxd` 把真实字节 dump 出来。它随 Vim 分发，Linux / mac 基本自带。\n\n```bash\nxxd file.sh\n# 00000000: 2321 2f62 696e 2f62 6173 680a       #!/bin/bash.\n#           └偏移   └每字节 hex(两字节一组)     └ASCII(不可打印显示 .)\n```\n\n三列：**偏移地址** / **每字节十六进制** / **对应 ASCII**(不可打印字符用 `.` 占位)。行尾那个 `0a` 就是 LF;如果是 `0d0a` 就是 CRLF——这是肉眼分不出、`xxd` 一眼看穿的典型场景。查 UTF-8 BOM 同理：`head -c3 file | xxd` 出现 `efbbbf` 即有 [BOM](260612-utf8-bom-breaks-tools.md)。\n\n常用 flag:\n\n```bash\nxxd -l 16 file       # 只看前 16 字节(-l = length)\nxxd -s 100 file      # 从第 100 字节开始(-s = seek)\necho -n hi | xxd     # 看一段文本的字节: 6869\n```\n\n`-r`(reverse) 能把 hex dump 还原成二进制，于是 `xxd` 成了简易二进制编辑器——dump、改 hex、再写回：\n\n```bash\nxxd file > dump.hex      # 导出\n# 编辑 dump.hex 改某个字节\nxxd -r dump.hex > file   # 还原写回\n```\n\n同类：`od -c`、`hexdump -C`。"},{"title":"#!/bin/bash vs #!/usr/bin/env bash","slug":"posts/260613-shebang-env-bash","date":"2026-06-13","tags":["shell"],"body":"区别就一句：`#!/bin/bash` 写死路径只认 `/bin/bash`，`#!/usr/bin/env bash` 让 `env` 去 `$PATH` 里找第一个 `bash`。\n\n为什么要绕 `env`：shebang 那行**只接受绝对路径，自己不查 PATH**。但 `bash` 在各机器路径不一（`/bin`、`/usr/bin`、`/opt/homebrew/bin`…），写死就不通用。`env` 几乎永远在固定的 `/usr/bin/env`,而它**能**查 PATH——于是用「固定位置的 env」把「查 PATH」这个能力接进 shebang。`python3` / `node` 同理。\n\nmacOS 上这不是风格问题而是会报错。Apple 不用 GPLv3，自带的 `/bin/bash` 冻结在 **3.2（2007 年）**;Homebrew 装的新 bash（5.x）在 `/opt/homebrew/bin/bash`,排在 PATH 前面：\n\n```bash\necho \"/bin/bash → $(/bin/bash --version | head -1)\"\necho \"env bash  → $(env bash --version | head -1)\"\n# /bin/bash → GNU bash, version 3.2.57(1)-release\n# env bash  → GNU bash, version 5.3.9(1)-release\n```\n\n同一台机器差两个大版本。脚本一旦用到 `declare -A`（关联数组）、`mapfile` 这类 bash 4+ 特性，`#!/bin/bash` 在 mac 上直接 `command not found`,必须 `#!/usr/bin/env bash` 才跑到 5.x。\n\n代价是 `env bash` 跟着 PATH 走——PATH 被改就换了 bash。所以反过来，**要绝对确定环境**的场景（root cron、系统安装脚本）才用写死的 `#!/bin/bash`,不受 PATH 干扰。\n\n还有第三种 `#!/bin/sh`——它跑的根本不一定是 bash。`sh` 是**接口名**(POSIX shell),实现各发行版不同:Debian / Ubuntu 是 `dash`,macOS 是一份独立的 bash 3.2 拷贝。而且 **bash 以 `sh` 名字被调用时自动进入 POSIX 模式**,关掉 `[[ ]]`、`declare -A`、数组等 _bashism_。于是高频翻车：脚本写 `#!/bin/sh` 却用了数组，在 mac(sh 是宽松的 bash) 侥幸能跑，push 到 Debian CI(sh 是 dash) 立刻 `[[: not found`——「本地能跑」不等于「符合 POSIX」,两边的 `sh` 是不同程序。\n\n口诀：要 bash 特性 → `#!/usr/bin/env bash`(默认);锁死环境 (root cron / 安装脚本)→ `#!/bin/bash`;只用 POSIX、求最大可移植 → `#!/bin/sh`(配 `shellcheck` 抓 bashism)。\n\n顺带 `env` 还能临时改环境跑命令，调「环境变量导致的 bug」常用：\n\n```bash\nenv FOO=bar ./script.sh   # 只给这次运行设 FOO，不污染当前 shell\nenv -i bash               # -i 空环境启动\nenv -u PATH command       # -u 临时删掉某变量\n```"},{"title":"UTF-8 的 BOM 没用，还会静默破坏脚本和 JSON","slug":"posts/260612-utf8-bom-breaks-tools","date":"2026-06-12","tags":["shell"],"body":"_BOM_(Byte Order Mark) 是文件最开头一段**肉眼不可见**的字节，本职是标记编码的字节序:UTF-16 用 `FE FF`(大端)/ `FF FE`(小端) 告诉读取方高位在前还是低位在前。\n\n关键非显然点：**UTF-8 根本没有字节序问题**——它的码元就是单字节，不存在「谁在前」。所以 UTF-8 的 BOM(`EF BB BF`) 纯属多余，标准也不推荐。但 Windows 系工具 (记事本、旧 Excel、部分 .NET) 会自动加，于是这三个看不见的字节就成了静默 bug 源：\n\n- **shebang 失效**:第一行变成 `<BOM>#!/bin/bash`,内核读头两字节看到的是 BOM 不是 `#!`,脚本不被当可执行 (参见 [shebang 是 magic number](260610-shebang-kernel-magic.md))。\n- **JSON 解析报错**:`json.loads` / 严格 parser 把开头 BOM 当非法字符。\n- **PHP \"headers already sent\"**:`<?php` 前那三个 BOM 字节被当作输出先发出去，之后 `header()` / `setcookie()` 全废。\n\n都因为字节在位置 0、又看不见，排查起来格外费时。检测和清除：\n\n```bash\nhead -c3 file | xxd                  # efbbbf 就是有 UTF-8 BOM\nsed -i '1s/^\\xEF\\xBB\\xBF//' file      # 删掉开头 BOM\nfile file                            # 输出含 \"with BOM\" 也能认\n```\n\n一句话:Unix 工具链默认无 BOM,UTF-8 存盘一律选「无 BOM」那个选项。"},{"title":"shebang 是内核认的 magic number，不是 shell 语法","slug":"posts/260610-shebang-kernel-magic","date":"2026-06-10","tags":["shell"],"body":"`#!` 不是 bash 的语法，而是文件开头的 **magic number**:字节 `0x23 0x21`。当你 `./script` 执行一个文件，是 Linux 内核的 `execve(2)` 读头两个字节，看到 `#!` 就把后面那行当解释器路径，改成执行「解释器 + 脚本路径」。名字 = `#`(sharp) + `!`(bang)。\n\n这带来几个非显然的硬约束：\n\n- **必须是文件最开头**——`#!` 前面不能有空行、空格或 BOM，否则内核认不出，文件会被当普通数据。\n- **解释器后只允许一个参数**。`#!/usr/bin/env python -u` 里 `python -u` 在多数系统被当作**单个**参数传给 `env`,于是去找名叫 `\"python -u\"` 的程序、失败。要传 flag 得在脚本内另想办法，别堆在 shebang 上。\n- 没有 shebang 的可执行文件，内核 `execve` 直接 `ENOEXEC`,是调用它的 shell 兜底用 `/bin/sh` 跑——所以「没写 shebang 也能跑」靠的是 shell 不是内核。\n\n```bash\nhead -c2 script.sh | xxd   # 00000000: 2321  →  #!\n```\n\n选 `#!/bin/bash` 还是 `#!/usr/bin/env bash`,见 [shebang vs env bash](260613-shebang-env-bash.md)。"},{"title":"plutil vs PlistBuddy","slug":"posts/260608-plutil-vs-plistbuddy","date":"2026-06-08","tags":["macos"],"body":"macOS 自带两个改 `.plist` 的命令行工具，功能重叠却谁也没淘汰谁——因为出身不同：\n\n- `/usr/bin/plutil`：给开发者的**文件级**工具，在 `PATH` 里，Apple 主推、文档全。强项是校验和编码转换。\n- `/usr/libexec/PlistBuddy`：早期系统安装器留下的**键值级**编辑器。`libexec` = 内部 helper，故意不进 `PATH`，文档几乎没有，但改深层嵌套最顺手。\n\n（plist = Apple 的配置文件格式，相当于苹果版 JSON/YAML；一个格式两种编码：XML 可读 / binary 紧凑。）\n\n## 改一个嵌套值：两种风格\n\nPlistBuddy 用类 DSL 命令，`:` 表层级，`Set` 不用声明类型：\n\n```bash\n/usr/libexec/PlistBuddy -c \"Print :Server:Port\" demo.plist     # 8080\n/usr/libexec/PlistBuddy -c \"Set :Server:Port 9090\" demo.plist\n```\n\nplutil 用选项参数，`.` 表层级，且**必须声明类型**：\n\n```bash\nplutil -replace Server.Port -integer 7070 demo.plist\nplutil -extract Server.Port raw demo.plist                     # 7070\n```\n\n两个都能读写任意路径的 plist，binary 编码也能直接改（不用先转 XML）。\n\n## 只有 plutil 能做的：校验 + 转码\n\n这是 PlistBuddy 没有的能力，CI / 构建脚本里最常用：\n\n```bash\nplutil -lint demo.plist               # demo.plist: OK   ← 语法体检\nplutil -convert binary1 demo.plist    # XML → binary\nplutil -convert xml1 demo.plist       # binary → XML（方便 diff）\nplutil -p demo.plist                  # 人类可读打印，binary 也认\n```\n\n`plutil -p` 把 binary 也解出来给你看：\n\n```text\n{\n  \"CFBundleVersion\" => \"1.0\"\n  \"Server\" => {\n    \"Port\" => 9090\n  }\n}\n```\n\n## 怎么选\n\n- **体检 / 转码 / 在 CI 里跑** → `plutil`，`-lint` 和 `-convert` 是它独有\n- **改具体值、尤其深层嵌套** → `PlistBuddy`，`Set/Add/Delete :a:b` 比 `-replace a.b -type` 少打字、不用记类型\n\n顺带：Apple 已预告未来 `defaults` 命令只认 preferences domain，按文件路径乱改 plist 的老用法要迁到这两个工具——所以它们短期不会消失。\n\nSee `man plutil` / `/usr/libexec/PlistBuddy -h`."},{"title":"git interpret-trailers：读写 commit message trailer 的官方工具","slug":"posts/260604-git-interpret-trailers","date":"2026-06-04","updated":"2026-07-06","tags":["git"],"body":"_trailer_ 是 commit message 末尾形如邮件头的 `Key: Value` 行——`Signed-off-by` / `Reviewed-by` / `Co-authored-by`。读写交给 `git interpret-trailers`,**别 grep/awk**:按字段位置取值会 silently drop(同 [按作者统计](191203-author-stats.md) 的定界符坑),只有 git 知道哪些行真是 trailer。\n\n判定很严，同时满足才算 trailer、否则当正文:**连续的含 `:` 行 + 前面隔一个空行 + 位于消息末尾**(或 `format-patch` 的 `---` 前)。所以 body 中段的 `URL: ...`、或紧贴正文没空行的，都不算——这是手写 parser 最易错处。\n\n**读**用 `--parse`,只吐已有 trailer、忽略配置：\n\n```bash\ngit log --format=%B -n1 HEAD | git interpret-trailers --parse\n```\n\n**写**用 `--trailer`,默认追加在 trailer 块末;与最后一条完全相同则不重复加：\n\n```bash\ngit interpret-trailers --in-place \\\n  --trailer 'Reviewed-by: Bob <bob@example.com>' msg.txt\n```\n\n用 `trailer.*` 配置把常用 key 定义成简写。**别名必须是完整 key 的前缀**(大小写不敏感)——所以 `ack` 能映射 `Acked-by`,却没法拿 `sign` 映射 `Reviewed-by`。再配 `.cmd` 让别名自带取值逻辑，`--trailer ack` 不给值也能生成：\n\n```bash\ngit config trailer.ack.key 'Acked-by'\ngit config trailer.ack.cmd 'echo \"$(git config user.name) <$(git config user.email)>\"'\ngit interpret-trailers --trailer ack   # 自动补 Acked-by: 你的名字 <邮箱>\n```\n\n`.ifexists` / `.ifmissing` 再单独控这个别名的重复与缺失行为。\n\n最常见是 `commit-msg` 钩子里自动补 trailer(`$1` 是 git 传入的消息文件，`--trim-empty` 丢掉空值行):\n\n```sh\n#!/bin/sh\ngit interpret-trailers --trim-empty \\\n  --trailer \"git-version: $(git describe)\" \"$1\" > \"$1.new\" && mv \"$1.new\" \"$1\"\n```\n\n`git help interpret-trailers` 看全。"},{"title":"reviewdog：把 linter 输出贴成 PR 行内评论","slug":"posts/260603-reviewdog-lint-to-pr","date":"2026-06-03","tags":["devops","ci","lint"],"body":"[reviewdog](https://github.com/reviewdog/reviewdog) 是一个通用适配器：吃任意 linter 的 stdout，按 _errorformat_ 解析，然后把结果作为 **PR 行内评论**贴回 GitHub / GitLab。它自己不做静态分析，只解决\"已有的 lint 结果怎么进 review\"这一段。\n\n价值全在它默认**只报 diff 改动的行**。整仓库跑 lint 有两个老问题：老代码一堆历史 warning 淹没真正要看的；报错埋在 CI 日志里 reviewer 根本不点开。reviewdog 用 `-filter-mode` 把结果过滤到本次改动，再走 review API 顶到代码行上，两个问题一起消。\n\n```bash\n# PHPStan checkstyle 输出 → 只评论本次 PR 改动行\nvendor/bin/phpstan analyse --error-format=checkstyle | \\\n  reviewdog -f=checkstyle -reporter=github-pr-review\n```\n\n`-f` 选输入格式：内置一批（`-f=golint` / `-f=checkstyle`...，`reviewdog -list` 看全），没有就用 `-efm='%f:%l:%c: %m'` 自定义 scanf 风格的 pattern。\n\n## `-filter-mode` 决定报多少\n\n| 值              | 报什么                             |\n| --------------- | ---------------------------------- |\n| `added`（默认） | 只报新增 / 修改的行                |\n| `diff_context`  | 改动行 ±N 行（默认 N=3）的上下文内 |\n| `file`          | 只要文件被改了，整文件的问题都报   |\n| `nofilter`      | 全报，不过滤                       |\n\n默认 `added` 是最克制的，新加的代码出新问题才说话——这正是\"零历史噪音\"的来源。本地想在 push 前自查，用 `-diff` 喂一份 diff：`reviewdog -diff=\"git diff origin/main\"`。\n\n## 两个 GitHub reporter，auth 不一样\n\n`-reporter` 有十来个（`gitlab-mr-discussion` / `bitbucket-code-report` / `gerrit-change-review`...），GitHub 上常用的两个**底层 API 不同，因此认证和能力都不同**，选错会白配半天：\n\n- **`github-pr-review`**：走普通 review-comment API，只要一个 PAT（`REVIEWDOG_GITHUB_API_TOKEN`，private 仓库需 `repo` scope）。**不依赖 reviewdog 的服务器**，而且支持 **code suggestion**（评论里带可一键 apply 的改法）。\n- **`github-pr-check`**：走 GitHub **Check API**，能在 PR 顶部生成正经的 check 状态（可当 merge 门禁）。代价是 Check Runs 这套 API 普通 PAT 调不动——要么在 Actions 里用 `secrets.GITHUB_TOKEN`，要么装 reviewdog 的 GitHub App 配 `REVIEWDOG_TOKEN`。**不支持 code suggestion**。\n\n口诀：想要能一键采纳的修改建议、配置最省 → `github-pr-review`；想要红绿状态做 merge gate → `github-pr-check`。\n\n## 同类工具其实是两条线\n\nreviewdog 属于 **rule-based** 那条——规则确定、几乎零误判，只把已有 linter 接进 review。同线还有 [Super-Linter](https://github.com/super-linter/super-linter)（一个容器塞几十种 linter，开箱即用）和 [SonarQube/SonarCloud](https://www.sonarsource.com/products/sonarcloud/)（带质量看板、覆盖率、quality gate，重）。\n\n另一条是 **AI-based**：[CodeRabbit](https://www.coderabbit.ai/)、[Qodo](https://github.com/qodo-ai/pr-agent) 读 diff 给自然语言评论，能看出逻辑层面的问题，但有概率出错。两条线不冲突：rule-based 兜底确定性问题，AI-based 补人类视角。\n\n装：`brew install reviewdog/tap/reviewdog`。文档：<https://github.com/reviewdog/reviewdog>。"},{"title":"git commit --fixup 配 rebase autosquash 收拢历史","slug":"posts/260603-git-fixup-autosquash","date":"2026-06-03","tags":["git"],"body":"code review 改意见时，最容易留下 `fix typo`、`address review` 这种噪音 commit。`--fixup` + autosquash 让每个补丁自动并回它对应的原始 commit，历史只剩有意义的那几个。\n\n发现 commit `A` 要补个改动时，不要新开一个独立 commit，而是：\n\n```bash\ngit commit --fixup A      # message 自动写成 \"fixup! <A 的标题>\"\n```\n\n`A` 是占位符，换成目标 commit 的任意 _commit-ish_:hash(`abc1234`)、`HEAD~2`、分支名，或 `:/登录` 模糊匹配 message。下文 `A` / `B` 同理，只是示意图里给 commit 起的标签。\n\n之后一把 rebase 收拢：\n\n```bash\ngit rebase -i A^\n```\n\n打开 todo 时 git 已经**自动**把 `fixup!` 那条挪到 `A` 正下方、并标好 `fixup` 动作，直接存盘退出即可吃进 `A`:\n\n```text\npick   A   feat: 登录接口\nfixup  A'  fixup! feat: 登录接口   ← 自动排序 + 自动标记\npick   B   feat: 注册接口\n```\n\n`~/.gitconfig` 里这行是关键——它让 `rebase -i` **默认**就开自动排序，否则每次得手动加 `--autosquash`,或在编辑器里自己挪行：\n\n```ini\n[rebase]\n  autosquash = true\n```\n\n两个易混点：\n\n- **`--fixup` vs `--squash`**:`fixup!` 丢弃补丁的 message，完全用原 commit 的;`squash!` 把两条 message 拼起来让你编辑。改 bug 用 fixup，要补充说明用 squash。\n- **autosquash 只改 `-i` 的默认行为**,不是全自动——仍要你手动触发一次 `git rebase -i`,它只负责把 `fixup!`/`squash!` 排到位。\n\n`--fixup` 还能直接指向\"动了哪个文件就修哪个 commit\":\n\n```bash\ngit commit --fixup :/登录    # 模糊匹配最近 message 含\"登录\"的 commit\n```\n\nSee `man git-rebase` → `--autosquash`,`man git-commit` → `--fixup`。"},{"title":"git 按目录自动切换提交身份 (includeIf)","slug":"posts/260603-git-includeif-identity","date":"2026-06-03","tags":["git"],"body":"多身份 (公司 / 个人) 最容易出的事是忘记切换、用错 email 提交。`includeIf` 让 git 按**仓库所在目录**自动加载对应身份，零手动操作。\n\n`~/.gitconfig`:\n\n```ini\n[user]\n name = Your Name        # 默认 name;故意不写 email\n\n[includeIf \"gitdir:~/work/\"]\n path = ~/.gitconfig_work\n[includeIf \"gitdir:~/personal/\"]\n path = ~/.gitconfig_personal\n```\n\n`~/.gitconfig_work` / `~/.gitconfig_personal` 各放一行 email:\n\n```ini\n[user]\n email = you@company.com\n```\n\n防呆关键：**全局不设默认 email**。任何没被规则覆盖的目录会直接报 `unable to auto-detect email address` 拒绝提交——把\"忘记\"变成\"无法继续\",而不是悄悄用错身份提交后才发现。这一步才是护城河，`includeIf` 本身 google 5 秒可查。\n\n三个坑：\n\n- **路径结尾的 `/` 必须有**:`gitdir:~/work/` 匹配该目录下所有 repo;漏了 `/` 只精确匹配 `~/work` 这一个 repo。\n- **多条命中时后写的赢**:git 按出现顺序逐个 include，后面覆盖前面;更具体的目录规则写在后面。\n- **local 配置仍是最高优先级**:某 repo 里 `git config --local user.email` 设过会盖掉 `includeIf`,这类历史 local 覆盖要清掉才轮到目录规则接管。\n\n验证别直接 `cd` 到父目录 (非 repo 测不出),建个临时 repo 看实际结果：\n\n```bash\nmkdir -p ~/work/.idtest && git -C ~/work/.idtest init -q\ngit -C ~/work/.idtest config user.email   # → you@company.com\nrm -rf ~/work/.idtest\ngit config --global --get-regexp includeIf  # 查所有规则\n```\n\n## 另一种匹配:hasconfig 按远程地址\n\n目录分不开时 (公司 / 个人 repo 混在一处),改用远程 URL 匹配：\n\n```ini\n[includeIf \"hasconfig:remote.*.url:git@github.com:acme/**\"]\n path = ~/.gitconfig_work\n```\n\n- 通配符除标准 glob 外，多了 `**/` 和 `/**` 能**跨多级路径**匹配。`git@github.com:acme/**` = acme 这个 org 下所有 repo;`git@github.com:*/**` 则是 github 上所有 repo。\n- **非显然点 (scan-ahead)**:`gitdir:` 看路径就能判;但 remote URL 通常在 repo 的 `.git/config` 里，比全局 `includeIf` **晚**解析。git 对 `hasconfig` 特殊处理——第一次遇到这个条件时，会**预扫描后面所有配置文件**找 remote URL，所以晚定义的 local remote 照样命中。\n- **代价**:被它 include 进来的文件 (`~/.gitconfig_work`)**禁止再声明任何 remote URL**——否则「是否包含」会反过来取决于「被包含文件里的 remote」,形成死循环，git 直接禁掉这种写法。\n\n按目录 (`gitdir:`) 仍是最简单可靠的默认，只有目录无法区分时才上 `hasconfig`。\n\nSee `man git-config` → \"Conditional includes\"。"},{"title":"Git LFS","slug":"posts/260603-git-lfs","date":"2026-06-03","tags":["git"],"body":"Git 为文本设计：每个版本的每个文件都永久留在历史里。代码这样很好——但二进制文件（视频、PSD、字体、数据集）会被同样对待。一个 50MB 的视频改 5 次，`.git` 就多 ~250MB，而且**每次 clone 都要把全部历史下载下来**。Git 还无法 diff 二进制，只能存整份拷贝，省不掉。\n\n## 原理：把大文件换成 pointer\n\nLFS 在仓库里只存一个 ~130 字节的 _pointer_ 文本文件，真身放到独立的 LFS server：\n\n```text\nversion https://git-lfs.github.com/spec/v1\noid sha256:4d7a214614ab2935c943f9e0ff69d22eadbb8f32b1258daaa5e2ca24d17e2393\nsize 52428800\n```\n\ngit 历史里流动的是这段文本（可 diff、可压缩），`git checkout` 时 LFS 认出 pointer 自动把真身拉回工作区。对你来说体感和普通 git 一样。\n\n```bash\nbrew install git-lfs\ngit lfs install                 # 装 git hooks，每台机一次\n\ngit lfs track \"*.mp4\"           # 写规则到 .gitattributes\ngit add .gitattributes          # 这个文件必须提交，否则队友不会触发 LFS\ngit add intro.mp4               # 正常用 git\ngit commit -m \"add intro video\"\n```\n\n`.gitattributes` 里那条规则长这样：\n\n```text\n*.mp4 filter=lfs diff=lfs merge=lfs -text\n```\n\n## filter=lfs 背后:clean / smudge\n\n`.gitattributes` 里那个 `filter=lfs` 指向 `~/.gitconfig` 中 `git lfs install` 写的一段定义，真正干「真身 ↔ pointer」转换的就是它：\n\n```ini\n[filter \"lfs\"]\n  clean = git-lfs clean -- %f       # add/commit:真身 → pointer\n  smudge = git-lfs smudge -- %f     # checkout:pointer → 真身 (下载)\n  process = git-lfs filter-process  # 上两步的常驻进程批量版\n  required = true\n```\n\n- **clean** 在 `git add` 触发 (工作区 → 仓库),**smudge** 在 checkout 触发 (仓库 → 工作区),`%f` 是文件名占位符。\n- **process** 是性能优化：默认每个文件 fork 一次 clean/smudge,`process` 改用一个常驻进程通过协议批量处理;git 优先用它，clean/smudge 作后备。\n- **required = true** 是关键安全项：过滤器失败就**报错中止**,而非把原始数据放过去。\n\n没装 git-lfs 的队友 clone 出来是 pointer 文本而不是真身，根因就在这——他们 `~/.gitconfig` 里没这段 filter,smudge 根本不会跑。\n\n## track 必须在 add 之前\n\nLFS 的\"拦截\"发生在 `git add` 那一刻：此时它查 `.gitattributes`，匹配就把文件换成 pointer。这是一次性的——**文件一旦以普通方式进了历史，后补规则也救不回来**。\n\n```bash\n# ❌ 错误顺序：视频以 50MB 真身进了历史\ngit add video.mp4 && git commit -m \"add video\"\ngit lfs track \"*.mp4\"           # 规则管不到「已经」发生的提交，仓库照样臃肿\n```\n\n补救要靠 `git lfs migrate`，它**改写历史**把旧文件从 git 挖进 LFS（同 [git filter-repo](260514-git-filter-repo-purge-secrets.md) 一样会变 commit hash，需要 force push + 团队协调）：\n\n```bash\ngit lfs migrate import --include=\"*.mp4\"\n```\n\n类比 Laravel：`git lfs track` 像写了条新 migration 规则，但它不 backfill 旧数据，老记录得专门跑一次 `migrate import` 才能改造。\n\n## file locking：因为二进制无法 merge\n\n文本冲突 git 能三方合并，**二进制不能**——两份 `.psd` 没有\"合并\"一说。于是 Alice、Bob 同时改 `logo.psd`，后推的人只能覆盖或丢掉自己的工。\n\nLFS lock 把 SVN/Perforce 的 _check-out / check-in_ 模型搬回 git：编辑前先认领，期间该文件在别人机器上变只读，server 拒绝他们 push。\n\n```bash\ngit lfs lock   images/logo.psd  # 认领，别人变只读\ngit lfs unlock images/logo.psd  # 干完释放\ngit lfs locks                   # 看谁锁了什么\n```\n\n前提是在 `.gitattributes` 给文件加 `lockable`，这个 flag 让它们默认只读、持锁才可写：\n\n```text\n*.psd filter=lfs diff=lfs merge=lfs -text lockable\n```\n\n锁是协调工具不是强保护——`git lfs unlock --force` 能撬开别人的锁，团队仍要靠沟通。GitHub / GitLab / Bitbucket 都支持 locking。\n\nSee `git help lfs`."},{"title":"git show 读对象数据库","slug":"posts/260603-git-show-object","date":"2026-06-03","tags":["git"],"body":"`git show` 读的是 git **对象数据库**里存的东西，不是磁盘工作区。这个区别决定了它能干 `cat` 干不了的事。\n\n## `<rev>:<path>`：看任意 commit 时刻的文件\n\n```bash\ngit show HEAD:config.php          # 当前 commit 里这个文件的样子\ngit show HEAD~3:config.php        # 往前数 3 个 commit 时的样子\ngit show main:config.php          # main 分支上的样子\ngit show abc1234:src/app.js       # 指定 commit hash\n```\n\n不用 checkout、不动工作区，直接把历史里某一版的文件内容打到 stdout。想对比\"线上分支的这个文件和我本地差在哪\"时特别顺手。\n\n## 为什么 `cat` 看不到 pointer\n\n[LFS](260603-git-lfs.md) / CRLF 转换 / `.gitattributes` 里的 filter，都是在文件**进出工作区时**做的变换。所以同一个文件两个来源不同：\n\n```bash\ncat video.mp4                     # 工作区：被 LFS 还原后的视频真身\ngit show HEAD:video.mp4           # 对象库：git 实际存的 pointer 文本\n```\n\n要看 git 仓库里\"真正存了什么\"，只能走 `git show`（或 `git cat-file -p`）。\n\n## 不带 `:path` 时\n\n```bash\ngit show                          # 最新 commit 的 message + 完整 diff\ngit show abc1234                  # 指定 commit 的 message + diff\ngit show v1.2.0                   # tag 指向的对象\n```\n\n判准：`git show <X>` 看的是\"X 这个对象的内容\"——commit 给你 diff，文件 blob 给你文件内容，tag 给你 tag 信息。\n\nSee `git help show`."},{"title":"折叠生成文件：linguist-generated vs gitlab-generated","slug":"posts/260603-linguist-generated","date":"2026-06-03","tags":["github"],"body":"生成文件（`dist/`、`*.min.js`、lock 文件）有两个麻烦：把仓库语言占比统计带歪，把 PR diff 撑出几千行。两个平台都在 `.gitattributes` 里解决，但**机制差别比想象大**——GitHub 一个 attribute 全包，GitLab 按用途拆成三套，标错只生效一半。\n\n## GitHub：`linguist-*` 一个搞定两件事\n\nGitHub 的语言统计和 diff 折叠都归 [Linguist](https://github.com/github-linguist/linguist) 一套管，所以 `linguist-generated` **一标同时**踢出语言统计 + 折叠 diff：\n\n```text\ndist/* linguist-generated\npackage-lock.json -linguist-generated   # 前导 - = 反向排除\n```\n\n同家族还有 `linguist-vendored`（第三方库）、`linguist-documentation`（文档）、`linguist-language=PHP`（改判语言）。判准：**generated = 机器产物，vendored = 别人的代码**，都被踢出统计，区别只在 diff 折不折叠。\n\n| attribute                | 不计统计 | diff 折叠 |\n| ------------------------ | -------- | --------- |\n| `linguist-generated`     | ✅       | ✅        |\n| `linguist-vendored`      | ✅       | ❌        |\n| `linguist-documentation` | ✅       | ❌        |\n\n## GitLab：三套各管一摊\n\nGitLab 只有**两个** `gitlab-` 专属 attribute，各自只干一件事；**语言统计反而借用 GitHub 的 `linguist-*`**——它的统计引擎 [go-enry](https://github.com/go-enry/go-enry) 是 Linguist 的 Go 移植，认 `linguist-` 前缀。\n\n| 用途              | GitLab 用什么                                    |\n| ----------------- | ------------------------------------------------ |\n| MR diff 折叠      | `gitlab-generated`（专属）                       |\n| 语法高亮改判      | `gitlab-language=prolog`（专属，`=text` 关高亮） |\n| 语言统计排除/改判 | `linguist-generated` / `linguist-vendored` / …   |\n\n**最大的坑**：在 GitLab 标 `gitlab-generated` 只折叠了 diff，文件照样算进语言占比；想同时从统计里拿掉，得再补一条 `linguist-generated`。和 GitHub 一个 attribute 全包正相反。\n\n```text\n*.min.js gitlab-generated      # 只折叠 diff\n*.min.js linguist-generated    # 才会从语言统计条消失\n```\n\n另注：GitLab 会自动识别部分生成文件（protobuf 产物、`go generate` 的 Go 文件、source map），不标也可能折叠，这也是\"有的折有的没折\"不一致反馈的来源（[#572763](https://gitlab.com/gitlab-org/gitlab/-/issues/572763)）。\n\n## 共同点：都是 `.gitattributes` 的 override\n\n`linguist-*` / `gitlab-*` 和 LFS 的 [`filter=lfs`](260603-git-lfs.md)、文件锁 `lockable`、行尾 `text=auto` 堆在同一个文件。记法：**git 原生 attribute（`filter` / `text` / `merge`）各平台通用；带平台前缀的只在对应平台网页生效**，换到本地仓库一律失效。\n\nSee [Linguist overrides](https://github.com/github-linguist/linguist/blob/main/docs/overrides.md) / [GitLab Git attributes](https://docs.gitlab.com/user/project/repository/files/git_attributes/)。"},{"title":"MSA / SOW / SLA / DPA / NDA 区别","slug":"posts/260601-saas-contract-acronyms","date":"2026-06-01","tags":["english","jargon"],"body":"B2B SaaS 谈企业客户时反复出现的五个合同缩写。最常见的错是把它们当**并列**选项挑一个签——实际是**分层**的：一份主协议套着其余几份，各管一层。\n\n口诀：**MSA 定规则，SOW 定范围，SLA 定质量，DPA 定数据，NDA 定保密。**\n\n| 缩写    | 全称                      | 管什么                                | 签几次                  |\n| ------- | ------------------------- | ------------------------------------- | ----------------------- |\n| **MSA** | Master Service Agreement  | 长期合作的法律框架                    | 一次（关系建立时）      |\n| **SOW** | Statement of Work         | 单个项目的范围 / 工期 / 报价 / 交付物 | 每个项目一份            |\n| **SLA** | Service Level Agreement   | 服务质量承诺 + 不达标的赔偿           | 一次（嵌进 MSA 或 SOW） |\n| **DPA** | Data Processing Agreement | 数据怎么存 / 删 / 出境 / 合规         | 一次（常作 MSA 附件）   |\n| **NDA** | Non-Disclosure Agreement  | 保密义务                              | 一次（常在 MSA 之前）   |\n\n## 分层关系\n\n```text\nMSA  主协议（法律框架，长期不变）\n ├── DPA  数据处理协议（附件 Exhibit）\n ├── SLA  服务等级（可在 MSA，也可下放到每份 SOW）\n ├── SOW #1  上线核心模块\n └── SOW #2  做 SSO / 数据对接定制\n```\n\nMSA 是顶层框架，DPA / SLA 通常作为它的附件（_Exhibit_）；SOW 挂在 MSA 下面，一个项目一份。所以听到 \"we have an MSA with them\" 基本等于\"我们是签了长期合同的正式企业客户关系\"。\n\n## MSA 和 SOW 为什么拆开\n\n拆开的价值是**谈判频率不同**：付款、责任上限、保密、终止这类通用条款变化慢，写进 MSA 谈一次管几年；范围、工期、报价这类项目细节每次都变，每个项目单独走一份 SOW，不重谈主合同。后续加功能只追加新 SOW——这就是框架协议省时间的地方。\n\n冲突时谁优先？合同里一般写明 **MSA 优先**，除非某份 SOW 明确声明覆盖某条。判别归属也靠这条：看到\"通用、反复用\"的归 MSA，看到\"这个项目、这个价\"的归 SOW。\n\n## DPA：谁是 controller，谁是 processor\n\n金融 / 医疗 / 涉及 PII 的 SaaS 几乎必签 DPA，核心是把双方角色定死：\n\n- **Data Controller（控制方）**——决定数据用来干什么的一方，通常是**客户**。\n- **Data Processor（处理方）**——替控制方处理数据的一方，通常是 **SaaS 厂商**。\n\n这个区分不是文字游戏：GDPR / CCPA 下两种角色的法律责任不同。DPA 还会写明子处理商（sub-processor，如厂商用的云）清单、数据保留期、合同结束后的删除义务、以及发生 breach 后多少小时内必须通知。\n\n## NDA 已被 MSA 的保密条款覆盖，为什么还单独签\n\n时间点不同。NDA 常在**还没决定合作**时就签——评估、技术对接、看 demo 阶段就要交换敏感信息，而这时 MSA 远没谈成。MSA 里的保密条款只在合作期内、且范围限于这次合作；NDA 是更早、更独立的一道闸。\n\n另一个区别是方向：NDA 可以是 **mutual（双向）** 或 **one-way（单向）**，谁向谁披露、谁负保密义务可以不对称；MSA 的保密条款默认是双向且绑死在主合作上。"},{"title":"English Vowel Reduction: Why Spelling ≠ Pronunciation","slug":"posts/260527-vowel-reduction","date":"2026-05-27","tags":["english","phonetics"],"body":"英语的元音弱化（_vowel reduction_）规律：**重音所在的音节，元音发得清楚；非重读音节的元音，几乎都退化成 `/ə/`（schwa，中央元音）或 `/ɪ/`**。跟字母拼写无关，只跟重音位置有关。\n\n## 同词根，重音移动，读音跟着变\n\n派生词加后缀时，重音常常跳到别的音节，原来被重读的元音就\"塌陷\"成 schwa。\n\n| 基础词          | 音标         | 派生词             | 音标         | 弱化的元音      |\n| --------------- | ------------ | ------------------ | ------------ | --------------- |\n| **au**·thor     | /ˈɔːθər/     | au·**thor**·i·ty   | /əˈθɒrɪti/   | `au` /ɔː/ → /ə/ |\n| **pho**·to      | /ˈfoʊtoʊ/    | pho·**tog**·ra·phy | /fəˈtɒɡrəfi/ | `o` /oʊ/ → /ə/  |\n| **a**·tom       | /ˈeɪtəm/     | a·**tom**·ic       | /əˈtɒmɪk/    | `a` /eɪ/ → /ə/  |\n| **e**·qual      | /ˈiːkwəl/    | e·**qual**·i·ty    | /ɪˈkwɒlɪti/  | `e` /iː/ → /ɪ/  |\n| **te**·le·graph | /ˈtelɪɡrɑːf/ | te·**leg**·ra·phy  | /tɪˈlɛɡrəfi/ | `e` /ɛ/ → /ɪ/   |\n| **Di**·vine     | /dɪˈvaɪn/    | di·**vin**·i·ty    | /dɪˈvɪnɪti/  | `i` /aɪ/ → /ɪ/  |\n\n## 介词、冠词、助动词——弱读形式是默认形式\n\n这类词在句子中几乎永远是非重读的，英语母语者日常会自动用弱读形式，重读版反而显得刻意或强调。\n\n| 词   | 强读（单独说 / 强调时） | 弱读（句中默认） |\n| ---- | ----------------------- | ---------------- |\n| and  | /ænd/                   | /ənd/ 或 /ən/    |\n| to   | /tuː/                   | /tə/             |\n| for  | /fɔːr/                  | /fər/            |\n| of   | /ɒv/                    | /əv/ 或 /ə/      |\n| can  | /kæn/                   | /kən/            |\n| have | /hæv/                   | /həv/ 或 /əv/    |\n| was  | /wɒz/                   | /wəz/            |\n| them | /ðɛm/                   | /ðəm/            |\n\n\"I'll go to the store and get some for you.\" 实际说出来是：  \n`/aɪl ɡoʊ tə ðə stɔːr ən ɡɛt səm fər juː/`  \n其中 `to` / `and` / `for` 全部弱化，这才是自然语速。\n\n## -tion / -ment / -ness 后缀——固定弱化模式\n\n这类后缀本身就是非重读音节，`o` / `e` 恒弱化为 schwa，不因词根变化。\n\n| 词           | 音标          | 弱化点               |\n| ------------ | ------------- | -------------------- |\n| na·tion      | /ˈneɪʃən/     | `-tion` 中 `o` → /ə/ |\n| sta·tion     | /ˈsteɪʃən/    | 同上                 |\n| gov·ern·ment | /ˈɡʌvərnmənt/ | `-ment` 中 `e` → /ə/ |\n| hap·pi·ness  | /ˈhæpinəs/    | `-ness` 中 `e` → /ə/ |\n\n## 为什么会弱化\n\n英语是 _stress-timed_ 语言——重读音节之间的时间间隔大致相等，为了维持节奏，非重读音节必须\"压缩\"。Schwa `/ə/` 是嘴巴最放松时发出的音，发音成本最低，所以成了弱化的终点。\n\n这也是为什么英语听力难：母语者不是\"说清楚每个字母\"，而是在保持节奏的前提下把非重读部分尽量压缩。\n\n## 实践口诀\n\n**重音在哪，元音在哪；离开重音，退化成 schwa。**\n\n背新词时，先标重音位置，再确认非重读音节哪些会弱化——比死记每个字母的读法省力得多。"},{"title":"Task：YAML 写的 Make 替代，自带 sh 解释器","slug":"posts/260526-task-make-alternative","date":"2026-05-26","tags":["devops","build-tool"],"body":"[Task](https://taskfile.dev/) 是 Go 写的 task runner，单二进制 + YAML 配置，定位\"现代 Make 替代\"。同代工具里 [Just](https://github.com/casey/just) 名气更大，但两者其实不在一条线上 —— Task 真正的差异点是**自带 POSIX sh 解释器**和**原生增量 / 并行依赖**，这两点决定了它能当轻量构建系统用，Just 不能。\n\n## Make 真痛点不在 Make 本身\n\nGNU Make 三大平台都有，跨平台坑出在 Makefile 里每条命令依赖的**宿主 shell 和 coreutils**：\n\n- **Shell 方言**：`/bin/sh` 在 Ubuntu 是 dash、在老 macOS 是 bash、Alpine 是 ash，`[[ ]]` / 数组 / `echo -e` 行为各不一样。本机 macOS 跑通的 Makefile 一进 CI 就 silently 行为漂移\n- **Coreutils 分裂**：macOS 自带 **BSD 版** `sed` / `find` / `xargs`，GNU 习惯的 `sed -i 's/x/y/g' f` 在 BSD 上必须写成 `sed -i '' 's/x/y/g' f`（`-i` 必须吃一个空字符串参数）；GNU 的 `find -printf` / `xargs -r` / `date -d` 在 BSD 上整个不存在\n- **Tab vs 空格**：Makefile 命令行必须 Tab 开头，VS Code 默认设置把 Tab 转空格 → `missing separator` 红一片\n\n要让一个 Makefile 跨 macOS / Linux 都跑通，迟早塞进 `ifeq ($(shell uname),Darwin)` 分支，维护成本爆炸。\n\n## Task 解法：bundled sh + 原生 incremental\n\nTask 内嵌 [mvdan/sh](https://github.com/mvdan/sh)，一个纯 Go 实现的 POSIX shell 解释器。`for f in src/*.go; do echo $f; done` 不再走 `/bin/sh`，macOS / Linux 行为一致，dash vs bash 的\"在我机器上能跑\"问题消失。\n\n**边界要写清楚**：Task 只 bundle 了 shell **语法**，没 bundle coreutils。`rm` / `sed` / `find` 仍然调系统的那份 —— BSD `sed -i ''` vs GNU `sed -i` 的坑 Task 不替你解决，要么显式 `gsed`，要么改写成只用 POSIX 公共子集。这一点官方文档也提：_\"any executables called must be available as a built-in or in the system's PATH.\"_\n\n增量构建走 `sources` + `generates`：\n\n```yaml\nversion: '3'\n\ntasks:\n  build:\n    cmds:\n      - go build -o bin/app ./cmd/app\n    sources:\n      - ./**/*.go\n    generates:\n      - bin/app\n\n  test:\n    deps: [build]          # deps 列表默认并行\n    cmds:\n      - go test ./...\n```\n\n- `sources` 里所有文件算 **checksum**（可切 `method: timestamp`），下次跑发现指纹没变就跳过该 task —— 大项目 `task build` 没改代码时几毫秒返回\n- `deps:` 默认**并行**执行（前提是 deps 之间互不依赖），串行用 `cmds:` 里 `- task: foo` 调用\n- 配合 `task --watch` 监听文件变化自动重跑\n\n## 跟 Just 的分野\n\nJust 和 Task 经常被并列推荐，但 Just README 自己写得很清楚：_\"just is a command runner, not a build system, so it avoids much of make's complexity and idiosyncrasies.\"_ —— **故意不做依赖图也不做增量**，任务每次从头跑。\n\n| 维度      | Just                                 | Task                             |\n| --------- | ------------------------------------ | -------------------------------- |\n| 定位      | command runner（不是 build system）  | task runner + 轻量 build tool    |\n| Shell     | 走宿主 shell                         | 自带 mvdan/sh，跨平台一致        |\n| 依赖并行  | 串行                                 | 默认并行                         |\n| 增量跳过  | 无                                   | `sources` + `generates` checksum |\n| `--watch` | 无                                   | 内置                             |\n| 语法      | 自定义 DSL（精简 Makefile 风）       | YAML                             |\n| 参数传递  | 位置参数一等公民：`just deploy prod` | 走变量：`task deploy ENV=prod`   |\n\n判准：\n\n- 项目里命令独立、每次都该完整跑（lint、deploy、clear-cache） → Just，手感更轻\n- 任务有产物链、改一个文件只想重建受影响下游 → Task\n- 命令需要传位置参数（`just deploy prod`） → Just 直观\n- 团队里 Makefile 已经在用 `sources / .PHONY / -j` 这些 Make 特性，想替换 → Task 一对一替\n\n装：`brew install go-task`。文档：<https://taskfile.dev/>。"},{"title":"httpbingo.org —— 按需返回任意 HTTP 响应","slug":"posts/260526-httpbingo","date":"2026-05-26","tags":["network"],"body":"写 HTTP 客户端时最难造的是失败：500、慢响应、3xx 链、basic auth 401。靠真实业务 API 不可控也污染数据。[httpbingo.org](https://httpbingo.org/) 就是干这个——按 URL 告诉它要什么响应，它就回什么。\n\n```bash\ncurl https://httpbingo.org/status/503         # 强制 503\ncurl https://httpbingo.org/delay/5            # 5 秒后响应（测超时）\ncurl https://httpbingo.org/redirect/3         # 3 次 302 链（测 follow 上限）\ncurl -u user:pass https://httpbingo.org/basic-auth/user/pass\ncurl https://httpbingo.org/anything           # 把请求 headers / body / query 原样 echo 回来\n```\n\n`/anything` 是 debug 神器——客户端实际发了什么 header、是不是按预期带了 cookie、proxy 有没有偷改 body，一行 curl 看到。\n\n域名容易和老前辈 httpbin.org 混。httpbingo.org 是 `mccutchen/go-httpbin` 跑的公网实例，Kenneth Reitz 那个老 httpbin 的 Go 重写。老版自 v0.6.1（2017-08）后没再发版，README 还挂着 `travis-ci.org` 的 badge（travis 早已对 OSS 停服）。go-httpbin 零依赖、stdlib only、持续发版（v2.22 / 2026），多了 `/sse` `/jsonl` `/websocket/echo`，少了 `/brotli`（Go stdlib 不带）。\n\n## CI 里别直接打公网\n\n免费服务限流 / 下线一次就 red 一片。CI 里起个 sidecar：\n\n```bash\ndocker run -p 8080:8080 ghcr.io/mccutchen/go-httpbin\n```\n\n写 Go 的话还能直接当 library 嵌进单测起 `httptest.Server`，连 sidecar 都省了。"},{"title":"CONCAT 遇到 NULL：MySQL vs PostgreSQL","slug":"posts/260526-concat-null-mysql-vs-postgres","date":"2026-05-26","tags":["postgresql","mysql"],"body":"`CONCAT(a, NULL, b)` 在 MySQL 是 `NULL`，在 PostgreSQL 是 `'ab'`。**默认行为相反**，跨库写 SQL 极易踩。\n\n```sql\n-- MySQL 8.0\nSELECT CONCAT('hello', NULL, 'world');  -- NULL\n\n-- PostgreSQL 16\nSELECT CONCAT('hello', NULL, 'world');  -- 'helloworld'\n```\n\n## 为什么相反\n\nSQL 标准守三值逻辑：`NULL` 表示「未知」，未知和任何值拼接结果仍是未知，所以传染整个表达式。MySQL 的 `CONCAT` 按这条走；PostgreSQL 引入 `CONCAT()` 时刻意违反标准 —— 文档直接写 _NULL arguments are ignored_ —— 理由是实战拼姓名 / 地址 / URL 时 NULL 传染几乎从来不是想要的行为。\n\n`||` 是 SQL 标准的字符串拼接操作符，两个 DB 都让 NULL 传染：\n\n```sql\n-- PostgreSQL\nSELECT 'hello' || NULL || 'world';  -- NULL\n```\n\nMySQL 默认 `||` 是逻辑 OR，要 `SET sql_mode = 'PIPES_AS_CONCAT'` 才当拼接 —— 所以 MySQL 项目里基本看不到 `||` 拼字符串。\n\n记口诀：**`CONCAT` 跨库不一致，`||` 跨库一致传染**。\n\n## 写法对照\n\n| 写法                           | MySQL    | PostgreSQL |\n| ------------------------------ | -------- | ---------- |\n| `CONCAT(a, NULL, b)`           | `NULL`   | `'ab'`     |\n| `CONCAT_WS(',', a, NULL, b)`   | `'a,b'`  | `'a,b'`    |\n| `CONCAT_WS(NULL, 'a', 'b')`    | `NULL`   | `NULL`     |\n| `CONCAT_WS(',', 'a', '', 'b')` | `'a,,b'` | `'a,,b'`   |\n| `a \\|\\| NULL \\|\\| b`           | 逻辑 OR  | `NULL`     |\n\n`CONCAT_WS` 跨库一致：其余 NULL 自动跳过，但**分隔符自己是 NULL 整表达式仍是 NULL**、**空串保留不当 NULL** —— 想把空串也跳过，外层先 `NULLIF(col, '')`。\n\n最常踩 `CONCAT(first_name, ' ', middle_name, ' ', last_name)` —— `middle_name` 为 `NULL` 时整个名字消失，前端只显示空白，业务侧不报错。改 `CONCAT_WS(' ', first_name, middle_name, last_name)` 立刻修好。"},{"title":"二进制安全 是什么意思","slug":"posts/260525-binary-safe","date":"2026-05-25","tags":["php","password-hash"],"body":"[PHP 文档里 `crypt()`](https://www.php.net/manual/en/function.crypt.php) 顶上挂着一行：\n\n> Warning: This function is not (yet) binary safe!\n\nbinary safe 指函数能完整处理任意字节序列，含 NUL 字节（`\\0`、`0x00`）。失安全的两种姿势：**遇 `\\0` 截断**、**结果随 locale 变化**。\n\n## crypt() 怎么\"不 binary safe\"\n\n底层是 C `crypt(3)`，C 字符串以 `\\0` 终止：\n\n```php\n$salt = '$2y$10$' . str_repeat('a', 22);\ncrypt(\"secret\\0extra1\", $salt) === crypt(\"secret\\0extra2\", $salt);  // true\n```\n\nC 端只读到 `\"secret\"`，`\\0` 后的字节全部丢弃。把 `random_bytes()` 出来的 32 字节 key 当密码直接喂 `crypt()` / `password_hash()` 时这就是定时炸弹 —— 32 字节里含 `\\0` 的概率是 `1 - (255/256)^32 ≈ 12%`，碰上就把高熵 key 蒸发掉一截。\n\n修法是先编码再哈希：\n\n```php\n$key = random_bytes(32);\npassword_hash(base64_encode($key), PASSWORD_BCRYPT);\n```\n\n更对路的做法：哈希二进制 key 别走 `crypt` 家族，用 `hash_hmac('sha256', $msg, $key)`，它本身 binary safe。\n\n## \"(yet)\" 是什么意思\n\n不是 typo —— 是文档作者留的\"想修但修不动\"的尾巴。`crypt()` 走 libc，要 binary safe 得绕开 libc 自己实现一份，跨平台一致性破坏。从 PHP 4 挂到 PHP 8，括号里这两字大概会一直挂下去。\n\n## 不 binary safe 分两类\n\n| 类型        | 典型函数                                                                 | 触发                 |\n| ----------- | ------------------------------------------------------------------------ | -------------------- |\n| `\\0` 截断   | `crypt()`、所有 fs 路径参数（`fopen` / `include` / `file_get_contents`） | 入参含 `\\0`          |\n| locale 依赖 | `strcoll`                                                                | 字节落在 `0x80-0xFF` |\n\n路径那条历史上是 [CVE-2006-7243](https://nvd.nist.gov/vuln/detail/CVE-2006-7243) —— `include $_GET['page'] . '.php'` + 攻击者传 `evil.so\\0` 能绕扩展名白名单包含任意文件。PHP 5.3.4 起 fs 函数拒收含 `\\0` 的路径（PHP 8.0 起直接抛 `TypeError`），但**拼接前先过用户输入**仍是写代码的人的责任。\n\nlocale 那条更隐蔽 —— `strcoll` 按 `LC_COLLATE` 排序，同样两字节给相反答案：\n\n```php\nsetlocale(LC_COLLATE, 'C');\nstrcoll(\"\\xE4\", \"z\");                    // > 0（按字节 0xE4 > 0x7A）\n\nsetlocale(LC_COLLATE, 'de_DE.UTF-8');\nstrcoll(\"\\xE4\", \"z\");                    // < 0（德语排序 ä 紧挨 a，在 z 前）\n```\n\n同一段代码在 CI（默认 `C`）、生产容器（可能装了 `de_DE.UTF-8`）、本地 mac 跑出不同结果，debug 极难找。要可移植的国际化排序走 `Collator`（intl 扩展）显式给 locale；要大小写转换走 `mb_strtolower($s, 'UTF-8')` 明确指定编码 —— `strtolower` 自 PHP 8.2 起只处理 ASCII，对非 ASCII 字节原样返回。\n\n## 哪些是 binary safe 的\n\n`strlen` `strpos` `strrpos` `substr` `str_replace` `strcmp` `strncmp` `explode` `implode` `hash` `hash_hmac` `fread` `fwrite` `md5` `sha1` —— 核心字符串和 hash 族基本全是。判别口诀：函数文档说\"以字节为单位\"且不依赖 locale。\n\n> `strcmp()` 是 binary safe，但**不是 constant-time** —— 校验 token / HMAC / CSRF 用 `hash_equals()`，否则吃时序攻击。两个正交属性，别混。\n\n`mb_*` 系列是**多字节安全**（认得 UTF-8 多字节序列），不等于\"任意二进制安全\" —— 喂非法 UTF-8 字节会报警或返回 `false`。"},{"title":"caffeinate：macOS 临时防睡眠","slug":"posts/260525-caffeinate","date":"2026-05-25","tags":["shell","macos"],"body":"macOS 自带 `/usr/bin/caffeinate`，临时阻止系统进入睡眠。最常见用法是给一条长任务套一层 caffeinate，命令跑完它自己退：\n\n```bash\ncaffeinate -i ./long-task.sh        # long-task.sh 跑完，防睡眠也撤销\n```\n\n核心设计：**caffeinate 的生命周期跟着另一个进程走**。子命令结束它跟着退，不会忘关导致整天不睡。\n\n## flag 阻止不同层级的「睡」\n\n| flag       | 阻止的层级                                                     |\n| ---------- | -------------------------------------------------------------- |\n| `-i`       | system idle sleep（默认，最常用）                              |\n| `-d`       | display sleep（屏幕睡）                                        |\n| `-m`       | disk idle sleep（磁盘空闲休眠）                                |\n| `-s`       | system sleep —— **仅在插电源时生效**                           |\n| `-u`       | user-active assertion（声明用户活跃；屏幕关着会唤醒；默认 5s） |\n| `-t <秒>`  | 给 assertion 加上限，到点自动撤销                              |\n| `-w <pid>` | 跟某 PID 命运绑，那 PID 退出就退                               |\n\n`caffeinate` 不带任何 flag 等同于 `-i` —— 很多教程不点明，以为「随便加 caffeinate」就万事大吉，结果屏幕照睡。需要屏幕也亮着（演示 / 仪表盘看板 / 长亮场景）就显式加 `-d`：\n\n```bash\ncaffeinate -di\n```\n\n## 四种生命周期\n\n对应四种调用形态：\n\n```bash\ncaffeinate -i -t 7200          # 接下来 2 小时不空闲睡，到点自动退\ncaffeinate -i ./long-task.sh   # 跟着子进程走，子进程结束就退\ncaffeinate -i -w 12345         # 跟某已有 PID 命运绑，PID 退出就退\ncaffeinate -i                  # 永远不退，要 Ctrl+C\n```\n\n`-w` 是事后补救手段：长任务已经在跑了才想起套 caffeinate，不用 kill 重启，`caffeinate -i -w \"$!\"` 抓刚 bg 的任务，或 `caffeinate -i -w $(pgrep -f long-task)` 抓按名找到的 PID。\n\n第四种是初学者最常踩的坑 —— 开了一个 `caffeinate -i` 然后 Ctrl+Z + `bg`（或一开始就 `&` 后台化），关掉那个终端 tab，zsh 默认 `nohup` on **不向后台任务转发 SIGHUP**，进程留在后台，Mac 几天不睡。避免方式：**永远用前三种，绝不裸跑**。即使「先开着以防万一」也强制带 `-t <秒>` 给个上限。\n\n怀疑有残留时：\n\n```bash\npgrep -lf caffeinate     # 列出当前所有 caffeinate 进程及参数\npkill caffeinate         # 全部杀掉（确认没有正在用的）\n```\n\n## `-s` 为什么只在插电源时生效\n\n苹果的设计假设：电池模式下用户「确实希望它睡」，否则损耗续航。要在电池模式下也不睡，用 `-i` —— idle sleep 在两种电源模式都能挡住。`-s` 是写给「插电时跑 CI / build 别中途睡」这类场景的。\n\n## debug：谁在阻止 sleep\n\nMac 莫名不睡时，看当前所有 PowerManagement assertion：\n\n```bash\npmset -g assertions\n```\n\n会列出每个进程持有的 assertion（`PreventUserIdleSystemSleep`、`PreventUserIdleDisplaySleep` 等）。caffeinate 在底层调用同一套 `IOPMAssertion*` API；Amphetamine 这类 GUI 工具、视频会议、多媒体播放也通过同样机制声明 assertion。`pmset -g assertions` 是排查根因的金钥匙。\n\n## 跟 `pmset` / GUI 工具区分\n\n- `caffeinate` —— 临时声明，进程级，跟生命周期绑\n- `pmset` —— 改全局电源配置，持久（`sudo pmset -a sleep 0` 是核选项）\n- Amphetamine / Lungo —— GUI 包装的 assertion，本质同 caffeinate\n\n`man caffeinate` 看完整文档。"},{"title":"tmux：detach 让进程独立于窗口","slug":"posts/260525-tmux-detach","date":"2026-05-25","tags":["shell"],"body":"iTerm2 + tmux 不是二选一，是叠两层。iTerm2 画像素（terminal emulator），tmux 在里面再开一组逻辑终端（terminal multiplexer）—— **核心收益是 _detach_**：关窗、断 SSH、合盖，里面的进程都不死，下次 `tmux a` 接回来。再加 session / window / pane 三层结构，一个工作区拆成多视图共存。\n\n## iTerm2 跟 tmux 各管什么\n\n| 维度            | iTerm2                 | tmux                                             |\n| --------------- | ---------------------- | ------------------------------------------------ |\n| 角色            | terminal emulator      | terminal multiplexer                             |\n| 实现            | 画 PTY master 端的像素 | 一个 server 进程持有多个 PTY pair                |\n| 关掉窗口        | 里面 zsh 收 SIGHUP 死  | client 断开，server 不受影响，PTY 和子进程继续跑 |\n| 多窗口 / 分屏   | 原生 tab / split       | session / window / pane 三层                     |\n| 多客户端 attach | 不支持                 | 同一 session 多客户端同时连（结对编程刚需）      |\n\ntmux server 给每个 pane 自己 `openpty()` 拿一对 PTY，里面跑的 shell 以为自己面对真终端。client 跟 server 通过 socket（`/tmp/tmux-501/default`）通信 —— **client 死了 server 不死**，这是 detach 的根因。\n\n## 三层结构\n\n```text\nserver\n└── session \"mywork\"             ← detach / attach 的单位\n    ├── window 0 \"edit\"           ← 一个 window 一个 tab\n    │   ├── pane 0  vim\n    │   └── pane 1  rg            ← 一个 window 内的 split\n    └── window 1 \"test\"\n        └── pane 0  pytest --watch\n```\n\n记忆口诀：session 是工作区，window 是 tab，pane 是 split。\n\n## 4 个 session 命令\n\n下面 `mywork` 是 session 名（自取），不是命令：\n\n```bash\ntmux new -s mywork              # 创建并 attach\ntmux ls                         # 列出所有 session\ntmux a -t mywork                # attach 到名为 mywork 的 session\ntmux kill-session -t mywork     # 结束某个 session\n\ntmux new-session -A -s mywork   # 幂等：没有就建，有就 attach\n```\n\n最后一条的 `-A` 是写脚本和 `.zshrc` 别名时的核心 —— 同一别名反复跑永远落到同一 session，不会因为重跑而创建第二个。\n\n## 必记的 4 个 prefix 键\n\n`-CC` 模式下切 window / pane / 滚屏 / 复制全被 iTerm2 native 接管（⌘ 系列），不再需要记几十个 tmux 按键。剩下这 4 个还得手动：\n\n| 键           | 作用                                |\n| ------------ | ----------------------------------- |\n| `prefix + d` | detach（最高频）                    |\n| `prefix + ?` | 列所有按键（忘了直接查）            |\n| `prefix + :` | 进命令模式（什么都能干）            |\n| `prefix + z` | 当前 pane 全屏 / 还原（看长输出用） |\n\n默认 prefix 是 `Ctrl-b`，先按 prefix 松开，再按命令键，**不是同时按**。\n\n不用 `-CC`（远端 SSH / Ghostty / Alacritty）时按键多用一些 —— 那就靠 `prefix + ?` 现场查，不必硬记。\n\n## detach 能扛哪些场景\n\n```bash\ntmux a -t mywork            # 合盖再开，session 状态完整\n```\n\n| 场景          | 没 tmux           | 有 tmux                                         |\n| ------------- | ----------------- | ----------------------------------------------- |\n| `⌘Q` 关 iTerm | 进程收 SIGHUP 死  | 继续跑                                          |\n| SSH 断线      | 进程死            | 继续跑                                          |\n| Mac 空闲睡眠  | 全机 SIGSTOP 暂停 | 全机 SIGSTOP 暂停（tmux 不解决，需 caffeinate） |\n| Mac 重启      | 进程必死          | 进程必死，session 元信息靠 tmux-resurrect       |\n\nMac 重启时 `tmux-resurrect` 能再画一遍 window / pane 布局，但**默认只恢复布局不重起程序** —— 除非 `@resurrect-processes` 配白名单（`'~vim->vim *' 'ssh->ssh'` 这种 pattern）。\n\n## iTerm2 + tmux -CC\n\niTerm2 独有的 `-CC` 控制模式：\n\n```bash\ntmux -CC new-session -A -s mywork\n```\n\ntmux 的 window / pane 被 iTerm2 渲染成**原生 tab / split**，复制粘贴、滚动、字体、tab 切换全走 iTerm2，detach 持久能力同时保留。肌肉记忆零迁移，是 iTerm2 用户的最佳形态。\n\n## 长跑 agent 实战\n\nClaude Code / Codex 跑大重构、批量测试改写，单次任务 30 分钟到几小时起步。关掉终端 / Mac 睡了，烧掉的 token 全废。配进 `~/.zshrc`：\n\n```bash\ncc() { tmux new-session -A -s \"cc-$(basename \"$PWD\")\" -c \"$PWD\" \"caffeinate -i claude\"; }\n```\n\n每个项目目录一个独立 session，`tmux ls` 一眼看全。`caffeinate -i` 套在 tmux **内层** —— caffeinate 命脉跟 claude 进程绑，detach 不影响。**反过来包错位**（`caffeinate -i tmux ...`）会出问题：caffeinate 跟外层 tmux 命令绑，detach 后 caffeinate 退出，Mac 该睡照睡，session 里的 claude 被 SIGSTOP 暂停。\n\ntmux 和 caffeinate 是两条正交的防线：tmux 让 agent 跟窗口脱钩，caffeinate 让 Mac 不进睡眠，缺一边都漏。agent 的对话上下文不在 tmux 这一层 —— 它存在 Claude Code 自己的 session 文件里（路径按 cwd 编码，如 `~/.claude/projects/-Users-you-myproj/`），靠 `--resume` 接回。\n\n## 同类怎么选\n\n| 选      | 何时                                    |\n| ------- | --------------------------------------- |\n| tmux    | 远程 SSH / 长跑任务 / 多 client attach  |\n| screen  | 老服务器装不上 tmux 时的 fallback       |\n| Zellij  | 本地、不想写配置、要按键提示和悬浮 pane |\n| WezTerm | 本地、要 GPU 加速 + 内置多路复用        |\n\nscreen 也能 detach + multi-attach（`screen -x`），但脚本化接口糙、配置默认值停留在 90 年代，agent 时代的工具链（mosh / overmind / 各家 AI CLI 的 tmux integration）几乎都只面向 tmux。\n\n`man tmux` 看完整命令；按键速查见 [tmux cheat sheet](https://tmuxcheatsheet.com/)。"},{"title":"failure 与 /-jər/ 词尾","slug":"posts/260522-failure-jer-ending","date":"2026-05-22","tags":["english","pronunciation"],"body":"failure /ˈfeɪljər/ 的坑：拼写 -ure 容易被中文母语者读成 \"your\" 的尾音，实际是 /-jər/ —— 一个 /j/（y 滑音）紧跟 /ər/。\n\n把它拆成 **fail + y + er**，重音在 fail。\n\n## 字母 U / I 在词尾担任 /j/ 滑音\n\n英语词尾 -ure / -ular / -liar 几乎都是这个套路：**U 或 I 不发元音，担任 /j/ 滑音**。\n\n| 词尾  | 发音    | 例                                           |\n| ----- | ------- | -------------------------------------------- |\n| -ure  | /jər/   | failure, figure /ˈfɪɡjər/                    |\n| -ular | /jələr/ | regular /ˈreɡjələr/, popular, particular     |\n| -liar | /ljər/  | familiar /fəˈmɪljər/, peculiar /pɪˈkjuːljər/ |\n\n念法套同一模式：fail-y-er、fig-y-er、reg-y-ler、fa-mil-y-er。\n\n## 变体：-sure 词尾的 /ʒər/\n\n```text\nmeasure  /ˈmeʒər/\npleasure /ˈpleʒər/\ntreasure /ˈtreʒər/\nleisure  /ˈliːʒər/\n```\n\nU 仍然担任 /j/ 滑音，只是和前面的 `s` 融合（palatalization），合成一个浊擦音 /ʒ/，表面看不见 U 的痕迹了。\n\n/ʒ/ 汉语里没有，最近似 `热 / 日 / 人` 的声母 /ʐ/ —— 把翘舌放平就是 /ʒ/。别和拼音的 `zh` 混：`zh` 是塞擦音 /ʈʂ/（先停后擦），跟 /ʒ/ 不是一类音。"},{"title":"fzf 一行做 SSH host picker","slug":"posts/260522-fzf-ssh","date":"2026-05-22","tags":["shell","ssh"],"body":"`~/.ssh/config` 已经把所有 host 写好了，fzf 把它当数据源就有了 picker，preview 窗口顺便显示选中 host 的完整配置：\n\n```bash\ns() {\n  local config=\"${HOME}/.ssh/config\"\n\n  [[ -f \"$config\" ]] || { echo \"no $config\"; return 1; }\n  command -v fzf >/dev/null || { echo \"fzf not installed\"; return 1; }\n\n  local hosts\n  hosts=$(awk 'tolower($1)==\"host\" && $2 !~ /\\*/ {print $2}' \"$config\")\n  [[ -z \"$hosts\" ]] && { echo \"no hosts in $config\"; return 1; }\n\n  local host\n  host=$(echo \"$hosts\" | fzf \\\n    --preview \"awk '/^Host / && \\$2 == \\\"{}\\\" {p=1; next} /^Host / {p=0} p' $config\" \\\n    --preview-window 'right:50%')\n\n  [[ -n \"$host\" ]] && ssh \"$host\"\n}\n```\n\n跑 `s` → fzf 弹列表 → 选 → SSH 上。光标停某个 host 时**右半屏实时显示该 host 的 `HostName` / `User` / `Port` / `IdentityFile`**，连之前一眼确认是不是要那台。\n\n几个细节：\n\n- 三个 fail-fast 检查（config 文件存在 / fzf 命令在 / awk 出非空），任一项不满足立刻报具体原因，不静默退出\n- `awk tolower($1)==\"host\"` 大小写不敏感识别（ssh config 本身就不区分），过滤 `Host *.dev` 这种通配符规则，`$2` 取 host 别名\n- preview 那条 awk 是个状态机：匹配到 `Host <选中>` 起 `p=1` 开始打印，遇到下一个 `Host` 行 `p=0` 停止——精确抓单段配置，比 `grep -A 10` 更准（不会跨到下一段）\n- preview 字符串的转义陷阱：外层用 `\"...\"` 让 fzf 展开 `{}` 和 shell 展开 `$config`，内层 `\\$2` / `\\\"` 转义是为了让 shell 把 `$2` 和 `\"` 原样递给 awk\n- `[[ -n \"$host\" ]]` 空选保护，避免 `ssh \"\"` 报错\n\n## 维护 `~/.ssh/config`\n\npicker 的数据源是 `~/.ssh/config`，没这文件 picker 空。最小骨架：\n\n```text\nHost blog\n    HostName 1.2.3.4\n    User deploy\n    Port 22\n    IdentityFile ~/.ssh/blog_ed25519\n\nHost *.dev\n    IdentityAgent ~/.bitwarden-ssh-agent.sock\n    AddKeysToAgent yes\n    IgnoreUnknown UseKeychain\n    UseKeychain yes\n```\n\n几个不踩坑必知：\n\n- **文件权限必须 `chmod 600 ~/.ssh/config`**，权限太松 ssh 直接拒绝读取\n- `Host` 一行可以多别名空格分隔：`Host blog blog2 mysite` 三个名字都连同一台\n- 行内 4-空格缩进非语法必须，但**强烈建议**——stanza 边界清晰，picker 的 awk 解析也稳\n- 项目 / 客户多时用 `Include ~/.ssh/config.d/*.conf` 拆文件（工作的、个人的、临时的分开）"},{"title":"Argon2id PHC 串拆解：salt 来源与 Base64 编码","slug":"posts/260521-argon2id-phc-anatomy","date":"2026-05-21","tags":["php","argon2","base64","password-hash"],"body":"PHP `password_hash($pwd, PASSWORD_ARGON2ID)` 吐出来这种东西：\n\n```text\n$argon2id$v=19$m=65536,t=4,p=1$UlhrVTlVbVI2S08wa2szSQ$gGGk/8NtsxcSbFOxcSkWOSf3fFE/a2KbMEqRiTrFmes\n```\n\n这是 [PHC string format](https://github.com/P-H-C/phc-string-format) —— 2013-2015 年那场 **Password Hashing Competition** 顺带产出的字符串格式规范（竞赛本身是算法选秀，Argon2 胜出后由 [RFC 9106](https://www.rfc-editor.org/rfc/rfc9106) 标准化；格式规范是副产品，让所有现代密码 hash 都能用自包含字符串表达，便于跨算法升级 / 跨语言迁移）。6 段用 `$` 分隔。\n\n```text\n$<id>[$v=<version>][$<param>=<value>(,<param>=<value>)*][$<salt>[$<hash>]]\n```\n\n| 段   | 值                                            | 含义                           |\n| ---- | --------------------------------------------- | ------------------------------ |\n| 算法 | `argon2id`                                    | 变体（不是 argon2i / argon2d） |\n| 版本 | `v=19`                                        | Argon2 v1.3（0x13）            |\n| 参数 | `m=65536, t=4, p=1`                           | 内存 64 MiB、迭代 4 轮、并行 1 |\n| Salt | `UlhrVTlVbVI2S08wa2szSQ`                      | 22 字符 → 解码后 16 字节       |\n| Hash | `gGGk/8NtsxcSbFOxcSkWOSf3fFE/a2KbMEqRiTrFmes` | 43 字符 → 解码后 32 字节       |\n\n## Salt 必须来自 CSPRNG\n\n**CSPRNG** = Cryptographically Secure Pseudo-Random Number Generator（密码学安全的伪随机数生成器）。判别标准：next-bit 不可预测 + 状态泄露不能反推历史输出 + seed 自身来自熵源。`rand()` / `mt_rand()` 都不满足。\n\n`password_hash()` 内部路径：\n\n```text\npassword_hash()\n  → php_password_make_salt()\n    → php_random_bytes_silent(16)\n      → getrandom(2) (Linux) / arc4random (macOS)\n        → OS 熵池（硬件中断、键盘 / 鼠标时序、磁盘 I/O 抖动）\n```\n\n等价于：\n\n```php\n$salt = random_bytes(16);\n```\n\n**永远不要用以下函数生成 salt / token / key**：\n\n| 不安全             | 替代                        |\n| ------------------ | --------------------------- |\n| `rand()`           | `random_int($min, $max)`    |\n| `mt_rand()`        | `random_int($min, $max)`    |\n| `uniqid()`         | `bin2hex(random_bytes(16))` |\n| `md5(microtime())` | `random_bytes(N)`           |\n\n`mt_rand()` 是 Mersenne Twister，速度快但**状态可被反推** —— 拿到约 624 个连续输出就能完全恢复内部状态、预测后续所有值。已有 [php_mt_seed](https://github.com/openwall/php_mt_seed) 在毫秒级破解。\n\nsalt 不需要保密（和 hash 一起明文存数据库），它的作用是：\n\n1. 让相同密码产生不同 hash，rainbow table 失效\n2. 强制攻击者**逐个**密码爆破，不能批量\n\n## 字节 ↔ 字符：编码的换算\n\n字节是 8 bit 的存储单位；字符是表示单位。两者的转换由编码定义。\n\n**为什么不能 1 字节 → 1 字符？** 一个字节有 256 种值（0~255），但可打印 ASCII 只有 **95 个**（`0x20`~`0x7E`，即 128 个 7-bit ASCII 编码点扣掉 33 个控制符 —— `0x00~0x1F` + `0x7F`。控制符是 NUL / Tab / 换行 / Esc / DEL 等，不能透传文本，所以 128 根本不是候选档位）。1:1 至少缺 161 个对应。退一步：能不能扩到 256 字符？\n\n| 候选方案            | 为什么不行                                                                         |\n| ------------------- | ---------------------------------------------------------------------------------- |\n| 全 256 ASCII        | `0x00~0x1F` 是 NUL / Tab / 换行 / Esc，终端、字符串、协议都会炸                    |\n| Latin-1 全 256 字符 | UTF-8 时代字节 `0x80~0xFF` 不是合法 UTF-8 序列，存数据库 / 走 JSON / 进 URL 全报错 |\n| 强行透传二进制      | URL、JSON、shell、HTTP header、email、`.env`、`grep` 输出全部假设 ASCII-safe       |\n\n所以真正约束是：**字符集必须落在「绝对安全的可打印 ASCII 子集」里**。理论上界 95，实操还要扣掉 `\"` `\\` `<` `>` `&` `=` `+` `?` `#` 等在 URL / JSON / shell 里有特殊含义的字符，安全集合一般 60~80 个。各档编码的取舍：\n\n| 字符集大小 | 编码                | bit/字符 | 膨胀率 | 16 字节后 |\n| ---------- | ------------------- | -------- | ------ | --------- |\n| 16         | Hex                 | 4        | 2×     | 32 字符   |\n| 32         | Base32              | 5        | 1.6×   | 26~32     |\n| 64         | Base64              | 6        | 1.33×  | 22~24     |\n| 85         | Base85 / Ascii85    | 6.4      | 1.25×  | 20~21     |\n| 95         | 全可打印 ASCII 上界 | ~6.57    | 1.22×  | 20        |\n\nBase64 字符表是 `A-Z a-z 0-9 + /`（62 个字母数字 + 2 个填空），「安全字符够多」+「6 bit 整齐对齐 24 bit 包」的双重最优解：再上一档要 128 字符，超出可打印 ASCII 上界；再下一档 32 字符膨胀率明显更高。\n\nBase64 把每 **3 字节** 打包成 **4 字符**，因为 `LCM(8, 6) = 24 bit` —— 字节 8 bit 和 Base64 字符 6 bit 不能整除，得找两边都数得齐的最小公共单位。24 bit 既是 3 个字节，也是 4 个 Base64 字符：\n\n```text\n[byte 1] [byte 2] [byte 3]      ← 24 bit\n 6 bit  6 bit  6 bit  6 bit\n[char 1][char 2][char 3][char 4]\n```\n\n试别的字节数都凑不齐：\n\n| 输入字节 | bit | ÷ 6 = 字符 | 问题                   |\n| -------- | --- | ---------- | ---------------------- |\n| 1        | 8   | 1.33       | 余 2 bit，半字符存不下 |\n| 2        | 16  | 2.67       | 余 4 bit，半字符存不下 |\n| **3**    | 24  | **4**      | **整除**               |\n| 4        | 32  | 5.33       | 回到 8 bit 周期        |\n\n所以 3 字节是「最小成块单位」，每 3 字节切一刀、吐 4 字符、循环。输入不是 3 的倍数时，结尾凑不齐 24 bit，要补 0 bit 到下一个 6 bit 边界，再用 `=` 标记「这是补的、不是真数据」 —— padding 的来历，下一节正好展开 PHC 为什么把 `=` 删了。\n\n所以 PHC 串里 16 字节 salt → 22 字符；32 字节 hash → 43 字符。反推也成立：看见 32 hex 是 MD5 / UUID（16 字节），64 hex 是 SHA256（32 字节）。\n\n## PHC 的 Base64 不带 padding\n\n标准 Base64 末尾会用 `=` 补齐到 4 的倍数：\n\n```text\n16 字节 → 21.33 字符 → 22 字符 + `==` = 24 字符\n```\n\nPHC 格式**故意去掉 `=`**：\n\n|              | 标准 Base64                | PHC Base64               |\n| ------------ | -------------------------- | ------------------------ |\n| 16 字节 salt | `UlhrVTlVbVI2S08wa2szSQ==` | `UlhrVTlVbVI2S08wa2szSQ` |\n\n理由：`=` 在 URL 里要转义、在 shell 里是赋值符。padding 是冗余信息（长度本来就能反推），删了不丢数据。\n\nPHP `base64_decode()` 两种都兼容。但 **Python 严格**：\n\n```python\nimport base64\nbase64.b64decode('UlhrVTlVbVI2S08wa2szSQ')     # binascii.Error\nbase64.b64decode('UlhrVTlVbVI2S08wa2szSQ==')   # OK\n\ns = 'UlhrVTlVbVI2S08wa2szSQ'\nbase64.b64decode(s + '=' * (-len(s) % 4))      # 手动补齐才能解\n```\n\n## bcrypt 的 \"Base64\" 不是标准 Base64\n\nbcrypt hash 里也有看似 Base64 的字段，但**字母表不同**：\n\n```text\n标准 Base64:  A B C D E F G H I J K ... + /\nbcrypt:       . / A B C D E F G H I ... 8 9\n              ↑ 起头就不一样\n```\n\nbcrypt 沿用 OpenBSD `crypt_blowfish` 的历史字母表，索引 0 是 `.` 不是 `A`。\n\n直接 `base64_decode($bcrypt_part)` 得到**完全错误的字节** —— silent failure，没有报错。要解 bcrypt 内部的 salt / hash 必须用专门的字母表映射，或者干脆别解（业务代码 `password_verify()` 就够了）。\n\n## 主流 hash 格式的 salt 编码对比\n\n放一起看，差异只在算法和字母表 —— **salt 原始长度（16 字节 / 128 bit）和编码方式（base64 无 padding）三家全一致**，这是密码学共识的\"足够防 rainbow table\"门槛：\n\n| Hash 格式                  | 原始 salt | 编码字母表             | 编码后字符数 |\n| -------------------------- | --------- | ---------------------- | ------------ |\n| **argon2id**（PHC）        | 16 字节   | 标准 `A-Z a-z 0-9 + /` | 22           |\n| **bcrypt**                 | 16 字节   | 私有 `./ A-Z a-z 0-9`  | 22           |\n| **scrypt / PBKDF2**（PHC） | 16 字节   | 标准                   | 22           |\n\n→ 这也解释了为什么把 bcrypt hash 喂给 `base64_decode()` silent failure：算法是 base64，但字母表对不上，输出字节全是垃圾。\n\n[RFC 9106 (Argon2)](https://www.rfc-editor.org/rfc/rfc9106) / [PHC string format spec](https://github.com/P-H-C/phc-string-format/blob/master/phc-sf-spec.md) / [Password Hashing Competition 主页](https://www.password-hashing.net/)。"},{"title":"pgrep / pkill 替代 ps | grep","slug":"posts/260521-pgrep-vs-ps-grep","date":"2026-05-21","tags":["shell"],"body":"按进程名 / 命令行查 PID，默认手势是 `pgrep`，不是 `ps | grep`。两个老问题 `pgrep` 一次性修了。\n\n## 1. 自匹配陷阱\n\n```bash\nps aux | grep node\n# zhaoyifan  9012  ...  node server.js\n# zhaoyifan 61234  ...  grep node                   ← grep 自己也被列出来\n```\n\n`grep node` 这条命令行本身就含 `node`，会**匹配到自己**。古早绕过手段是 `grep '[n]ode'`——用字符类骗 grep 不匹配自己（字符串 `[n]ode` 在正则里展开成 `node`，但字面上不再包含 `node` 子串，所以 grep 自己那行不被匹配）。\n\n更隐蔽的问题是**假阳性**：`grep node` 不光匹配命令叫 `node` 的，**还匹配整行任何字段含 `node` 的**——USER 叫 `node` 的、路径含 `node-exporter` 的、TTY 含 `node` 的统统中招。\n\n`pgrep` 设计上**按字段查**（默认匹配 process name，`-f` 匹配完整命令行），不是 grep 整行——既排除自己的 PID，也不会被 USER / 路径里的同字符串拐进去：\n\n```bash\npgrep -lf node\n# 9012 node server.js\npgrep -x node            # 精确等于（process name 完全匹配，不含参数）\n```\n\n## 2. 脚本里的退出码语义\n\n```bash\n# pgrep —— 退出码即答案\nif pgrep -f caffeinate > /dev/null; then ...\n\n# ps | grep —— ps 永远 0，得靠 grep -q + 排除自己\nif ps aux | grep -v grep | grep -q caffeinate; then ...\n```\n\n`ps` 的工作是「打印出来」不是「找有没有」，所以没匹配也算成功——退出码语义全靠后面的 `grep` 承担。\n\n## 配套 pkill —— 一步杀\n\n```bash\npkill -f caffeinate                  # 等价 kill $(pgrep -f caffeinate)\npkill -9 -f long-running-task        # SIGKILL 强杀\npkill -HUP nginx                     # 发 SIGHUP 让 nginx reload 配置（不止于杀）\n```\n\n`pgrep` 和 `pkill` 共用同一套 flag 模型，会一个自动会另一个。\n\n## 常用 flag\n\n| flag        | 作用                                                                            |\n| ----------- | ------------------------------------------------------------------------------- |\n| `-l`        | 输出 PID + 进程名（不加只有 PID）                                               |\n| `-f`        | 匹配**完整命令行**而不止进程名——`caffeinate -i -t 300` 这种带参数的必须用 `-f`  |\n| `-x`        | 精确匹配 process name（默认是子串匹配，没 `-x` 时 `pgrep ode` 也会命中 `node`） |\n| `-u <user>` | 限定某用户（多用户机才有意义；单用户 Mac 上等同不加）                           |\n| `-P <ppid>` | 找某 PPID 的所有**子进程**（debug 进程树用）                                    |\n| `-n` / `-o` | newest / oldest 各取一个                                                        |\n| `-d,`       | 用逗号分隔多个 PID（喂给别的工具，见下）                                        |\n| `-i`        | 忽略大小写                                                                      |\n\n## 跟 ps 组合而不是 ps | grep\n\n想看进程的 CPU / 内存 / start 时间，**让 pgrep 出 PID、ps 取详情**——把第 1 节那两个 grep bug 一起绕过：\n\n```bash\nps -p \"$(pgrep -d, -f caffeinate)\" -o pid,ppid,user,%cpu,%mem,stat,start,command\n```\n\n`pgrep -d,` 输出逗号分隔的 PID 列表，`ps -p` 直接吃。`ps` 本身的列**不**写死——`ps -axo pid,user,%cpu,...` 也能自由挑列；pgrep + ps -p 的核心优势是省掉 `| grep` 这一步，而不是控列。\n\n## 平台差异\n\n| 工具              | macOS | Linux | BSD |\n| ----------------- | :---: | :---: | :-: |\n| `pgrep` / `pkill` |  ✅   |  ✅   | ✅  |\n| `pidof`           |  ❌   |  ✅   | ❌  |\n| `pwait`           |  ❌   |  ✅   | ✅  |\n\n`pidof` 是 Linux 独有的「按名字查 PID」简化版，macOS 上没有——用 `pgrep` 全平台通吃。\n\n`man pgrep`、`man pkill` 看完整 flag。"},{"title":"shellcheck —— 抓 set -e 漏过的坑","slug":"posts/260521-shellcheck","date":"2026-05-21","tags":["shell"],"body":"`set -euo pipefail` 是 runtime 早退；[shellcheck](https://www.shellcheck.net/) 是 parse 时静态分析——抓 `set -e` 漏过的写法层 bug。部署 / 迁移 / CI 脚本两个都要上。\n\n```bash\nbrew install shellcheck     # 或：sudo apt install shellcheck\nshellcheck deploy.sh\n```\n\n## set -e 漏过的 5 条\n\n- **SC2155** `local x=$(cmd)` —— `local` / `declare` / `export` 自身返回 0，里头 `$(cmd)` 失败被吞，`set -e` 也救不了。修：先 `local x` 再 `x=$(cmd)`\n- **SC2086** unquoted `$VAR` —— `rm $FILE` 在 `FILE=\"a b\"` 时删两个文件，word splitting / pathname expansion 会拆开。默认 `\"$VAR\"`\n- **SC2046** unquoted `$(cmd)` —— 同上但是命令替换：`cd $(pwd)` 路径含空格时炸\n- **SC2068** `$@` 没加引号 —— 含空格的参数被再次拆开（`foo \"a b\" c` 传进去变 `a b c` 三个）。修：`\"$@\"`\n- **SC2059** `printf \"$msg\"` —— `$msg` 含 `%` 时被当 format directive，输出错乱或 format string 注入。修：`printf \"%s\" \"$msg\"`\n\n## 关掉警告\n\n```bash\n# 单行\n# shellcheck disable=SC2155\nlocal x=$(some_legit_command)\n\n# 整文件（紧跟 shebang）\n#!/usr/bin/env bash\n# shellcheck disable=SC2086,SC2046\n\n# ~/.shellcheckrc 或项目根 .shellcheckrc\ndisable=SC1090-SC1100\n```\n\n`disable=all`（v0.8+）关全部——通常意味着该重写。\n\n## CI\n\nGitHub Actions 一行：`shellcheck **/*.sh`（ubuntu runner 自带）。pre-commit 用 [`koalaman/shellcheck-precommit`](https://github.com/koalaman/shellcheck-precommit)。"},{"title":"dogfooding","slug":"posts/260520-dogfooding","date":"2026-05-20","tags":["english","jargon"],"body":"软件工程黑话之一。直译\"吃自己的狗粮\"，指：\n\n> **公司内部员工把自己开发的产品当日常工具用，靠真实使用暴露 QA / metrics 抓不到的体验问题。**\n\n更体面的同义说法是 _drinking your own champagne_。\n\n1988 年微软内部一封题为 \"Eating our own Dog Food\" 的邮件（经理 Paul Maritz 发出）让这个说法在科技圈固定下来，沿用至今。\n\n## 为什么 QA / 单元测试替代不了\n\nQA 跟着 checklist 点一遍验**功能存在**；dogfooding 验**长期真实使用下的体感** —— 慢、绕路、别扭、不直觉。这些既不会触发断言，也写不进测试用例。\n\n| 行为                                   | 算 dogfooding 吗                    |\n| -------------------------------------- | ----------------------------------- |\n| 团队每天用自己写的内部 CRM             | ✅                                  |\n| 自己做金融 SaaS 但从不开账户跑投顾流程 | ❌                                  |\n| QA 跟着用例点一遍                      | ❌（缺「真实工作场景 + 长期使用」） |\n\n## 失败模式：internal build ≠ public build\n\ndogfooding 失效的经典姿势是：**自己用的版本跟客户拿到的不是同一个**。\n\nAnthropic 在 2026-04-23 关于 Claude Code 质量退化的 [postmortem](https://www.anthropic.com/engineering/april-23-postmortem) 里坦白：员工用的是带内部 flag 的 test build，客户用的是 public build。其中一个缓存清理 bug 在 internal build 里被另一个并行实验掩盖了，员工 dogfooding 完全没暴露问题，客户端持续退化超过两周才定位。\n\n改进措施第一条：**让内部员工用 exact public build**。"},{"title":"MySQL utf8mb4_0900_ai_ci 拼读","slug":"posts/260520-utf8mb4-0900-ai-ci","date":"2026-05-20","tags":["mysql"],"body":"后缀是**正交标志**，按 `<charset>_<Unicode 版本>_<accent>_<case>` 拆读：\n\n- `utf8mb4`：字符集\n- `0900`：Unicode 9.0 UCA（Unicode Collation Algorithm）\n- `ai`：_accent insensitive_，重音不敏感\n- `ci`：_case insensitive_，大小写不敏感\n\n换后两位就拼出整个网格：`_as_cs`（严格区分）、`_as_ci`（保重音忽略大小写）、`_bin`（纯二进制）。\n\n非显然之处在它**真正按 UCA 处理 Unicode 等价**，老的 `utf8mb4_general_ci` 全部按字节比，结果不可预测：\n\n<!-- autocorrect-disable -->\n\n```sql\nSELECT 'café' = 'cafe';   -- 1（_ai_ci 忽略重音；general_ci 为 0）\nSELECT 'ß' = 'ss';         -- 1（UCA 定义为等价；general_ci 为 0）\nSELECT 'Ａ' = 'A';         -- 1（全角 = 半角；general_ci 为 0）\nSELECT '😀' < '😁';        -- 按 Unicode 9.0 顺序可预测；general_ci 不可预测\n```\n\n<!-- autocorrect-enable -->\n\n8.0 把默认从 `utf8mb4_general_ci` 换到这里就是为了这条「正确性」。Unicode 出新版时会跟进新 collation（如未来 `utf8mb4_1400_ai_ci`），**版本号 pin 在 collation 名里**，老 collation 永久保留，不会改语义破坏老库。\n\n参考 [Unicode Character Sets · MySQL 8 docs](https://dev.mysql.com/doc/refman/8.0/en/charset-unicode-sets.html)。"},{"title":"LLM 应用编排三层：Workflow / Agent / Function Calling","slug":"posts/260519-llm-orchestration-layers","date":"2026-05-19","tags":["ai","llm","langgraph","python","architecture"],"body":"讨论 LLM 应用编排，**workflow、agent、function calling** 是三个绕不过去的基础词；外加一个编排框架（LangGraph / CrewAI / Pydantic AI / OpenAI Agents SDK 任选其一）就构成完整讨论。它们**不是平行关系** —— 先分清各自位置，再谈选型，问题会简单很多。\n\n## 三层位置图\n\n```text\n[ 应用形态 ]  Workflow（图固定） /  Agent（图运行时才长出来）\n                              ↓ 用\n[ 编排框架 ]  LangGraph：节点 + 边 + 共享 state\n                              ↓ 调\n[ LLM 协议 ]  Function Calling：强约束 JSON schema\n```\n\n- **Function Calling**：LLM API 的协议能力，给 LLM 一份 JSON Schema，逼它按 schema 输出。**最底层、最稳定**，OpenAI / Anthropic / Gemini 都有。\n- **LangGraph**：本文用的编排框架（同族还有 CrewAI / Pydantic AI / OpenAI Agents SDK 等），把流程画成「节点 + 边 + 共享 state」。**工具层**，本身不发明能力，只是把节点间的传递标准化。下面的位置图和分水岭判断对其他框架同样适用。\n- **Workflow / Agent**：两种应用形态，区别只有一个 —— **控制流在你的代码手里，还是在 LLM 手里**。抽象层的差异见 [Workflow vs Agent 的本质差异](./260518-agent-vs-workflow)。\n\n关键认知：Workflow 和 Agent **不是 LangGraph 提供的「模式开关」**，是你用同一套 LangGraph + Function Calling **拼出来的两种不同形态**。LangGraph 两种都能做，分水岭在你怎么写边。\n\n## Function Calling 是两种形态共用的底层\n\n同一个 function calling API，两种用法决定你做出的是 workflow 还是 agent：\n\n| 维度                 | Workflow 用法                        | Agent 用法                      |\n| -------------------- | ------------------------------------ | ------------------------------- |\n| 给 LLM 几个 function | 1 个（= 输出 schema）                | N 个（每个工具一个）            |\n| `tool_choice`        | `required`，必须调                   | `auto`，LLM 自己挑              |\n| function 的角色      | 数据合同                             | 可调用工具                      |\n| 下一步谁定           | Python 看返回字段                    | LLM 看 observation 决定         |\n| LangChain 写法       | `llm.with_structured_output(Schema)` | `llm.bind_tools([t1, t2, ...])` |\n\n底层 HTTP 请求几乎一样，差别只在**给几个 tool**、**`tool_choice` 设成什么**。\n\n> 一句话：**function calling 是协议层；workflow / agent 是你怎么用这个协议**。\n\n## demo：分水岭具体在代码哪一行\n\n配套两个文件，**同一段业务逻辑（理财顾问会议笔记 → 抽字段 → 合规检查 → 分支生成文档）、两种控制流形态**，对照看一目了然：\n\n- [`financial_workflow.py`](./260519-llm-orchestration-layers/financial_workflow.py) —— workflow 版，每个关键点标 `[WORKFLOW vs AGENT]` 告诉你这一行换成 agent 会变什么样\n- [`financial_agent.py`](./260519-llm-orchestration-layers/financial_agent.py) —— 同业务的 agent 版，每个关键点标 `[WORKFLOW → AGENT]` 给出实际改造结果\n\nworkflow 文件里三段注释对应三个设计决策，正好覆盖上面三层：\n\n1. **Function Calling 层** —— `with_structured_output(Pydantic)` 锁死 LLM 返回 schema，下游节点拿到的是类型化对象，不是裸文本要 `json.loads` 加 `try/except`\n2. **LangGraph 层** —— `state` 是节点间的「信封」，节点 `return dict` 由 LangGraph 自动 merge，加节点 / 改顺序 / 插 human-in-the-loop 都不动函数签名\n3. **应用形态层（分水岭）** —— `add_conditional_edges` 的路由参数：是你的 Python 函数 → workflow；是 `tools_condition`（读 LLM last message 判定）→ agent\n\n对照两个文件最直接的三组改造：\n\n| Workflow 里                     | Agent 里换成                             |\n| ------------------------------- | ---------------------------------------- |\n| `route_by_risk` Python 路由函数 | `tools_condition`（读 LLM `tool_calls`） |\n| `TypedDict` 业务字段 state      | `MessagesState`（只有 `messages` 列表）  |\n| `(state) → dict` 节点签名       | `@tool` 装饰的业务函数                   |\n\n那一瞬间失去的代码控制力 = agent 多出来的不确定性的具体形态，也是你为什么需要 trace / eval / guardrail 那一整套基础设施的根因。\n\n## 跑起来\n\n```bash\npython -m venv .venv && source .venv/bin/activate\npip install -r requirements.txt\necho \"OPENAI_API_KEY=sk-xxx\" > .env\n\npython financial_workflow.py   # workflow 版：固定流程，编译期就能 draw_ascii\npython financial_agent.py      # agent 版：LLM 决定每一步，stream 打印实际 loop\n```\n\n样本对白故意写满合规雷点（`绝对能保证 8% 复利` / `从来没跌过` / 未审批私募），所以默认走 P1 分支。把这些词删掉重跑，能看到 safe 分支怎么走。两个文件用同一段样本，方便对照输出。\n\n## 相关词地图\n\n上面三层每层都有兄弟词，列在这里方便选型时快速定位 —— 每个词的展开看各自的笔记。\n\n### 应用形态层（Workflow / Agent 同族）\n\n- Anthropic [《Building Effective Agents》](https://www.anthropic.com/research/building-effective-agents) 给的 workflow 五模式：**Prompt chaining / Routing / Parallelization / Orchestrator-workers / Evaluator-optimizer**\n- Agent 经典模式：**ReAct / Reflexion / Plan-and-Execute / Multi-agent / Swarm**\n- 语义重叠但定位不同：**Assistant / Copilot / Chatbot / Autonomous agent**\n\n### 编排框架层（LangGraph 同族）\n\n- **Python**：LangChain / LangGraph / LlamaIndex / CrewAI / AutoGen / Pydantic AI / Haystack\n- **TypeScript**：Vercel AI SDK（底层 model 交互 + streaming）/ Mastra（建在 Vercel AI SDK 上加 workflows / RAG / memory，两者是分层不是并列）\n- **厂商原生**：OpenAI Agents SDK / Anthropic Claude Agent SDK / Google ADK / Microsoft Semantic Kernel\n\n### LLM 协议层（Function Calling 同族）\n\n- 同义 / 近义：**Tool use**（Anthropic 叫法）/ **Tool calling**\n- 强约束输出：**Structured Outputs / JSON mode / Guided decoding**\n- 新协议：**MCP**（Model Context Protocol，Anthropic 推的跨厂商 tool/resource 标准协议）\n- 相邻能力：**Computer Use / Prompt Caching**\n\n### 常被一起提、其实在别层的词\n\n| 词                               | 实际位置                                              | 关系                                          |\n| -------------------------------- | ----------------------------------------------------- | --------------------------------------------- |\n| **RAG / Retrieval**              | 另一种应用形态                                        | 和 workflow/agent 不互斥，可做节点也可做 tool |\n| **Memory**                       | 跨会话状态                                            | 和 LangGraph 的 state（会话内）不是一回事     |\n| **Tracing / Eval / Guardrails**  | 基础设施层（LangSmith / Langfuse / Arize / Helicone） | Agent 多起来后这层比框架更重要                |\n| **CoT / ToT / Self-consistency** | Prompt engineering 技巧                               | 和编排同层但更内层                            |\n| **Embeddings / Vector DB**       | 检索基础设施                                          | 服务 RAG，不参与编排                          |"},{"title":"LLM CLI proxy：rtk 是命令字典而非算法","slug":"posts/260519-rtk-per-command-parser","date":"2026-05-19","tags":["ai","llm","context-engineering","cli"],"body":"[rtk](https://github.com/rtk-ai/rtk) 是夹在 AI 编码助手和 shell 之间的 Rust CLI proxy，宣称帮 LLM 省 60-90% 的 token。它做的是 _context compression_ —— LLM 想跑 `git status`，rtk 把噪音过滤后再喂回去。看着像「魔法压缩算法」，拆开后发现根本不是。\n\n## 核心：一个 CLI 工具一份手写 parser\n\nrtk 没有通用压缩引擎。`src/cmds/` 下**按工具切文件**：\n\n- `git/git.rs` —— `git status` / `git diff` / `git push` 等子命令\n- `python/pytest_cmd.rs`、`ruff_cmd.rs`、`mypy_cmd.rs`\n- `rust/cargo_cmd.rs`\n- `cloud/container.rs` —— docker\n- `js/npm_cmd.rs`、`tsc_cmd.rs`、`vitest_cmd.rs`、`playwright_cmd.rs`\n\n约 45 个 parser 文件覆盖 100+ 子命令。每个文件用 match 分发子命令，里面写死「哪些字段是噪音、压缩后长什么样」—— 手写领域知识，不是算法推导。\n\n官网讲的 _filtering / grouping / truncation / dedup_ 也不是引擎，只是 4 个共享字符串处理函数（`dedup_lines()` / `truncate_middle()` 之类）。parser 各自挑用哪几个 —— 同一函数在 `git diff` 里删 progress 行，在 docker logs 里折叠重复行。\n\n新加一种工具支持 = 提一个 PR 加一个 `<tool>_cmd.rs`。changelog 看得清清楚楚：v0.38.0 加 GitLab `glab`、v0.40.0 加 Gradle，一条一条加。\n\n## 关键技巧：拿机器格式做解析\n\nrtk 的稳定性靠一个简单决定 —— **不解析给人看的输出**。常见命令几乎都有 `--porcelain` / `--json` / `--message-format` 之类的脚本契约，rtk 就走这条。\n\n机器契约是工具维护者给脚本用的稳定 API，UI 文案改了不影响。逆向人类输出（彩色、对齐、本地化）则一升级就崩。rtk 选前者 —— 这是它能跟着 100+ 工具活下去的根因，比任何「压缩算法」都重要。\n\n## 工作流（以 `rtk git status` 为例）\n\n```text\nLLM 决定跑 git status\n  ↓\nClaude Code PreToolUse hook 拦截 Bash 调用\n  ↓ 改写成 `rtk git status`\nrtk 进程：\n  1. fork 真命令但走机器格式：git status --porcelain=v2 --branch\n  2. parser 切字段：branch / ahead-behind / modified / untracked\n  3. 紧凑模板渲染成单行\n  ↓\n返给 LLM，LLM 完全不知道有中间层\n```\n\n## 为什么不选其他路\n\n| 候选方案              | 为什么 rtk 没用                                         |\n| --------------------- | ------------------------------------------------------- |\n| gzip 通用压缩         | LLM 不解压，喂进去还是压缩字节                          |\n| 让 LLM 自己 summarize | 慢、贵、丢信息，summarize 本身吃 token                  |\n| 盲截前 N 行           | 没语义，可能切掉关键 error 行；切到的还是不稳的人类格式 |\n| per-command parser    | <10ms、确定性、保关键信号 —— 代价是每个工具都得单独写   |\n\n## 推论\n\n1. **护城河是命令字典而非算法**。任何人能写 `pytest_cmd.rs`，但要覆盖 90% 日常命令、跟着每个工具升级（git 改 porcelain、pytest 升级 reporter）才是真功夫。\n2. **工具支持决定收益**。`rtk hugo`、`rtk astro` 不存在就是裸过。\n3. **永远滞后于命令本身**。git 改了 porcelain 格式得等 rtk 跟上。\n4. **PreToolUse hook 才是 UX 关键**。靠 prompt 教 LLM「记得用 rtk」不可靠；hook 把改写做在协议层，LLM 不感知。"},{"title":"state vs status","slug":"posts/260519-state-vs-status","date":"2026-05-19","tags":["english","naming"],"body":"两个词中文都译\"状态\"，工程命名混用会被 review 反复打回。先记一句口诀：\n\n> **state is what you ARE; status is where you ARE in a process.**\n\n判准两步：\n\n1. 取值是有限几个枚举标签吗？→ `status`\n2. 是描述对象当下完整情况的一坨数据吗？→ `state`\n\n`status` 通常是 `state` 里的**一个字段**，不是同级概念。这一层嵌套关系是判断命名时最重要的视角。\n\n## 最干净的演示：Kubernetes Pod\n\n```yaml\nspec:      # desired state（期望）\n  containers: [...]\n\nstatus:    # 实际情况\n  phase: Running           # ← 流程节点标签\n  conditions: [...]\n  containerStatuses: [...]\n```\n\n`spec` + `status` 一起构成 Pod 的完整 **state**。`status.phase` 是 state 内部的一个枚举字段。命名 / 查 API 文档时，先判断你在描述哪一层。\n\n## 反例：GitHub PR 用 `state` 不用 `status`\n\n```text\nGET /repos/:owner/:repo/pulls/:number\n→ {\n    \"state\": \"open\" | \"closed\",\n    \"merged\": true | false,\n    ...\n  }\n```\n\n`open` / `closed` 明明是枚举标签，按规则该叫 `status`，但 GitHub API 字段就叫 `state`，`merged` 另起一个 boolean。\n\n为什么？GitHub 把 PR 当成\"一个对象当前是什么\"在表达，不是把它当流程节点。**语义优先于规则，不要死记**。\n\n## 常见场景速查\n\n| 场景       | 用 `status`                            | 用 `state`                            |\n| ---------- | -------------------------------------- | ------------------------------------- |\n| HTTP       | `200 OK`、`response.status_code`       | TCP 连接：`ESTABLISHED` / `TIME_WAIT` |\n| 数据库字段 | `orders.status = 'paid'`               | —                                     |\n| 前端组件   | antd `<Steps status=\"finish\" />`       | React `useState({ count, loading })`  |\n| CI/CD      | GitHub Actions job `status: completed` | GitHub PR `state: open`（反例）       |\n| 即时通讯   | 用户可设 `status: 🍵 Tea break`        | WebSocket `readyState: OPEN`          |\n| 游戏       | —                                      | 存档（整个世界快照）                  |\n\n## 易混 trap：OAuth 的 `state` 参数\n\n```text\n/authorize?response_type=code&state=abc123xyz\n```\n\n这里的 `state` **不是**本文讨论的\"对象状态\"，是 OAuth 2.0 防 CSRF 的随机 nonce（RFC 6749 §4.1.1），授权服务器回调时原样带回，客户端校验是否匹配。\n\n遇到这个词别按\"完整数据快照\"理解，它跟 state machine 一点关系没有。\n\n## 一句话决策\n\n- 数据库字段、订单 / 任务 / 用户的流程节点 → `status`\n- React 组件内部数据、连接 / 进程的运行时快照 → `state`\n- 卡在中间 → 看现有 codebase 怎么命名，跟它走，比\"正确\"重要"},{"title":"git worktree —— 一仓多目录","slug":"posts/260519-git-worktree","date":"2026-05-19","tags":["git"],"body":"正在 feature 分支写代码，文件改了一半 —— 线上 bug 要紧急修。常规做法是 stash / WIP commit / 切分支 / 回来再 pop，这套动作越熟越说明 git 缺机制。`git worktree` 就是这个机制：**一个仓库同时挂多个工作目录**，每个目录一个 HEAD，共享同一份 `.git`。比 stash 干净，比 `git clone` 轻量（不复制 objects、不重新 fetch）。\n\n## 工作树 ≠ 分支\n\n- **分支**：一个指针（`.git/refs/heads/<name>`），仓库级元数据，几个字节\n- **工作树**：一个目录 + 一个 HEAD。HEAD 可以 _attached_ 到分支，也可以 _detached_ 直接指向 commit\n\n多棵工作树**共享所有分支、objects、hooks**。每棵 _linked worktree_ 的 HEAD 独立存在 `.git/worktrees/<name>/HEAD`，主工作树的 HEAD 仍在 `.git/HEAD`。**同一分支同一时刻只能被一棵工作树 checkout** —— 否则两边 HEAD 互相覆盖、reflog 串台。这是设计上的保护。\n\n推论：\n\n- 创建 worktree ≠ 创建分支：`--detach` 完全不动分支；复用已有分支也不创建\n- 删除 worktree ≠ 删除分支：`worktree remove` 只清目录和元数据，分支还在\n- 删分支前先 `worktree list`：被占用的分支删不掉，会报 `error: Cannot delete branch 'X' checked out at '<path>'`\n\n## 三种 add 姿势\n\n```bash\ngit worktree add -b feat/login ../login main       # 新建分支，基于 main\ngit worktree add ../review feat/login              # 复用已有分支\ngit worktree add --detach ../inspect origin/main   # 不开分支，detach 到某个 ref/commit\n```\n\n末位 `<commit-ish>` 接 branch / tag / SHA / `HEAD~3` 都行。其他子命令（`list` / `remove` / `move` / `lock` / `prune` / `repair`）按需查 `git help worktree`，日常 95% 是这三条 add + `remove`。\n\n## 典型工作流\n\n**feature 写一半，hotfix 插队**：\n\n```bash\ngit worktree add -b hotfix/login ../hotfix main\ncd ../hotfix\n# 改代码、commit、push、PR\ncd -\ngit worktree remove ../hotfix\ngit branch -d hotfix/login        # remove 不删分支，按需手动\n```\n\n**Review 别人 PR**：\n\n```bash\ngit fetch origin pull/123/head:pr-123\ngit worktree add --detach ../pr-123 pr-123\n# composer install / 跑测试 / 手测\ngit worktree remove ../pr-123 && git branch -D pr-123\n```\n\n## AI agent 让 worktree 从奢侈变刚需\n\ngit 2.5（2015 年）就有 worktree，十年小众。AI agent 让\"同时跑 N 个任务\"从奢侈变日常 —— branch 是 git 给「**异步协作的人**」的礼物，worktree 是 git 给「**并行执行的 agent**」的礼物。\n\n让 agent 在你正在工作的目录里改代码，风险是文件 race（A 改 `Home.php` 第 100 行同时 B 也改）、watcher 链式反应、dev server 端口抢占、test DB 互 truncate。worktree 把每棵任务关进独立目录，git 层面互不踩脚。但 **worktree 只隔离 git 状态**，周边资源（端口 / 测试 DB / `vendor/` / `node_modules/`）得自己按 worktree 分。\n\nClaude Code 内置 `EnterWorktree` 和 `Agent(isolation: \"worktree\")`，工作树落在 `.claude/worktrees/<name>/`，基于 `origin/<default-branch>`。[Conductor](https://conductor.build) / Cursor / Aider 底层同套机制。三条纪律：\n\n1. **一棵树一个任务** —— 否则 review 时分不清谁改了什么\n2. **完事立刻 `remove`** —— 不清理 `.claude/worktrees/` 堆几十个，磁盘和心智都崩\n3. **依赖各装各的** —— `vendor/` `node_modules/` 是文件不是 ref，不会跟着 worktree 同步\n\n## 高频陷阱\n\n| 现象                                                                              | 原因 / 解                                                                                            |\n| --------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |\n| `git branch -d feat/x` 报 `Cannot delete branch 'feat/x' checked out at '<path>'` | 分支被某棵 worktree 占着。`git worktree list` 定位 → `git worktree remove ../x` 释放                 |\n| 手动 `rm -rf ../x` 后 `git worktree list` 仍列 `../x`                             | 元数据残留在 `.git/worktrees/x/`。`git worktree prune` 清死链                                        |\n| 想在两棵树同时 checkout `main`                                                    | 设计禁止。第二棵用 `--detach main`，或 `-b <new> main` 基于 main 开新分支                            |\n| pre-commit / husky / lefthook 在某棵树改了，另一棵也变                            | `.git/hooks/` 是**共享**的，所有 worktree 共用一份 hook 配置。装一次即可，别多树重复 `husky install` |\n\n`git help worktree` 看完整 manpage，重点段是 _DESCRIPTION_ 里 `.git/worktrees/` 布局描述。"},{"title":"markdownlint × prettier：format 与 lint 分工","slug":"posts/260519-markdownlint-vs-prettier","date":"2026-05-19","tags":["markdown"],"body":"markdown 在工具链上有两条独立线：**prettier** 管\"长什么样\"（format），**markdownlint** 管\"对不对\"（lint）。共存方案不是把两者并列开就完事 —— 默认配是稳定的，但有一处 CJK 用户必踩的雷。\n\n## 职责分工\n\n| 维度     | prettier                                 | markdownlint                                                                  |\n| -------- | ---------------------------------------- | ----------------------------------------------------------------------------- |\n| 干什么   | format：表格对齐、空行、缩进、引号统一   | lint：规则告警（重复标题、缺 lang、emphasis 当标题、列表风格）                |\n| 配置     | `.prettierrc.json` + `.prettierignore`   | `.markdownlint-cli2.jsonc`（自带 `ignores` / `gitignore: true`）              |\n| globs    | **不能写在配置里**，命令行 / Makefile 传 | 配置里 `globs` 字段直接写                                                     |\n| auto-fix | 全部规则（修一次到位）                   | 行内类（MD009/012/047 等）能修；**表格类（MD055/056/058/060）整组不 fixable** |\n| 互相感知 | 零                                       | 零                                                                            |\n\n最后两行是关键 —— 决定了下面所有\"为什么这么配\"的判断。\n\n## 默认值高度吻合，平时不打架\n\n`~/.config/markdownlint/base.jsonc` 的关键规则（MD003 atx、MD004 dash、MD007 indent 2、MD035 `---`、MD046 fenced、MD048 backtick、MD009/012/047 清理尾空格 / 多空行 / 末换行）和 prettier 默认逐项对齐。保存 → prettier format → markdownlint 跑完只剩 MD036 / MD024 / MD040 这类语义告警，没有 format 层冲突。\n\n## 真踩雷：MD060 aligned + CJK 表格\n\nprettier 按 east-asian-width 算 CJK 列宽（视觉对齐），MD060 按字符数算（字节对齐），两者在 CJK 表格上永远收敛不了 —— 把 `~/.config/markdownlint/base.jsonc` 的 `\"MD060\"` 设为 `{ \"style\": \"consistent\" }` 或 `false` 让给 prettier。\n\n## 局部翻案：ignore 注释\n\n- **prettier**：HTML 注释里写指令 `<!-- ... -->`。单行 `prettier-ignore` 跳下一行；整段成对 `prettier-ignore-start` / `prettier-ignore-end`（保留 raw 表格 / 空格 / 折行）。\n- **markdownlint**：同上，按规则号开关。整段成对：`markdownlint-disable MD013 MD056` 起、`markdownlint-enable` 收；单行：`markdownlint-disable-line` / `disable-next-line`。不带规则号的 `disable` = 全部关，慎用。\n\n## Makefile 把两者串成一个入口\n\nprettier 不接受配置里的 globs，只能在命令行和 markdownlint 的 `globs` 字段手动对齐；顺序固定 **prettier 先、markdownlint 后** —— prettier 修完 format，markdownlint 剩下的就纯是语义告警，信噪比最高。\n\n```makefile\n# Keep MD_GLOBS in sync with .markdownlint-cli2.jsonc#globs.\nMD_GLOBS := 'content-til/**/*.md' '*.md'\n\nlint:\n  prettier --write $(MD_GLOBS)\n  markdownlint-cli2 --fix\n\nlint-check:\n  prettier --check $(MD_GLOBS)\n  markdownlint-cli2\n```\n\n## 经验法则\n\nVS Code 两个扩展共存：`esbenp.prettier-vscode` 读 `.prettierrc.json`，`DavidAnson.vscode-markdownlint` 读 `.markdownlint-cli2.jsonc`，各自互不感知，配置不冲突。规则索引见 <https://github.com/DavidAnson/markdownlint/blob/main/doc/Rules.md>。"},{"title":"Agent vs Workflow：控制流在谁手里","slug":"posts/260518-agent-vs-workflow","date":"2026-05-18","tags":["ai","llm","agent","architecture"],"body":"把 LLM 包进应用的写法分两种：**Workflow**（你的代码画好流程图，LLM 只填某些节点）和 **Agent**（LLM 自己决定下一步调哪个工具、什么时候停）。两者长得很像 —— 都接输入、都调 LLM、都返结果。本质差异只有一条：**下一步该干什么，是你的代码决定，还是 LLM 自己决定**。这条线划在哪，决定了你在写哪种东西。\n\nAnthropic 把两者合称 _agentic systems_，但坚持把 workflow 和 agent 分清楚 —— 它们的成本、延迟、可控性差着一个数量级。\n\n## 控制流在谁手里\n\n**Workflow：控制流写死在你的代码里。**\n\n```text\n用户输入\n  ↓\n你的代码：调 LLM 提取意图\n  ↓\n你的代码：switch case 分发\n  ↓\n你的代码：调对应函数\n  ↓\n返回\n```\n\nLLM 只是流程里的一个 `text-in / text-out` 节点。流程图你画，模型填格子。\n\n**Agent：控制流在 LLM 手里。**\n\n```text\n用户目标\n  ↓\nLLM 决定：调哪个工具、给什么参数\n  ↓\n工具返回结果\n  ↓\nLLM 决定：要不要继续，继续就调下一个\n  ↓\n（循环，直到 LLM 说\"完了\"）\n```\n\n你的代码只负责执行 LLM 给的指令。每一步「下一步干嘛」由模型自己判断。\n\n判准：把工作流画出来 —— **如果存在「LLM 决定要不要进下一步 / 进哪一步」的节点 = Agent；只有「LLM 帮我填空 / 分类 / 改写」的节点 = Workflow**。\n\n## 五件套：Model + Prompt + Tools + Memory + Orchestrator\n\nAgent 的最小完备架构。少一个就跑不起来：\n\n| 组件             | 干嘛                                               | 工程上最容易翻车的点                           |\n| ---------------- | -------------------------------------------------- | ---------------------------------------------- |\n| **Model**        | 决策核心；要支持 Function Calling                  | 指令遵循 / 工具选择准确率                      |\n| **Prompt**       | system prompt + tool 描述 + few-shot               | 工具描述写糊 → Agent 调错或调不出              |\n| **Tools**        | 真正改变外部世界的接口（读写文件、调 API、跑 SQL） | 工具粒度 / 错误信息可读性                      |\n| **Memory**       | 工作记忆（context）+ 短期（会话）+ 长期（跨会话）  | 塞太多炸 context，塞太少失忆                   |\n| **Orchestrator** | 主循环：把上面四个串起来跑                         | 最大轮数 / 超时 / 错误重试 / human-in-the-loop |\n\nOpenAI 的 [Practical guide](https://cdn.openai.com/business-guides-and-resources/a-practical-guide-to-building-agents.pdf) 只列三件套（Model / Tools / Instructions），把 Memory 和 Orchestrator 视作框架默认配置。从底层实现角度五件套缺一不可；从 API 使用者角度三件套是你能感知到的部分 —— 别被简化版误导以为不存在剩下两个。\n\n反直觉的踩坑权重：**大部分 Agent 做不好不是模型不够强，是 tool 描述写得稀烂 + memory 策略乱设计**。换模型贵又简单，写描述便宜又关键。\n\n## ReAct 循环：Agent 的心跳\n\n`Thought → Action → Observation` 三拍循环，是当前几乎所有 Agent 框架的内核：\n\n```text\n┌─────────────────────────────────────────┐\n│  Thought      —— 我现在该干嘛？          │\n│     ↓                                    │\n│  Action       —— 调 tool X，参数 {...}   │\n│     ↓                                    │\n│  Observation  —— 工具返回了 Y            │\n│     ↓                                    │\n│  （回到 Thought，直到决定 Finish）        │\n└─────────────────────────────────────────┘\n```\n\n每一轮 Orchestrator 把 `[目标 + 工具列表 + 历史 Thought/Action/Observation]` 整包扔给 Model，Model 输出下一个 Thought + Action 或最终答案。**模型本身无状态**，所有「记忆」都靠 Orchestrator 把历史拼回 prompt 里 —— 这也是为什么 context window 总不够用，也是为什么 Agent 的 token 成本随轮数指数增长。\n\nClaude Code 跑一句「把 User 的 email 字段改成 unique」内部大致就是：\n\n```text\nThought: 先定位 User 在哪\nAction:  Grep \"class User\"\nObservation: src/User.php:12\n\nThought: 看现在 email 怎么定义的\nAction:  Read src/User.php\nObservation: <文件内容>\n\nThought: 字段约束在 migration 不在 model，找 migration\nAction:  Grep \"users\" 在 migration 目录\nObservation: 2025_01_users_table.php\n\nThought: 写新 migration 加 unique\nAction:  Write add_unique_to_users_email.php\nObservation: ok\n\nThought: 跑测试确认\nAction:  Bash 跑测试\nObservation: 失败 —— 现有数据里有 3 行重复 email\n\nThought: 那要先去重再加约束，改方案\nAction:  Edit migration，先 DELETE 重复行再 ALTER\n...\n```\n\n每步「下一步该干嘛」全是模型根据上一步 Observation 自己决定的 —— 没人写死 `if grep_success then read`。报错也是模型自己看到 Observation 后调整策略，不是你代码 `catch` 的。**Agent 与 Workflow 的本质差异，在终端里就长这样**。\n\n## 什么时候真的需要 Agent\n\nAnthropic 和 OpenAI 在这点上罕见地一致：**能用 Workflow 跑通的就不要上 Agent**。控制流的不确定性是 Agent 所有成本和风险的来源 —— 慢、贵、不可预测。\n\n| 场景                                            | 选什么                                    |\n| ----------------------------------------------- | ----------------------------------------- |\n| 用户提一个明确问题，答完就完                    | 直接调 LLM API                            |\n| 流程固定，只是某几步要 LLM 抽信息 / 总结 / 分类 | **Workflow** + LLM 节点（LangGraph 这类） |\n| 任务能拆但步骤不确定，路径依赖中间结果          | **Agent**                                 |\n| 需要试错 / 自我纠错 / 调多个外部系统            | **Agent**                                 |\n| 单步深推理（数学 / 代码）                       | **Reasoning model 直调**，不需要 Agent    |\n\n判准：**「下一步该做什么」如果你能提前画清楚流程图，就不是 Agent 该解决的问题**。\n\n## 参考\n\n- Anthropic [Building effective agents](https://www.anthropic.com/research/building-effective-agents)\n- OpenAI [A practical guide to building agents](https://cdn.openai.com/business-guides-and-resources/a-practical-guide-to-building-agents.pdf)（PDF）\n- [anthropic-cookbook/patterns/agents](https://github.com/anthropics/anthropic-cookbook/tree/main/patterns/agents)"},{"title":"Reasoning model 别再写 CoT prompt","slug":"posts/260518-reasoning-vs-cot-prompts","date":"2026-05-18","tags":["ai","prompt-engineering","llm","reasoning-models"],"body":"老的 CoT prompt 技巧（`Let's think step by step`、few-shot CoT 示例）在 reasoning model 上不仅多余，还会**降低**性能 —— OpenAI 在 o1 的 prompting guide 里明确说了这一点。原因不在\"模型更聪明所以不需要\"，而在 CoT 和 reasoning model 根本不是一回事。\n\n## CoT 是提示技巧，reasoning 是模型能力\n\n| 维度       | CoT prompting                              | Reasoning model                         |\n| ---------- | ------------------------------------------ | --------------------------------------- |\n| 来源       | inference 阶段的 in-context learning       | training 阶段的 RL（RLVR 等）           |\n| 触发       | `Let's think step by step` / few-shot 示例 | 默认就在思考，无需触发                  |\n| 思考过程   | 普通输出 token，跟答案混在一起             | 专门的 reasoning tokens，与最终答案分离 |\n| 行为       | 线性向前推，错了一路错下去                 | backtrack、self-verify、试多条路径      |\n| 能力天花板 | 在已能做的任务上提准确率                   | 解锁了之前完全做不了的题                |\n\n一句话：CoT 是\"哄\"出来的推理，reasoning model 是\"烧\"进权重里的推理。\n\n## 老技巧为什么反而有害\n\n模型已经有自己 RL 训出来的推理风格。prompt 层再叠一套等于污染：\n\n- **`Let's think step by step`** —— 纯噪音 tokens，模型本来就在 think\n- **Few-shot CoT 示例** —— OpenAI 实测显示**降低** o1 性能，因为示例会污染模型自有推理风格。注意区分：**few-shot 本身 OK**（给格式 / 领域示例没问题），**few-shot CoT** 才有害\n- **\"请仔细分析\"、\"深入思考\"、\"一步步来\"** —— 全是冗余 tokens，思考预算被消耗在\"读你的废话\"上\n- **角色扮演**（\"你是数学专家\"）—— [Kim 2024](https://arxiv.org/html/2408.08631v2) 实测在 12 个 reasoning 数据集里 7 个**降低**准确率\n\n角色扮演翻车的机制是 persona 注入了和题目无关的 domain prior —— Kim 2024 的例子：一道数学题套了 civil engineering 故事壳，模型就把自己往土木工程师方向调，忽略了本质是道数学题。AQuA 上 13.78% 题因 persona 从对变错，Coin Flip 18%。**匹配的 persona 也救不回来**：[Zheng 2024](https://arxiv.org/html/2311.10054v3) 测 in-domain persona（律师做法律题、医生做医学题）是 \"minimal effect size\"，期望收益约等于 0，且自动选 persona 的策略几乎打不过随机 —— 不存在\"选对 persona\"这件事。经验法则：**匹配 persona 期望收益 ≈ 0，不匹配 < 0，默认别加**。\n\n## 新范式：减噪音胜过加技巧\n\nOpenAI 官方建议是 [\"Keep prompts simple and direct\"](https://platform.openai.com/docs/guides/reasoning-best-practices) 和 \"Avoid chain-of-thought prompts\"。\n\n老 prompt 风格（CoT 时代）：\n\n```text\nYou are an expert programmer. Let's think step by step.\nFirst, analyze the problem. Then, consider edge cases.\nFinally, write the code...\n```\n\nReasoning model 时代：\n\n```text\nRefactor this function to handle null inputs.\n\nConstraints:\n- Keep the existing public API\n- Must pass tests in tests/UserTest.php\n- PHP 8.2, no new dependencies\n\nReturn: the modified function only, no explanation.\n```\n\n短、直接、富约束、明确输出。把模型当聪明同事，不是当学生。\n\n## 真正重要的维度\n\nprompting 没消失，重心从\"教它怎么想\"变成\"告诉它想什么、想多深、想完干嘛\"：\n\n- **任务定义清晰度** —— 模型按字面意思深度推理，需求模糊 = 在错的方向上 deep think 几千 tokens\n- **约束 / 边界条件** —— 显式写\"必须用 PHP 8.2\"、\"不能改 schema\"，否则它会自由发挥\n- **输出格式** —— reasoning 完了的最终答案才是你要的，显式要 JSON / 表格 / 代码块\n- **Thinking budget** —— Claude / Gemini / GPT-5 都有显式参数控制思考长度，简单任务别让它想 30 秒\n- **上下文质量** —— garbage in, deeply-reasoned garbage out\n\n## 品类正在消失\n\n2025 下半年起，\"是不是 reasoning model\" 不再是模型品类，而是运行模式：\n\n- Claude Sonnet 4.6 / Opus 4.7：extended thinking 开关 + budget tokens\n- GPT-5：`reasoning_effort` 参数\n- Gemini 2.5：`thinking_config`\n- DeepSeek V3.x / R1 已合并为统一模型\n\n未来不会再有\"non-reasoning model\"这个品类，只会有 `thinking budget = 0` 的运行模式。但小快省的 Haiku 类、端侧模型、embedding 这些纯非 reasoning 阵营，在长尾场景依然不可替代。\n\n## 参考\n\n- OpenAI [Reasoning best practices](https://platform.openai.com/docs/guides/reasoning-best-practices)\n- Anthropic [Extended thinking](https://docs.anthropic.com/en/docs/build-with-claude/extended-thinking)"},{"title":"Agent 写代码时代：review 比 prompt 值钱","slug":"posts/260518-review-over-prompt","date":"2026-05-18","tags":["ai","llm","agent","prompt-engineering"],"body":"Reasoning model 让 prompt 技巧失效已是共识（CoT、persona 反而拖性能）。更大的变化在另一边 —— agent 一天写出比 2024 一整周还多的代码，看起来都很 idiomatic。**瓶颈从「模型懂不懂我」换成了「我看不看得出它错」**。\n\n## 把力气从 prompt 转到 context engineering\n\n省下 prompt 技巧的力气，花在塞进 context 的内容质量：\n\n- **任务定义** —— 模糊需求 = 在错的方向上 deep think 几千 tokens\n- **约束 / 边界** —— `必须 PHP 8.2`、`不能改 schema`、`通过 tests/UserTest.php`\n- **输出格式** —— 显式 JSON / 表格 / 仅函数体\n- **Thinking budget** —— Claude `budget_tokens` / GPT-5 `reasoning_effort` / Gemini `thinking_config`。Token 成本暴跌，但思考预算成了新成本中心；简单任务别开 high\n- **上下文质量** —— garbage in, deeply-reasoned garbage out\n\n## Review 是新护城河\n\nAgent 写 PR，你 merge。Agent 不签字，**你签字**。看不出问题就是你的问题，没有「AI 写错了」这个免责条款。\n\nAgent 最爱踩的雷（PHP / Laravel 视角）：\n\n| 类型             | 典型样子                                               |\n| ---------------- | ------------------------------------------------------ |\n| N+1              | 测试都过，本地慢 200ms，prod 上炸                      |\n| 静默 scope creep | 让它修一个 method，顺手「改进」旁边三个                |\n| 幻觉 API         | 用了 Laravel 里不存在但听起来很像的方法                |\n| 删测试           | 测试碍它的事，agent 默默改 / 删，PR diff 漏看          |\n| 过度抽象         | 一个 if 包成三层 strategy pattern                      |\n| 危险 migration   | 加 NOT NULL 列在 5000 万行表上，本地秒过 prod 锁半小时 |\n| 摘 middleware    | 改 controller 时把 auth / validation 顺手干掉          |\n| 放宽类型         | `int` 改 `int\\|null` 让测试过，业务不该 null           |\n\n这些都不是 prompt 写得不好导致，是 review 没看出来。**再好的 prompt 也防不住**。\n\n2026 的「好 review」不是逐行读完，是几个具体动作：\n\n- 读 diff 时先问「**我没让它改的它改了吗**」—— 比「它写的对吗」更重要\n- 对 agent 盲区有 muscle memory —— 它爱过度抽象、爱兜底 try/catch、爱删测试、爱写「为了向后兼容」的 if\n- **守 invariants 而不是守语法** —— 类型对 / 测试过 / lint 过都没用，问「我的业务规则还成立吗」\n- 怀疑「太像样」的代码 —— agent 最危险的输出是 idiomatic 到让你想直接 approve 的\n\n## 一句话\n\n**2024 年「会写代码」值钱，2025 年「会用 AI 写代码」值钱，2026 年「会判断 AI 写的代码对不对」值钱。**\n\n写代码的肌肉记忆贬值最快；taste、code smell 嗅觉、review 时的怀疑反射，越来越值钱。"},{"title":"GitHub URL 隐藏参数：`.diff` / `?w=1` / `compare@{时间}` 工作流","slug":"posts/260515-github-url-hidden-params","date":"2026-05-15","tags":["github"],"body":"GitHub 网页 URL 上有一组 2012 年就存在、UI 从没暴露过的参数。LLM 工作流让它们价值翻倍 —— `curl` 一个 `.diff` URL 直接拿到纯文本 diff 喂给 prompt，比让 LLM 自己抓网页快也准。下面所有链接都点得开（用 `laravel/framework` 演示）。\n\n**`.diff` / `.patch`** —— 任何 PR / commit / compare 页 URL 加扩展名，返回纯文本：\n\n```bash\ncurl -L https://github.com/laravel/framework/pull/10000.diff\ncurl -L https://github.com/laravel/framework/pull/10000.patch\n```\n\n`.diff` 体积最小，适合喂 LLM；`.patch` 多带 commit metadata（作者、邮箱、时间），适合 `git am` / cherry-pick。commit / compare URL 同样支持。\n\n**`?w=1`** —— 任何 diff URL 加上，忽略纯 whitespace 改动：\n\n<https://github.com/laravel/framework/pull/10000/files?w=1>\n\n把 indent 调整 / 换行符变化彻底从视野抹掉，只剩真改动。refactor / formatter 大重写后救命。\n\n**`?ts=4`** —— diff / blob 页改 tab 渲染宽度：\n\n<https://github.com/laravel/framework/blob/master/src/Illuminate/Foundation/Application.php?ts=4>\n\n数值任意（`?ts=2` / `?ts=8`），只改你眼里的呈现，远端文件不变。在 tab 缩进的项目（Go / Linux kernel）里效果最直观。\n\n**`#L16-L25`** —— 行号锚点 + range：\n\n<https://github.com/laravel/framework/blob/master/src/Illuminate/Foundation/Application.php#L1-L20>\n\n提 PR 评论、写文档、给同事丢 issue 时定位用，比\"Application.php 大概第 20 行\"准多了。\n\n**`compare/<base>@{<时间>}...<head>`** —— compare 页接受时间锚点，不只是 SHA / branch：\n\n<https://github.com/laravel/framework/compare/master@{1.week.ago}...master>\n\n<https://github.com/laravel/framework/compare/master@{2026-05-01}...master>\n\n语法和 `git log @{...}` 一致。release notes 起草、\"上周改了啥\"周报、incident 后回溯都用得上。"},{"title":"git filter-repo 清除历史敏感文件","slug":"posts/260514-git-filter-repo-purge-secrets","date":"2026-05-14","tags":["git"],"body":"仓库不小心把 `.env` / `database.yml` 推到远程后，**第一动作是轮换密钥**，假定文件已被抓取 —— 改写历史只能挡住\"还没看到的人\"，已 clone / fork / 爬虫的副本拿不回来。这一步先做完，再谈 `git filter-repo`：它是第二步，不是第一步。\n\n## 为什么不是 filter-branch\n\n`git filter-branch` 官方已弃用。每个 commit fork 一个 shell 子进程，大仓库跑数小时；默认保留 `refs/original/` 和 reflog，旧对象很容易\"清理后又复活\"。`git filter-repo` 是 Python 单进程流式管道，快 10–100 倍，且默认要求 fresh clone（拒绝在带遗留 ref 的工作目录上跑）。\n\n```bash\nbrew install git-filter-repo\n```\n\n## 原理：重写而非删除\n\nGit commit 的 SHA-1 由 tree + parent + author + message 哈希得出，不可变。\"删一个文件\"等价于**生成一份新历史替代旧历史**。\n\n```text\n旧仓库 ── git fast-export ──▶ 事件流 ── 过滤器 ──▶ git fast-import ──▶ 新仓库\n```\n\n`git fast-export` 把仓库历史序列化成纯文本指令流（blob、commit、tag）；filter-repo 在内存里按规则改写事件流；`git fast-import` 把它灌成新对象库。\n\n关键后果：**一个 commit 的 tree 变了 → 它的 SHA 变 → 子 commit 的 parent 指向新 SHA → 子 SHA 也变 → 一路传染到 HEAD**。所以 force push 是必须的 —— 远程的 commit 链被一条 SHA 完全不同的链替换。\n\n## `--invert-paths`：默认是白名单\n\n非显然：filter-repo 默认把 `--path` 当**保留**列表，不是删除列表。\n\n```bash\n# 默认：只保留 src/，其他全删（拆分 monorepo 用法）\ngit filter-repo --path src/\n\n# --invert-paths：删除 src/，其他保留（清理用法）\ngit filter-repo --path src/ --invert-paths\n```\n\n设计初衷之一是从 monorepo 抽子项目 —— \"列出我要的那部分\"比\"列出我不要的\"自然。清理敏感信息正好相反：99% 留 + 1% 删，**必须**加 `--invert-paths`。漏写就把整个仓库删干净只剩那个泄露文件，反应过来时已经覆盖远程了。\n\n```bash\ngit filter-repo --path .env --invert-paths\ngit filter-repo --path-glob '*.pem' --invert-paths\ngit filter-repo --path-regex '^secrets/' --invert-paths\n```\n\n## `--mirror`：不配它等于没删\n\n普通 `git clone` + `git push --force --all` 看似清理，实际**只覆盖 branches**。远程上 branch 之外的 ref 没动，仍指向旧 commit：\n\n- **Tags**：旧 tag 仍含敏感 commit\n- **Notes、其他自定义 ref**\n\n只要**远程任何一个 ref 还能可达旧 commit**，那个 blob 就不会被 GC 回收 —— 攻击者拿到 SHA 就能 `git cat-file -p <sha>` 读出来。删了等于没删。\n\n`git clone --mirror` 拉一份裸仓库（`.git` 结构、无工作区）+ 所有 ref；`git push --mirror` 把本地 refs 一比一推到远程，远程多出来的 ref 会被删 —— \"让远程完全等于本地\"。这才是真清理：\n\n```bash\ngit clone --mirror git@github.com:org/repo.git\ncd repo.git\ngit filter-repo --path config/database.yml --invert-paths\ngit push --force --mirror\n```\n\n> `--mirror` 只解决服务端的 ref 问题。**任何一个老的本地 clone 仍能通过 reflog 和 remote-tracking refs (`refs/remotes/origin/*`) `cat-file` 出旧 blob** —— 这正是 filter-repo 默认要求 fresh clone 的原因，也是清理后必须通知所有协作者删本地仓库重新 clone 的原因。\n\n## 清理后必做\n\n| 步骤                | 说明                                                                                                      |\n| ------------------- | --------------------------------------------------------------------------------------------------------- |\n| 轮换密钥            | 见开篇，第一优先                                                                                          |\n| 通知协作者          | 所有人重新 clone，旧本地分支基于已消失的 commit 链                                                        |\n| 联系 GitHub Support | PR refs (`refs/pull/<n>/head`) 由 GitHub 服务端维护，`push --mirror` 覆盖不了；fork 拷贝也要走 Support 清 |\n| 加 `.gitignore`     | 防止再次提交                                                                                              |\n| 装 pre-commit 钩子  | `gitleaks protect --staged` 自动拦截                                                                      |\n\n```bash\nbrew install gitleaks\ngitleaks detect             # 扫整个历史，确认清理生效\ngitleaks protect --staged   # pre-commit 钩子里跑\n```\n\n[git-filter-repo 文档](https://github.com/newren/git-filter-repo)"},{"title":"JS 生态分层：规范 / 引擎 / 运行时 / 类型","slug":"posts/260513-js-ecosystem-layers","date":"2026-05-13","tags":["js"],"body":"JavaScript 生态混乱的根源是**没分清三件事**：写规范的、实现引擎的、做宿主运行时的。再叠加 TypeScript 这个外挂类型层，就是常被混为一谈的\"JS\"。把四层拆开，整个服务端 JS 世界就不再奇怪。\n\n## 规范层：ECMAScript（ECMA-262）\n\n由 **TC39** 维护（Ecma International 第 39 号技术委员会），每年 6 月发新版。规范定义语法、内置对象、执行模型，**但不定义** DOM、`fetch`、`localStorage`、`fs`、`http` —— 这些归别处管：\n\n```text\nECMAScript          ← TC39          (语法 / Promise / Map / async)\nDOM / fetch         ← WHATWG        (浏览器 API)\nHTTP / TLS          ← IETF          (RFC)\nfs / http / process ← Node OpenJS   (Node 自加，不是 Web 标准)\n```\n\n写 JS 99% 在调宿主 API（`fetch` / `document` / `fs`），其中只有少数真正是 ECMAScript。**`fetch` 不是 ECMAScript** —— 它是 WHATWG 的，Node / Bun \"借\"过来内置。\n\nJS 这语言官方名是 ECMAScript 不是 JavaScript，因为 _JavaScript_ 是 Sun（现 Oracle）的商标。Brendan Eich 自嘲过 \"ECMAScript 听起来像皮肤病\"，但这名字沿用了 30 年。\n\n## 引擎层：V8 / JavaScriptCore / SpiderMonkey\n\n不是\"标准统一就只该有一个实现\" —— 优化目标真的不同：\n\n|          | V8（Chrome / Edge / Node） | JSC（Safari / Bun）                  | SpiderMonkey（Firefox） |\n| -------- | -------------------------- | ------------------------------------ | ----------------------- |\n| 出身场景 | 标签页常驻几小时           | 页面打开就关                         | 平衡                    |\n| 优化目标 | 长跑峰值                   | **启动 + 内存**                      | 平衡                    |\n| JIT 策略 | 激进多层                   | 克制（保持低层便宜）                 | 保守                    |\n| 许可证   | BSD 3-Clause               | LGPL 2.1                             | MPL 2.0                 |\n| 治理     | Google 主导                | WebKit 联盟（Apple + Igalia + Sony） | Mozilla 基金会          |\n\n**iOS 上所有浏览器都是 JSC** —— Chrome / Edge / Firefox iOS 版只是 Safari 套了 UI 壳，被 App Store 强制用 WebKit。这条 2024 年起 EU DMA 法案才开始松动，但实际进展缓慢。\n\n**Edge 自 2020 年起 = V8** —— Microsoft 当年用的 Chakra 死了，Edge 重写为 Chromium。所以\"V8 阵营\"实际控制桌面 80%+。\n\n许可证差异决定生态形状：V8 的 BSD 才让 Node / Deno / Electron / Cloudflare Workers 这些闭源 / 商业派生敢用；JSC 的 LGPL 允许嵌入式调用但不允许闭源修改引擎本身。这是 Bun 选 JSC 的非技术理由之一。\n\n## 运行时层：Node / Bun / Deno / 浏览器\n\nV8 只是**会跑 JS**，不会读文件、不会发 HTTP、不会监听端口。Node 把 V8 嵌进 C++ 程序，再补上：\n\n```text\nNode = V8 (引擎)\n     + libuv (事件循环 + 跨平台异步 I/O：Linux epoll / macOS kqueue / Windows IOCP)\n     + 标准库 (fs / http / crypto / stream)\n     + N-API (C++ addon 接口)\n     + 模块系统 (CommonJS / ESM)\n```\n\n同一个 V8 嵌进不同宿主就是不同运行时：\n\n|                    | 引擎    | 事件循环                       | 默认权限             |\n| ------------------ | ------- | ------------------------------ | -------------------- |\n| Chrome             | V8      | Blink 自家                     | 沙盒受限             |\n| Node               | V8      | libuv                          | 完整 OS 访问         |\n| Deno               | V8      | Tokio (Rust)                   | 默认无权限，按需授权 |\n| Bun                | **JSC** | 自家（io_uring / kqueue 直调） | 完整 OS 访问         |\n| Cloudflare Workers | V8      | V8 isolate + 自家 scheduler    | 沙盒                 |\n\n**这就是 Node 不能\"换引擎追上 Bun\"的根因**：N-API、所有 native addon、调试器协议、profile 工具全围绕 V8 设计。换 V8 等于重写 Node。Bun 走的是从零写所有胶水的路，代价是 95-98% npm 兼容率（剩下的 2-5% 用了 V8/libuv 内部细节的边角库）。\n\n## 类型层：TypeScript\n\nTS = JS 严格超集 + 类型注解。**类型在编译后完全消失** —— `.ts` → `.js` 后类型一个字符都没了，运行时拿不到任何\"这是 User 不是 string\"的信息。这是为什么 `instanceof` 对 interface 没用：\n\n```typescript\n// 写\nfunction greet(user: User): string { return `Hi, ${user.name}` }\n\n// 编译后实际跑的\nfunction greet(user) { return `Hi, ${user.name}` }\n```\n\nTS 现在多渠道编译：`tsc`（官方，慢）/ `esbuild`（Go）/ `swc`（Rust）/ `bun`（直接跑无中间步骤）/ Node 22+ `--experimental-strip-types`。Microsoft 2025 年用 Go 重写了 `tsc`，叫 TypeScript Native，快 10×。\n\n**TC39 有 Type Annotations 提案**让 JS 引擎直接忽略类型注解（注释化），TS 代码不用编译就能跑。Stage 1 卡了 4 年还没动 —— 浏览器引擎不愿为\"不执行的注解\"做语法解析。短期内不会落地。\n\n---\n\n写代码时叫 TypeScript、调试时叫 JavaScript、查规范时叫 ECMAScript、谈性能时谈 V8 / JSC、谈部署时谈 Node / Bun / Workers —— **同一件事的五个面**，分清面才不被术语骗。"},{"title":"PHP libvips 写文字：Pango 接管 layout，不用算 bbox","slug":"posts/260512-libvips-text-rendering","date":"2026-05-12","tags":["php"],"body":"[GD 那一套 baseline / bbox / 减偏移](200416-php-gd-text-bbox-alignment) 在 libvips 里**不存在**。`Image::text()` 走 _Pango_ 做 layout —— 你给一个 `width` 框 + `align` 方向，结果 image 直接拿到 `$txt->width` / `$txt->height`，没有 baseline 这回事。\n\n## Pango 是什么\n\nGNOME 项目下的**文字 layout 库**。渲染一段文字本来要分两步，libvips 把第一步外包给了它：\n\n| 阶段          | 做什么                                                                               | 谁来做           |\n| ------------- | ------------------------------------------------------------------------------------ | ---------------- |\n| **Layout**    | 选字体、断行、字距、对齐、Bidi（阿拉伯/希伯来从右到左）、CJK 换行规则、emoji shaping | **Pango**        |\n| **Rasterize** | 把矢量字形画成像素                                                                   | FreeType / Cairo |\n\nGD 的 `imagettftext` 只做第二步 —— 调用方必须自己算宽高、自己拆行、自己处理对齐，所以才有 `imagettfbbox` 8 元素数组、baseline 减偏移那堆脏活。libvips 直接传 `width: 800, align: 'high'`，Pango 帮你折行 + 对齐，结果 image 的 `width` / `height` 就是排版后的真实尺寸 —— baseline 这个概念在调用方完全不出现。\n\n额外 bonus：开 `rgba: true` 之后可以传 **Pango markup**，单段里混排粗体 / 颜色 / 字号：\n\n```php\n$txt = Image::text(\n    '<span foreground=\"red\" weight=\"bold\">警告</span> 内容超过限制',\n    ['font' => 'Sans 20', 'rgba' => true]\n);\n```\n\nGD 想做这个得渲染 N 次再拼。\n\n## 实际用法：右下角水印\n\n同样的场景，libvips 写法：\n\n```php\nuse Jcupitt\\Vips\\Image;\n\n$bg  = Image::newFromFile('photo.jpg');\n$txt = Image::text('© 2026', [\n    'font'  => 'Sans Bold 24',\n    'width' => $bg->width - 40,   // 框内自动 word-wrap\n    'align' => 'high',\n    'rgba'  => true,              // 不开是灰度 mask，没法合到彩图\n]);\n\n$bg->composite($txt, 'over', [\n    'x' => 20,\n    'y' => $bg->height - $txt->height - 20,\n])->writeToFile('out.png');\n```\n\nGD 同场景要：`imagettfbbox` 拿 8 元素 → `$bbox[2] - $bbox[0]` 算宽 → `$bg_w - $width - 20` 算 x → 还要担心 baseline 跟图像 y 轴方向相反。libvips 一步：`align: high`。\n\n`Image::text()` 关键 options（[官方文档](https://www.libvips.org/API/current/ctor.Image.text.html)）：\n\n| Option     | 作用                                                          | 默认         |\n| ---------- | ------------------------------------------------------------- | ------------ |\n| `font`     | Fontconfig 名，如 `'Sans Bold 24'` / `'Noto Serif CJK SC 18'` | —            |\n| `fontfile` | 加载非系统字体的 `.ttf` 路径，需配合 `font` 用名字引用        | —            |\n| `width`    | 换行宽度（像素），超过按 word boundary 折行                   | 不换行       |\n| `align`    | `'low'` / `'centre'` / `'high'` —— 多行的水平对齐             | —            |\n| `justify`  | 两端对齐（拉伸词间空格），bool                                | `false`      |\n| `spacing`  | 行距（像素）                                                  | 字体自然行距 |\n| `dpi`      | 渲染 DPI，影响实际像素大小                                    | 72           |\n| `rgba`     | 出彩色 / emoji / Pango markup，不开是 8-bit 灰度 mask         | `false`      |\n\n**部署成本**是 libvips 在 PHP 圈用得不如 GD 普及的主因 —— `ext-gd` 几乎随 PHP 自动装好，libvips 要：\n\n```bash\n# Debian/Ubuntu\napt install libvips42 libvips-dev\n\n# macOS\nbrew install vips\n\n# PHP 端二选一\npecl install vips                                # 传统 C extension\n# 或开 FFI（PHP 7.4+），用 jcupitt/php-vips 包，无需编译扩展\ncomposer require jcupitt/vips\n```\n\n容器里加一行 `apt install libvips42` 就够，但这一行常被忘。装上之后水印 / 缩略图 / 多行排版量大时，libvips 的内存占用和吞吐都比 GD 强一个数量级 —— 它是流水线架构，不像 GD 把整张图按解压尺寸驻留内存。"},{"title":"cloc / tokei / scc：codebase LOC 统计的三代工具","slug":"posts/260510-cloc-tokei-scc","date":"2026-05-10","tags":["git","loc"],"body":"数代码行数（LOC by language）这件事有三代工具，每代换语言、换取舍：\n\n|                   | [cloc](https://github.com/AlDanial/cloc)     | [tokei](https://github.com/XAMPPRocky/tokei) | [scc](https://github.com/boyter/scc)                               |\n| ----------------- | -------------------------------------------- | -------------------------------------------- | ------------------------------------------------------------------ |\n| 实现 / 起年       | Perl / 2006                                  | Rust / 2016                                  | Go / 2018                                                          |\n| Linux kernel 用时 | ~64s                                         | ~1.4s                                        | ~0.8s                                                              |\n| 默认并行          | ❌（要 `--processes=N`）                     | ✓                                            | ✓                                                                  |\n| 安装              | `brew install cloc`                          | `brew install tokei`                         | `brew install scc`                                                 |\n| 独有能力          | `--diff` 对比两个 commit；读 `.zip` / `.tar` | JSON / YAML 输出                             | 复杂度 / [COCOMO](https://en.wikipedia.org/wiki/COCOMO) / 重复检测 |\n\nscc 那三个名词各自的意思：\n\n- **复杂度**：数代码里 `if` / `for` / `&&` 这类分支关键字，估算 cyclomatic complexity。\n- **COCOMO**：1981 年 Barry Boehm 提出的成本模型，给 KLOC 估\"工业平均要多少人月、多少钱\"。\n- **重复检测**：按文件 hash 判定，相同内容只算一次（monorepo / vendor 目录里常见，cloc / tokei 会重复计入）。\n\n对同一个目录（这个 blog 的 `til/src/`）跑默认命令，差异自己跳出来：\n\n```bash\n$ cloc til/src/\nLanguage           files          blank        comment           code\nAstro                 10            117             62           1340\nTypeScript             7             14             49             89\nSUM:                  17            131            111           1429\n\n$ tokei til/src/\n Language         Files        Lines         Code     Comments       Blanks\n Astro               10         1519         1344           59          116\n TypeScript           7          152           97           44           11\n Total               17         1671         1441          103          127\n\n$ scc til/src/\nLanguage        Files       Lines    Blanks  Comments       Code Complexity\nAstro              10       1,519        99        49      1,371         55\nTypeScript          7         152        11        44         97          2\nTotal              17       1,671       110        93      1,468         57\nEstimated Cost to Develop (organic) $40,425\nEstimated Schedule Effort (organic) 4.06 months\nEstimated People Required (organic) 0.88\n```\n\ncloc 没有 Lines 总数列（自己加 blank+comment+code）；tokei 加了 Lines；scc 多 Complexity 列 + 一段 COCOMO 估算块。**同样的 17 个文件，三个工具给出三个 Code 总数：1429 / 1441 / 1468**——blank 和 comment 怎么判，每家略不同。\n\ncloc 的 `--diff` 是至今没被替代的核心——code review 想量化\"这个 PR 改了多少 code 行\"时，tokei / scc 都做不到：\n\n```bash\n$ cloc --git --diff HEAD~5 HEAD\nLanguage        files          blank        comment           code\nMarkdown\n same              0              0              2            924\n modified          2              0              0              9\n added             0              0              0              4\n removed           0              0              0             19\nBourne Shell\n same              0              0              5             14\n modified          1              0              0              1\n added             0              0              0              1\n removed           0              0              0              0\nSUM:\n same              0              0              7            938\n modified          3              0              0             10\n added             0              0              0              5\n removed           0              0              0             19\n```\n\n按语言分组，每组拆 `same / modified / added / removed`；想细到文件级加 `--by-file`。比 `git diff --stat` 信噪比高一截。\n\n**怎么选：** 默认装 scc（最快 + 复杂度）；CI 输出要 JSON 接管线用 tokei；要 git diff 的 LOC 变化才掏 cloc。"},{"title":"git-extras：把多步 git 操作压成一个动词","slug":"posts/260509-git-extras","date":"2026-05-09","tags":["git"],"body":"[`tj/git-extras`](https://github.com/tj/git-extras) 是 70+ 个 `git-xxx` 子命令的合集，`brew install git-extras` 装完直接以 `git xxx` 调用。70 个里真正值得放进肌肉记忆的，是那些**把多步 git incantation 压成一个动词**的：\n\n`git summary` 是接手陌生仓库的瑞士军刀，一行看到全貌：\n\n```bash\n$ git summary\n project     : blog-hugo\n repo age    : 1 year, 1 month\n commits     : 37\n active on   : 19 days\n authors     :\n    36 zhaoyifan   97.3%\n```\n\n`git effort <path>` 接着追问\"哪些文件改得最多\"，按 commits × active days 排，找重构 / 归档候选最直接：\n\n```bash\n$ git effort --above 3 content/posts/\n  path                                      commits  active days\n  hugo.yaml..............................   10       6\n  README.md..............................    7       5\n```\n\n- `git undo [n]` — 退掉最近 N 个 commit（保留改动），不用再想 `--soft HEAD~n` 的对错。\n- `git rename-branch new-name` — 同时 rename local + remote，省掉 `branch -m`、`push origin :old`、`push -u origin new` 三步。\n- `git delete-merged-branches` — 一条命令清掉所有已合并到当前分支的本地分支。\n- `git ignore \"*.log\" build/` — 命令行追加 `.gitignore`，不用开文件。\n\nzsh 补全要单独 source：\n\n```bash\necho 'source /opt/homebrew/opt/git-extras/share/git-extras/git-extras-completion.zsh' >> ~/.zshrc\n```\n\n剩下 60+ 命令按需查 <https://github.com/tj/git-extras/blob/master/Commands.md>。"},{"title":"PHP 安装工具的几条路径","slug":"posts/260508-php-install-tools","date":"2026-05-08","tags":["php"],"body":"PHP 安装按\"装什么 + 在哪装\"分四条路径，每条各自的工具：\n\n纯 PHP 代码（复制即用）\n\n- **[PEAR](https://pear.php.net/)** — _PHP Extension and Application Repository_。最早的类库分发，用前要 `include`。已被 Composer 取代，基本退场。\n- **[Composer](https://getcomposer.org/)** — _PHP 依赖管理工具_，灵感来自 npm / Bundler。`composer.json` 声明依赖，`composer install` 拉齐，**事实标准**。\n\nC 扩展，裸机环境（编译 + 改 `php.ini`）\n\n- **[PECL](https://pecl.php.net/)** — _PHP Extension Community Library_。PEAR 的子项目，分发 redis、apcu 这类底层扩展。**已被官方标记 deprecated。**\n- **[PIE](https://github.com/php/pie)** — _The PHP Installer for Extensions_。PHP 基金会官方接管，**取代 PECL**，命令风格抄 Composer。\n\nC 扩展，Docker 容器场景\n\n- **[docker-php-ext-install](https://github.com/docker-library/docs/tree/master/php#how-to-install-more-php-extensions)** — 官方 PHP Docker 镜像内置脚本，容器里装 C 扩展的事实标准。\n- **[install-php-extensions](https://github.com/mlocati/docker-php-extension-installer)** — 上面的友好封装，自动处理依赖、编译选项、共享库。`Dockerfile` 里写 PHP 几乎人人用它。\n\nPHAR 形式的 CLI 开发工具（PHPUnit、phpstan、psalm 这种）\n\n- **[PHIVE](https://phar.io/)** — _PHAR Installation and Verification Environment_。装 PHAR 工具并校验签名，避免污染 `composer.json`。\n\n七个命令并排看：\n\n```bash\n# 纯 PHP 代码\npear install Mail                        # 老路子，已退场\ncomposer require monolog/monolog         # 现代\n\n# C 扩展（裸机）\npecl install apcu                        # 老路子（deprecated）\npie install apcu/apcu                    # 现代\n\n# C 扩展（Docker），以装 gd 为例——需要系统库 + configure\napt-get install -y libfreetype-dev libjpeg-dev libpng-dev \\\n  && docker-php-ext-configure gd --with-freetype --with-jpeg \\\n  && docker-php-ext-install gd            # 官方：3 步联动\ninstall-php-extensions gd                 # 友好封装：1 行搞定\n\n# PHAR CLI 工具\nphive install phpunit\n```\n\n记忆：\n\n- **PHP 代码** → Composer\n- **C 扩展，裸机** → PIE（PECL 已 deprecated）\n- **C 扩展，Docker** → install-php-extensions\n- **PHAR CLI 工具** → PHIVE"},{"title":"2025 StackOverflow Developer Survey","slug":"posts/250731-stackoverflow-2025","date":"2025-07-31","tags":["survey"],"body":"[2025 SO Developer Survey - Technology](https://survey.stackoverflow.co/2025/technology) 出了，挑几个感兴趣的数字留个底。\n\n## Programming Language\n\n| #   | 语言       | 使用率 |\n| --- | ---------- | ------ |\n| 1   | JavaScript | 66%    |\n| 2   | HTML/CSS   | 61.9%  |\n| 3   | SQL        | 58.6%  |\n| 4   | Python     | 57.9%  |\n| 5   | Bash/Shell | 48.7%  |\n| 6   | TypeScript | 43.6%  |\n| 7   | Java       | 29.4%  |\n| 8   | C#         | 27.8%  |\n| 9   | C++        | 23.5%  |\n| 10  | PowerShell | 23.2%  |\n\nPython 同比 +7pp，AI / data science 拉动。PHP 没进 top 10。\n\n## AI Model\n\n| 模型               | 使用率 |\n| ------------------ | ------ |\n| OpenAI GPT         | 81.4%  |\n| Claude Sonnet      | 42.8%  |\n| Gemini Flash       | 35.3%  |\n| OpenAI Reasoning   | 34.6%  |\n| DeepSeek Reasoning | 23.3%  |\n\n## Database\n\nPostgreSQL 55.6% / MySQL 40.5% / SQLite 37.5% / SQL Server 30.1% / Redis 28%。PG 拉开 MySQL 15pp。\n\n## Web Framework\n\nNode.js 48.7% / React 44.7% / jQuery 23.4% / Next.js 20.8% / Express 19.9%.\n\n## IDE\n\nVS Code 75.9% / Visual Studio 29% / Notepad++ 27.4% / IntelliJ IDEA 27.1% / Vim 24.3%.\n\n完整报告：<https://survey.stackoverflow.co/2025/>"},{"title":"proto 装 Node.js 多版本","slug":"posts/250726-proto-install-node","date":"2025-07-26","tags":["node","version-manager"],"body":"[proto](https://moonrepo.dev/docs/proto) 是 moonrepo 团队 Rust 写的多 runtime 版本管理器，单二进制管 Node / Bun / Deno / Go / Rust 等。**关键差异：shim-based 架构 + `cd` 自动切版本，不需要往 `.zshrc` / `.bashrc` 里挂任何 shell hook** —— nvm / fnm 都做不到。\n\n## 安装\n\n```bash\nbash <(curl -fsSL https://moonrepo.dev/install/proto.sh)\n```\n\n装到 `~/.proto/`，shim 写到 `~/.proto/shims/` 并自动加进 PATH。不污染 `/usr/local`。\n\n## 装 / 切 Node\n\n```bash\nproto install node lts            # 装最新 LTS\nproto install node 20.10.0        # 装指定版本\nproto pin node lts --to global    # 设全局默认\nproto versions node               # 看本地装了哪些 + 远端可装版本\n```\n\n## 项目级版本：`.prototools`\n\n一文件管所有 runtime，**应该提交进 git**（团队共享工具链版本，类比 `composer.json` 锁版本但管的是工具链本身）：\n\n```toml\nnode = \"20\"\nnpm = \"10\"\nbun = \"1.3\"\n```\n\n`cd` 进项目时无感切换 —— 触发机制是跑 shim 时读最近的 `.prototools`，不需要 shell hook，不需要 `proto use`。\n\n`--to` 三档路径：\n\n| flag                 | 写到                   | 用途                |\n| -------------------- | ---------------------- | ------------------- |\n| `--to local`（默认） | `./.prototools`        | 项目级，提交进 git  |\n| `--to user`          | `~/.prototools`        | 用户级，覆盖 global |\n| `--to global`        | `~/.proto/.prototools` | 系统级默认          |\n\n## 跟 nvm / fnm / mise 比\n\n|           | 写法      | 多 runtime   | 自动切版本         | 备注                          |\n| --------- | --------- | ------------ | ------------------ | ----------------------------- |\n| nvm       | bash 函数 | ❌ Node only | 需装 `avn` 等 hook | 每次 shell 启动加载，明显拖慢 |\n| fnm       | Rust      | ❌ Node only | ✅ 但需 shell hook | nvm 平替，仍 Node-only        |\n| **mise**  | Rust      | ✅           | ✅                 | asdf 插件兼容，生态最大       |\n| **proto** | Rust      | ✅           | ✅ 无 hook         | WebAssembly 插件              |\n\n最直接的对手是 **mise**（前身 rtx），asdf 插件兼容、社区大。proto 的差异是**插件用 WASM 写**（mise / asdf 用 shell / Lua 脚本），跨平台 + 沙盒化。**两者都比 nvm / fnm 现代得多，没必要再用 nvm**。按口味选：要生态用 mise，要插件干净 + 一律 shim 用 proto。\n\n## 工作原理：shim 拦截\n\n```text\n$ which node\n~/.proto/shims/node                  ← 一个 shim 可执行\n\n执行流程：node arg → shim 拦截 → 找最近 .prototools\n       → 解析 node 版本 → exec ~/.proto/tools/node/<v>/bin/node\n```\n\nPATH 永远指向 `~/.proto/shims/`，**真二进制路径在每次调用时动态决定**。这是为什么不需要 shell hook、不需要 `proto use` —— 切版本不动 PATH。代价是每次跑命令多一层 shim 解析，毫秒级，可忽略。\n\n[proto docs](https://moonrepo.dev/docs/proto) · [moonrepo/proto on GitHub](https://github.com/moonrepo/proto)"},{"title":"PHP 图像处理：GD / Imagick / libvips 选型与层级","slug":"posts/250526-image-processing-stack","date":"2025-05-26","tags":["php"],"body":"PHP 圈做图像处理有三个 native 选择 —— GD、Imagick、libvips。它们看起来功能重叠（都能 resize / 转格式 / 合成），关键差异不在「支持哪些格式」（最终都调 libpng / libjpeg / libwebp 同一层 codec），而在 **C 引擎怎么调度内存**，以及**它们在调用链里所处的层级**。\n\n## 三个 lib 对比\n\n| Lib         | 安装                                                      | 内存模型                  | 适用                               |\n| ----------- | --------------------------------------------------------- | ------------------------- | ---------------------------------- |\n| **GD**      | `apt install php-gd`（PHP 自带 ext）                      | 整张图按解压尺寸驻留      | 90% 项目默认                       |\n| **Imagick** | `apt install imagemagick php-imagick`                     | 整图驻留，内存最大        | 要 HEIC / PSD / PDF / 色彩配置文件 |\n| **libvips** | `apt install libvips42` + `composer require jcupitt/vips` | **流水线**，按 chunk 处理 | 高吞吐缩略图、大图（> 4K）         |\n\nGD 的「整张图驻留」是常被忽略的成本：一张 4000×3000 的 JPEG 压缩后 2 MB，解压到内存是 `4000 × 3000 × 4 byte` ≈ **46 MB raw**。PHP-FPM 默认 `memory_limit = 128M`，并发处理两三张就 OOM kill。libvips 是 demand-driven streaming，同一张图峰值内存可压到几 MB（[详见 libvips 写文字那篇](260512-libvips-text-rendering)）。\n\nImagick 在 2026 仍是「功能最全但坑最多」 —— CVE 历史长，上游 `policy.xml` 默认禁用一堆 coder（PDF / PS / HTTPS / MSL），运维要反向解锁才能用全。容器镜像也最大。除非真要 HEIC / PSD，否则 GD 或 libvips 更划算。\n\n## 调用层级\n\n```text\n┌──────────────────────────────────────────────────────────────┐\n│ Layer 1  应用 wrapper（PHP userland）                         │\n│   Intervention v3 / Glide / 自写代码                          │\n│   ↑ driver 可切换                                             │\n├──────────────────────────────────────────────────────────────┤\n│ Layer 2  PHP 扩展（C 绑定 / FFI）                             │\n│   ext-gd        ext-imagick       ext-vips / FFI              │\n├──────────────────────────────────────────────────────────────┤\n│ Layer 3  Native C 引擎                                        │\n│   libgd         ImageMagick       libvips                     │\n│   （内存模型差异在这一层）                                     │\n├──────────────────────────────────────────────────────────────┤\n│ Layer 4  格式 codec（三个引擎共享）                            │\n│   libpng  libjpeg(IJG)  libwebp  libavif  FreeType            │\n├──────────────────────────────────────────────────────────────┤\n│ Layer 5  系统库                                                │\n│   zlib（libpng 依赖）fontconfig（FreeType 字体查找）         │\n└──────────────────────────────────────────────────────────────┘\n```\n\n两个非显然结论：\n\n**Layer 4 是共享的** —— 三个 C 引擎最终都调 libpng / libjpeg / libwebp 读写格式字节。所以问「GD 能不能输出 WebP」本质是问「GD 编译时有没有 link libwebp」，而不是 GD 算法层面的能力。`apt install php-gd` 把 libpng / libjpeg 作为依赖一起拉进来，要 WebP 还得加 `--with-webp` 重编。Imagick 多出来的 HEIC / PSD 也只是它在 Layer 3 自己挂了更多 codec wrapper，不是 Layer 4 神奇变多。\n\n**Layer 1 跟 Layer 2/3 解耦** —— Intervention v3 是 driver-swappable，业务代码写在 Layer 1 抽象层，未来从 GD 升 libvips 只换 driver 注册不动业务：\n\n```php\n// config/image.php\n'driver' => env('IMAGE_DRIVER', 'gd'),   // 切 'imagick' / 'vips' 不动业务代码\n```\n\n写代码时该有意识地利用这一层 —— 直接 `imagecreatefromjpeg()` 把自己钉死在 Layer 2 上，迁移成本就再也下不来了。"},{"title":"macOS 多版本 PHP：shivammathur brew tap","slug":"posts/250420-shivammathur-brew-tap","date":"2024-05-20","tags":["php","macos"],"body":"Homebrew `core` 只供应当前 stable 一个 PHP（当下是 8.5），多项目共存——老 Laravel 跑 7.4、新项目用 8.3——就走不通，必须从 [`shivammathur/php`](https://github.com/shivammathur/homebrew-php) tap 拉版本化 formula。覆盖 5.6 → 8.6，每个主版本另带 `-debug` / `-zts` / `-debug-zts` 三个变体。\n\n```bash\nbrew tap shivammathur/php\nbrew install php@7.4 php@8.3\nbrew unlink php@7.4 && brew link --overwrite php@8.3   # 切换默认\n/opt/homebrew/opt/php@7.4/bin/php -v                   # 或走绝对路径免 link\n```\n\nC 扩展（imagick / redis / xdebug ...）配套用 [`shivammathur/extensions`](https://github.com/shivammathur/homebrew-extensions) tap。命名是「扩展 × PHP 版本」笛卡尔积——每个组合一个独立 formula，PHP 版本必须随扩展名一起写出，所以多版本同装 N 个扩展就是 N×M 个 bottle。\n\n```bash\nbrew tap shivammathur/extensions\nbrew install shivammathur/extensions/imagick@8.3\nbrew install shivammathur/extensions/xdebug@7.4\n```\n\n为什么不直接 `pie install imagick/imagick` / `pecl install imagick`：macOS 上自编扩展要先把 `imagemagick` / `phpize` / `autoconf` / OpenSSL header 这套链拉齐，ImageMagick 6 与 7 不兼容、`oniguruma` 之类间接依赖踩坑率高。brew bottle 直接装预编译产物，把 C 工具链问题外包给 tap 维护者；代价是扩展和某个 `php@x.y` 强绑死，切 PHP 版本得相应换扩展 formula。\n\n只装一个 PHP 留 `core` 的 `php` 就够，多版本 + 扩展才进 tap。"},{"title":"Vim 中文资源","slug":"posts/230202-vim-chinese","date":"2023-02-02","tags":["vim"],"body":"Vim 自带英文教程和帮助文档，中文化分两条路：跑自带教程时切语言，装社区维护的帮助文档包。\n\n`vimtutor` 是 Vim 自带的 30 分钟交互式入门，环境变量切到中文 locale 就能直接读：\n\n```bash\nLANG=zh_CN.UTF-8 vimtutor\n```\n\n`vimcdoc` 是 [yianwillis/vimcdoc](https://github.com/yianwillis/vimcdoc) 维护的中文帮助包，放进 `pack/start` 即随 Vim 加载，`:help` 直接出中文：\n\n```bash\nmkdir -p ~/.vim/pack/my/start\ncd ~/.vim/pack/my/start\ngit clone https://github.com/yianwillis/vimcdoc.git\n```"},{"title":"Dockerfile 中三组易混指令怎么选","slug":"posts/221117-dockerfile-easy-to-confuse-instructions","date":"2022-11-17","tags":["docker"],"body":"Dockerfile 里有几组指令名字相近、用途易混。按\"什么时候执行 + 写到哪里\"两条轴归位即可：\n\n- **构建时执行**（写进 image 层）：`RUN`、`COPY`、`ADD`、`ARG`\n- **运行时执行**（启动 container 时跑）：`CMD`、`ENTRYPOINT`\n- **构建期声明、运行时仍可见**：`ENV`\n- **只是元数据**（不执行也不影响行为）：`LABEL`\n\n## COPY vs ADD\n\n两者都把构建上下文里的文件复制进 image，但 `ADD` 多了两个隐式行为：\n\n- 源是 `.tar` / `.tar.gz` 时**自动解压**到目标目录\n- 源是远程 URL 时**自动下载**\n\n这两个 \"智能\" 行为正是 `ADD` 比 `COPY` 危险的地方——upstream tarball 内容变了，image 跟着变，且解压结果难审计。\n\n口诀：**默认用 `COPY`，确实需要解压本地归档时才上 `ADD`**。远程文件用 `RUN curl -O` 更显式、更可控。\n\n## RUN vs CMD vs ENTRYPOINT\n\n`RUN` 在构建时执行并提交一层。`CMD` 和 `ENTRYPOINT` 都只描述启动命令，区别在能不能被 `docker run` 的尾参覆盖：\n\n| 指令         | 何时跑 | 默认参数？ | 被尾参覆盖？                |\n| ------------ | ------ | ---------- | --------------------------- |\n| `RUN`        | 构建时 | —          | —                           |\n| `ENTRYPOINT` | 启动时 | 否         | 不会（除非 `--entrypoint`） |\n| `CMD`        | 启动时 | 是         | 会                          |\n\n两种写法行为不同：\n\n```dockerfile\nENTRYPOINT [\"/scripts/entrypoint.sh\"]    # exec form\nCMD \"/scripts/start-fpm.sh\"              # shell form\n```\n\n_exec form_（JSON 数组）= 直接 execve，进程是 PID 1，能正常收到 `SIGTERM`。\n_shell form_（裸字符串）= Docker 包成 `/bin/sh -c \"...\"`，再**整串作为参数**传给 `ENTRYPOINT`，最终启动等价于：\n\n```bash\n/scripts/entrypoint.sh /bin/sh -c \"/scripts/start-fpm.sh\"\n```\n\n只有 `CMD [\"nginx\"]` 没 `ENTRYPOINT` 时，`CMD` 自己就是启动命令；`docker run img bash` 会把 `nginx` 整串替换成 `bash`。\n\n口诀：**主程序用 `ENTRYPOINT` exec form；可被覆盖的默认参数走 `CMD` exec form**。两个都用 shell form 会丢掉信号转发，容器收不到 `docker stop` 的 `SIGTERM`，10 秒后被 `SIGKILL` 强杀。\n\n## LABEL vs ARG vs ENV\n\n三者都是 \"声明值\"，但生命周期完全不同：\n\n| 指令    | 构建时可见？       | 运行时可见？       | 写进 image？                                      |\n| ------- | ------------------ | ------------------ | ------------------------------------------------- |\n| `ARG`   | 是                 | 否                 | 否（但值会出现在 `docker history`，**别塞密钥**） |\n| `ENV`   | 是（声明之后的层） | 是（容器环境变量） | 是                                                |\n| `LABEL` | 否                 | 否                 | 是（仅元数据，`docker inspect` 可见）             |\n\n`ARG` 还有一条作用域陷阱：**`FROM` 之前声明的 `ARG` 只能在 `FROM` 行里展开**，要在后续指令里用同名 ARG，得在 `FROM` 之后**再声明一次**：\n\n```dockerfile\nARG PHP_VERSION=8.3\nFROM php:${PHP_VERSION}-fpm    # OK，这里能用\n\nARG PHP_VERSION                # 必须重新声明，下面才能用\nRUN echo \"Building PHP ${PHP_VERSION}\"\n```\n\n口诀：**版本号 / 镜像 tag 用 `ARG`；运行时配置（端口、路径、非密钥配置）用 `ENV`；作者 / VCS 地址 / 镜像描述用 `LABEL`**。"},{"title":"Laradock 偷师：6 个值得抄走的 Docker 工程模式","slug":"posts/221109-laradock-patterns","date":"2022-11-09","tags":["docker"],"body":"[Laradock](https://github.com/laradock/laradock) 是 PHP/Laravel 的 docker-compose 全家桶模板。近年活跃度下降，新 Laravel 项目更多用 [Sail](https://laravel.com/docs/sail)。但它的 Dockerfile / compose 配置里有些经过多年打磨的模式，值得偷师——以下 6 条不限 PHP 场景。\n\n## 1. PUID/PGID：容器用户对齐宿主，否则 bind mount 文件全是 root\n\n```dockerfile\nARG PUID=1000\nARG PGID=1000\nRUN groupadd -g ${PGID} laradock && \\\n    useradd -l -u ${PUID} -g laradock -m laradock\n```\n\n容器默认 root 跑，写到 bind mount 的文件宿主端归 root 所有——开发者下次 `vim` 撞 permission denied，`rm` 要 sudo。把 host 的 `id -u` / `id -g` 通过 `.env` 注入 ARG，容器内建同 uid 用户，文件落地就是宿主用户。**Linux dev 必踩**；Mac/Win 因 Docker Desktop 自动做 uid 映射，不会感受到这个坑。\n\n## 2. `DEBIAN_FRONTEND=noninteractive` 写 RUN 不写 ENV\n\n```dockerfile\n# 不要：会污染容器运行时\nENV DEBIAN_FRONTEND=noninteractive\n\n# 这样：只作用于该层 build\nRUN DEBIAN_FRONTEND=noninteractive apt-get install -y tzdata\n```\n\n`ENV` 写进的是镜像层、运行时也生效——之后 `docker exec` 进容器手动装东西时，所有交互配置询问也被跳过（默认值生效），可能错过冲突选项或时区选择。`RUN` 内赋值只对那一条命令生效，构建期免提示、运行期保留交互。Docker 官方 [Dockerfile best practices · ENV](https://docs.docker.com/develop/develop-images/instructions/#env) 同此建议。\n\n## 3. `extra_hosts: dockerhost` 把\"容器访问宿主\"统一掉\n\n```yaml\nextra_hosts:\n  - \"dockerhost:${DOCKER_HOST_IP}\"\n  # Linux 20.10+ 现在还可以这样：\n  - \"host.docker.internal:host-gateway\"\n```\n\nMac/Win Docker Desktop 自带 `host.docker.internal`，Linux 长期没有（20.10 才支持 `host-gateway` magic value）。Laradock 给容器塞统一别名 `dockerhost`——Xdebug 回连 IDE 时永远写 `xdebug.client_host=dockerhost`，跨平台共用一份 ini。\n\n## 4. `ARG INSTALL_XXX=false` 的 opt-in 扩展矩阵\n\n```dockerfile\nARG INSTALL_XDEBUG=false\nRUN if [ ${INSTALL_XDEBUG} = true ]; then \\\n    pecl install xdebug && docker-php-ext-enable xdebug; \\\nfi\n```\n\n```dotenv\nWORKSPACE_INSTALL_XDEBUG=true\nPHP_FPM_INSTALL_XDEBUG=true\n```\n\n一份 Dockerfile 适配 N 个项目：要 swoole 的项目 `INSTALL_SWOOLE=true`，不要的 false——不用为每个项目 fork 一份 Dockerfile。代价：build cache 粒度变粗（任一 ARG 变全层失效），高频切换的扩展（xdebug）值得拆独立 stage 减小重建范围。\n\n## 5. `docker-php-ext-disable` 切扩展不重编译\n\n```bash\n# 生产关 xdebug，.so 文件仍保留\ndocker-php-ext-disable xdebug pcov\n\n# 开发再 enable\ndocker-php-ext-enable xdebug\n```\n\nPHP 扩展本质是 `.so` + 一行 `extension=xdebug.so` ini 引用。`docker-php-ext-disable` 只移走 ini，`.so` 还在。开发/生产同一个镜像，不为\"是否带 xdebug\"重编两套——秒级切换。这条 official PHP image 自带，跟 Laradock 无关，但 Laradock 大量在用，是个被低估的工具。\n\n## 6. db / cache 用命名 volume，不要 bind mount\n\n```yaml\nservices:\n  mysql:\n    volumes:\n      - mysql:/var/lib/mysql # 这样\n      # - ./data/mysql:/var/lib/mysql   # 不要这样\nvolumes:\n  mysql:\n```\n\n源码和数据卷分两种挂法 —— 源码要编辑器实时改，bind mount；数据要长期存活、和代码仓库脱钩，named volume。后者具体好在哪：\n\n- **误删难** —— `rm -rf ./data` 碰不到，得 `docker volume rm` 才删得了\n- **不污染 git** —— bind mount 的 `./data/mysql/` 会进工作树，`.gitignore` 漏一次就把 ibdata 提进仓库\n- **备份 / 迁移有现成模式** —— named volume 跑一次 helper 容器就能打包；bind mount 还得另处理 owner\n- **权限不打架** —— 容器里 mysql uid 999 经常 chown 不动 host 目录；named volume 由容器自己初始化，没这层\n\n**规则**：源码 bind mount，数据卷（mysql / postgres / redis / elasticsearch）走 named volume。"},{"title":"git clean 删未跟踪文件：先 -n 看，-X 单独清 ignored","slug":"posts/221001-git-clean-untracked","date":"2022-10-01","tags":["git"],"body":"`git clean` 删的是 working tree 里**未被追踪**的文件——`.gitignore` 命中的、和未 `add` 过的新文件。删完没有 `git checkout` 可以救，所以**永远先用 `-n` 走一遍**：\n\n```bash\ngit clean -n           # 列出会删的文件，不动磁盘\ngit clean -nd          # 含未跟踪的目录\ngit clean -nXd         # 只列 .gitignore 命中的（如 build/ dist/）\n```\n\n确认后把 `-n` 换成 `-f`（`force`，必填——不写就拒绝执行）：\n\n```bash\ngit clean -fd          # 删未跟踪的文件 + 目录\ngit clean -fXd         # 只删 ignored（清 build artifacts、.cache）\ngit clean -fd -- src   # 限定路径\n```\n\n**`-X` 大小写反义**：\n\n- `-X`（大写）：**只**删 `.gitignore` 命中的，未跟踪的新文件（如手动创建忘 add 的）保留\n- `-x`（小写）：把 ignored **也算成**未跟踪一并删——破坏性更大，会清掉 `.env`、本地 IDE 配置等被 ignore 的工作文件，慎用"},{"title":"git 改提交时间：--date 只动 author，要改 committer 用环境变量","slug":"posts/221001-git-commit-date","date":"2022-10-01","tags":["git"],"body":"git 每个 commit 有**两个时间字段**：_author date_（谁写的代码、什么时候写的）和 _committer date_（谁应用的这个 commit、什么时候应用的）。`git log` 默认显示 author date，但 `git log --format=%ci`、GitHub 时间线排序、`git log --since` 都看 committer date——只改 `--date` 常出现\"日志看着对了，GitHub 顺序没动\"。\n\n新 commit 同时改两个：\n\n```bash\nGIT_COMMITTER_DATE=\"2019-06-11T17:50:50+08:00\" \\\n  git commit -m \"fix...\" --date=\"2019-06-11T17:50:50+08:00\"\n```\n\n修上一个 commit（**会改 hash**，已 push 的不要碰）：\n\n```bash\nGIT_COMMITTER_DATE=\"$(date -R)\" \\\n  git commit --amend --no-edit --date=\"$(date -R)\"\n```\n\n验证两个字段：\n\n```bash\ngit log -1 --format='author %ai%ncommit %ci'\n```"},{"title":"git 忽略文件权限位变更：core.filemode false","slug":"posts/221001-git-ignore-filemode","date":"2022-10-01","tags":["git"],"body":"跨 OS 协作时（macOS / Linux ↔ WSL ↔ NTFS / FAT 网络盘），git 会把权限位变化（`100755` ↔ `100644`）当成内容变更，`git status` 满屏 mode-only diff 但其实没东西可改。\n\n仓库局部关掉：\n\n```bash\ngit config core.filemode false\n```\n\n`core.filemode` **不在版本控制里**——它是 `.git/config` 的本地设置，不会同步给协作者（git 设计如此：权限策略是工作环境属性，不是仓库属性）。所以改完不用 commit。\n\n陷阱：一刀切关掉后，**你新加的可执行脚本权限不会被同事看到**——CI 上跑 `./deploy.sh` 直接 permission denied。要保留\"真正的可执行位\"只压\"杂讯\"，用：\n\n```bash\ngit update-index --chmod=+x path/to/script.sh\n```\n\n显式把这个文件标记成 executable 写进 index，比关 filemode 精准。"},{"title":"git 建独立无历史的分支用 --orphan","slug":"posts/221001-git-orphan-branch","date":"2022-10-01","tags":["git"],"body":"`git checkout --orphan` 创建一个不指向任何已有 commit 的新分支——HEAD 移过去后 working tree 保留，但 staging 和 commit history 都从零开始。常用场景：一个 repo 同时管代码和文档站，文档站需要独立 history（如 `gh-pages`），或者发布开源版本时不想暴露内部 commit 史。\n\n```bash\ngit checkout --orphan gh-pages\ngit rm -rf .                         # 关键：清掉父分支带过来的 working tree\necho \"<h1>hi</h1>\" > index.html\ngit add . && git commit -m \"init gh-pages\"\ngit push origin gh-pages\n```\n\n陷阱：`--orphan` 切过去后 `git status` 会显示一堆 staged 文件——那是从父分支继承的 working tree，**没 `git rm -rf .` 就 commit 等于把整套源码塞进了所谓的\"独立\"分支**。"},{"title":"查 Linux 包：pkgs.org 找上游 + rpm/dpkg 看本地","slug":"posts/220728-pkgs-org-installed-packages","date":"2022-07-28","tags":["linux"],"body":"排查\"这台机器上 X 是哪个版本\"\"我想装的版本哪个发行版有\"——两边问题，两类工具。\n\n[pkgs.org](https://pkgs.org/) 把主流发行版（CentOS/RHEL/Fedora/Debian/Ubuntu/openSUSE/Arch...）的官方仓库索引到一起，按包名搜能直接看到各发行版各版本提供的版本号、依赖、下载链接。比挨个翻 `apt search` / `yum search` 快，也方便确认\"上游有没有这个版本\"。\n\n本地查\"装了什么版本\"：\n\n```bash\n# Red Hat / CentOS / Fedora (rpm 系)\nrpm -qa | grep docker            # 列出所有匹配的已装包\nrpm -q docker-ce                 # 查具体包\nrpm -qi docker-ce                # 详细信息（描述、维护者、构建时间）\n\n# Debian / Ubuntu (dpkg 系)\ndpkg -l | grep docker            # 已装包，含版本和架构\napt list --installed | grep docker\ndpkg -L docker-ce                # 列出这个包装了哪些文件\n```\n\n`rpm -qa` / `dpkg -l` 走本地包数据库，不联网；`pkgs.org` 是上游索引，不知道你本地装了啥。两者互补——前者答\"装没装\"，后者答\"哪儿有装的\"。"},{"title":"查 CNAME：dig / host / nslookup 三选一","slug":"posts/220728-cname-lookup","date":"2022-07-28","tags":["network"],"body":"CNAME（_canonical name_）是 DNS 把一个域名指向另一个域名的别名记录。常见场景：`www.example.com` CNAME 到 CDN 边缘域名，或 SaaS 给你一个 `<your>.heroku.com` 让你 CNAME 过去。\n\n```bash\ndig example.com CNAME              # 完整记录\ndig +short example.com CNAME       # 只要目标\ndig +trace example.com             # 完整解析路径（root → TLD → 权威）\n\nhost -t CNAME example.com          # 输出最人话：\"is an alias for ...\"\n\nnslookup -type=CNAME example.com   # Windows 时代遗留，少用\n```\n\nCNAME 两条硬规则：\n\n- **不能和其他记录共存**：一个 name 上有 CNAME 就不能再有 A / MX / TXT。所以根域名 `example.com` 通常**不能用 CNAME**（`MX` 在那里），子域才行\n- **链式解析**：CNAME 可以指向另一个 CNAME，resolver 会一路 chase，最终必须落到 A/AAAA。链长了拖慢首次解析\n\n挂在云厂商前的根域名查不出 CNAME 多半是因为对方用了 _ANAME / ALIAS / flattening_——根域 CNAME 限制的绕过手段，权威服务器替你预解析到 A 记录返回，外部看到的就是一条 A 而不是 CNAME。"},{"title":"CDPATH：让 cd 自动搜多个父目录","slug":"posts/220728-cdpath","date":"2022-07-28","tags":["shell"],"body":"`cd foo` 默认只在当前目录找 `foo/`。`CDPATH` 给 cd 一份\"备选父目录列表\"，找不到就依次往里查：\n\n```bash\nexport CDPATH=.:~/projects:~/work\n```\n\n之后无论在哪个目录：\n\n```bash\ncd blog          # 命中 ~/projects/blog/\ncd client-x      # 命中 ~/work/client-x/\n```\n\n三个坑：\n\n- **第一项必须是 `.`**——否则 `cd subdir` 会优先去 CDPATH 列表里搜，遇到同名目录就把你\"传送\"到别处\n- **CDPATH 命中时 `cd` 会把目标路径打到 stdout**，会污染脚本里 `cd \"$dir\" && do-something` 这类捕获 cd 输出的逻辑。**脚本开头 `unset CDPATH`**，并用绝对路径不依赖它\n- **不要塞太多目录**——顺序匹配，列表越长越容易撞同名；3-5 个就到头\n\n适合场景：固定的几个 workspace 根目录（`~/code`、`~/work`、`~/oss`）。如果项目散得很（dotfiles 在 home、客户代码在 NAS、个人项目在 iCloud），用 [zoxide](https://github.com/ajeetdsouza/zoxide) 这种基于 _frecency_ 的跳转工具更顺手。"},{"title":"删了文件 df 不掉：lsof | grep deleted 找仍持有的进程","slug":"posts/220728-disk-space-deleted-files","date":"2022-07-28","tags":["shell"],"body":"经典症状：`rm` 掉一个大日志，`df` 看磁盘空间没变。\n\n根因：unix 文件系统下 `rm` 做的只是 _unlink_——把目录项里的引用删掉。inode 真正释放要等到**所有持有它的 file descriptor 都关闭**。如果某个进程还开着这个文件（典型场景：app 把日志 open 后一直 append），inode 就一直挂在 fd 上不释放，磁盘空间也回不来。\n\n定位是哪个进程：\n\n```bash\nlsof | grep deleted\n# nginx  1234  www-data  3w  REG  8,1  524288000  131  /var/log/nginx/access.log (deleted)\n```\n\n第 7 列就是占的字节数，第 1-2 列是进程名 + PID。两条出路：\n\n```bash\n# A. reload / 重启持有进程，让它关 fd\nnginx -s reopen                  # nginx 专门有这个信号\nsystemctl restart <svc>          # 通用兜底\n\n# B. 源头改成 truncate 而不是 rm\ntruncate -s 0 /var/log/access.log    # 文件保留，内容清空，进程继续写同一 inode\n> /var/log/access.log                # 同效果，shell 内置\n```\n\n**别用 `echo \"\" > file`**——会写一个换行符进去，不是真清空。\n\n预防：日志类场景上 `logrotate` 并开 `copytruncate` 选项，它就是为了解决 rotate 时进程仍持有老 fd 的问题。"},{"title":"shell job control:& nohup Ctrl+Z fg bg jobs","slug":"posts/220728-job-control-bg-fg-nohup","date":"2022-07-28","tags":["shell"],"body":"把命令丢后台、再拽回前台的全套手势。\n\n`&` 直接后台跑，但**关 terminal 时会被 SIGHUP 打死**——shell 退出会向自己所有子进程发 _HUP_。`nohup` 的作用就是让进程忽略 HUP，于是关 terminal 也不死：\n\n```bash\ncmd &                            # 后台跑，关终端就没了\nnohup cmd &                      # 关终端不死，stdout 默认写 nohup.out\nnohup cmd > out.log 2>&1 &       # 自己接管输出\n```\n\n已经在前台跑的命令想丢后台——按 `Ctrl+Z` 暂停（SIGTSTP），shell 把它放进 jobs 列表，然后 `bg` 让它在后台继续跑：\n\n```bash\n$ long-running-cmd\n^Z\n[1]+ Stopped  long-running-cmd\n$ bg                             # 继续在后台跑\n$ jobs -l                        # 看 jobs 列表（带 PID）\n[1]+ 12345 Running   long-running-cmd &\n$ fg                             # 拽回前台\n```\n\n多个 job 时用 `%N` 选：\n\n```bash\nfg %1                            # 拽 job 1 回前台\nfg %vim                          # 按命令名匹配\nbg %2                            # 让 job 2 在后台继续\nkill %1                          # 杀 job 1\n```\n\n`nohup` 只挡 HUP，不解决长跑任务的\"我下次想接回 session 看实时输出\"。生产场景认真用 `tmux` / `screen` / systemd unit，nohup 是临时手段。"},{"title":"shell 脚本必加 set -euo pipefail","slug":"posts/220728-set-euo-pipefail","date":"2022-07-28","tags":["shell"],"body":"bash 默认行为对脚本极不友好——命令失败默认继续往下跑、未定义变量默认空串、管道里前面命令失败被后面成功的命令掩盖。每个长一点的脚本开头都该加：\n\n```bash\n#!/usr/bin/env bash\nset -euo pipefail\n```\n\n各位含义：\n\n- **`-e`（_errexit_）**：任一命令非零退出立即终止脚本。**陷阱**：在 `||` / `&&` / `if` / `while` 条件里的命令不算\n- **`-u`（_nounset_）**：用未定义变量直接报错。把 `rm -rf /$UNDEFINED/` 这种灾难挡在执行前。可能未定义的变量用 `${VAR:-default}` 显式给默认值\n- **`-o pipefail`**：默认 `cmd1 | cmd2` 的退出码只看 `cmd2`——`curl bad-url | jq .` 即便 curl 失败也返 0。开 pipefail 后任一段失败整个 pipeline 就失败\n- **`-x`（_xtrace_）**：执行前打印每条命令。debug 用，**生产脚本别开**——会在日志里泄露 password / token\n\n不开 errexit 时的手动错误处理：\n\n```bash\ncmd || { echo \"cmd failed: $?\" >&2; exit 1; }\n\nif ! cmd; then\n  echo \"cmd failed\" >&2\n  exit 1\nfi\n```\n\nerrexit 不是银弹——在 pipeline、command substitution、subshell 等场景下都有边界，`set -e` 会\"看起来开了实际没生效\"。强约束脚本（部署 / 数据迁移）建议直接上 [`shellcheck`](https://www.shellcheck.net/) 做静态检查，比 `set -e` 兜底更可靠。"},{"title":"npx 跑 npm 包的几种姿势","slug":"posts/220726-npx","date":"2022-07-26","tags":["node"],"body":"`npx` 是 npm 自带工具，主要场景：跑本地 / 远端 / 指定版本的 npm 包，省掉全局安装。\n\n## 跑本地 node_modules/.bin\n\n项目装了 gulp 想跑：\n\n```bash\nnode_modules/.bin/gulp -v\n# 等价于\nnpx gulp -v\n```\n\n`npx` 查找顺序：`node_modules/.bin` → 系统 `$PATH` → 远端下载。所以系统命令也能 `npx ls`，但 shell builtin（如 `cd`）不行。\n\n## 跑远端包，免全局安装\n\n```bash\nnpx create-react-app my-app   # 临时下载 → 跑完即删\nnpx http-server               # 在当前目录起静态服务器\n```\n\n下次再跑还要重新下载。强制只用本地、不下载远端：`npx --no-install <pkg>`；反过来强制用远端忽略本地：`npx --ignore-existing <pkg>`。\n\n## 指定 Node 版本跑脚本\n\n```bash\nnpx node@0.12.8 -v\n# v0.12.8\n```\n\n临时切版本比 nvm 省事。\n\n## `-p` 先装包再跑命令\n\n```bash\nnpx -p node@0.12.8 node -v          # 先装 node@0.12.8，再跑 node -v\nnpx -p touch nodetouch gulpfile.js  # 先装 touch 包，再跑 nodetouch\n```\n\n## 跑 GitHub / Gist 源码\n\n```bash\nnpx github:piuccio/cowsay hello\nnpx https://gist.github.com/zkat/4bc19503fe9e9309e2bfaa2c58074d32\n```\n\n要求远端必须是合法包：含 `package.json` 和入口脚本。\n\n> 来源：[npx 使用教程 - 阮一峰](https://www.ruanyifeng.com/blog/2019/02/npx.html)"},{"title":"GROUP_CONCAT 截断","slug":"posts/220110-group-concat-max-len","date":"2022-01-10","tags":["mysql"],"body":"生产踩了一个：`GROUP_CONCAT` 聚合的结果被悄悄截断，默认上限只有 1024 bytes。\n\n```sql\nSELECT @@GROUP_CONCAT_MAX_LEN;\n-- 1024\n```\n\n会话临时调大：\n\n```sql\nSET SESSION group_concat_max_len = 1000000;\n```\n\n永久改 `my.cnf`：\n\n```ini\n[mysqld]\ngroup_concat_max_len = 1000000\n```\n\n参考 [`group_concat_max_len`](https://dev.mysql.com/doc/refman/8.0/en/server-system-variables.html#sysvar_group_concat_max_len)。"},{"title":"PHP __invoke：实例 callable，相当于带状态闭包","slug":"posts/210720-php-invoke-callable-object","date":"2021-07-20","tags":["php"],"body":"`__invoke` 是 PHP 的 magic method —— 类里定义了它，实例就能当函数直接调用：\n\n```php\nclass Adder {\n    public function __construct(private int $base) {}\n    public function __invoke(int $x): int { return $this->base + $x; }\n}\n\n$add5 = new Adder(5);\n$add5(3);                     // 8\nis_callable($add5);           // true\n```\n\n价值不在\"语法甜\"，在**承载状态的 closure**。原生 closure 用 `use ($base)` 捕获变量；`__invoke` 用 `__construct` 注入依赖、`$this` 持有任意复杂状态，还能继承、被 mock、实现接口。\n\n## 单动作 controller（Laravel）\n\n路由绑类不绑方法，文件 = 端点。一个 controller 做一件事，不再有 8 个 action 挤一个 class：\n\n```php\nclass ShowProfile extends Controller\n{\n    public function __invoke(int $id)\n    {\n        return view('user.profile', ['user' => User::findOrFail($id)]);\n    }\n}\n\nRoute::get('user/{id}', ShowProfile::class);\n```\n\n文档：[Single Action Controllers | Laravel](https://laravel.com/docs/controllers#single-action-controllers)。\n\n## 策略 / provider 对象\n\n需要\"可配置的函数\"时 —— 查表、解析、路由匹配 —— 比 closure 更适合，因为有完整 OOP toolkit 可用。AWS SDK 的 [`PatternEndpointProvider`](https://github.com/aws/aws-sdk-php/blob/master/src/Endpoint/PatternEndpointProvider.php) 就是这个套路：构造时吃配置 patterns，调用时按 `service/region` 查规则、返回 endpoint：\n\n```php\n$provider = new PatternEndpointProvider($patterns);\n$endpoint = $provider(['service' => 's3', 'region' => 'us-east-1']);\n```\n\n调用方不必知道里面是查表、远程 fetch 还是 ML 推理 —— 签名永远是\"参数 in, 结果 out\"。换实现不动 caller。\n\n## 一个坑：`callable` ≠ `Closure`\n\n`is_callable($obj)` 返回 `true`，但 `$obj instanceof Closure` 是 `false`。type-hint 写 `Closure` 会拒掉 `__invoke`-able 对象，写 `callable` 才接受：\n\n```php\nfunction run(callable $fn) { return $fn(1); }     // ✅ 接受 Adder 实例\nfunction run(Closure $fn)  { return $fn(1); }     // ❌ TypeError\n```\n\n要把 `__invoke` 对象转成真正的 `Closure` 实例（比如某 API 强制 `Closure` 入参），用：\n\n```php\n$closure = Closure::fromCallable($add5);  // PHP 7.1+\n$closure = $add5(...);                    // PHP 8.1+ first-class callable\n```"},{"title":"PHP strict_types 看调用方，不看声明方","slug":"posts/210719-php-strict-types-call-site","date":"2021-07-19","tags":["php"],"body":"`declare(strict_types=1)` 关掉 PHP 的隐式类型转换 —— 函数 type-hint 写 `int $x`，不再吞 `1.0` / `\"15\"` / `true`，传错直接 `TypeError`：\n\n```php\ndeclare(strict_types=1);\nfunction add(int $a, int $b): int { return $a + $b; }\nadd(1.0, 2.0);\n// PHP Fatal error: Uncaught TypeError: add(): Argument #1 ($a) must be of type int, float given\n```\n\n不写 declare 行（默认 weak mode）时 `add(1.0, 2.0)` 返回 `int(3)`，安静吞掉。\n\n## 反直觉点：strict 看 caller，不看 callee\n\n文件级声明会让人下意识以为\"哪边定义函数 / 类，哪边的 mode 控制类型检查\"。**反了**：检查谁，看 **call site 所在文件**的 declare。\n\n`A.php` 严格 / `B.php` 默认：\n\n```php\n// A.php\ndeclare(strict_types=1);\nfunction add(int $a, int $b): int { return $a + $b; }\n\n// B.php\nrequire 'A.php';\nadd(1.0, 2.0);  // ✅ int(3) —— B 没声明 strict，照旧 coerce\n```\n\n`A.php` 默认 / `B.php` 严格：\n\n```php\n// A.php\nfunction add(int $a, int $b): int { return $a + $b; }\n\n// B.php\ndeclare(strict_types=1);\nrequire 'A.php';\nadd(1.0, 2.0);  // ❌ TypeError —— B 声明了 strict，调用按 strict 检查\n```\n\n实际后果：第三方库无论自家标了多少 `strict_types`，决定你调它会不会抛 `TypeError` 的是**你自己文件顶部那行**。Composer 装回来的 `vendor/` 大概率是 strict，但你在 weak 文件里传 `1.0` 给它的 `int` 参数，PHP 不会拦。\n\n要\"全项目 strict\"，每个 `.php` 顶部都得写一遍。靠 [php-cs-fixer](https://github.com/PHP-CS-Fixer/PHP-CS-Fixer) 的 `declare_strict_types` rule 或 [symplify/easy-coding-standard](https://github.com/symplify/easy-coding-standard) 自动补，别手写。\n\n## 写法约束\n\n必须是脚本里**最早**的语句（`<?php` 后第一行，前面不能有 `use` / 注释外的任何内容）：\n\n```text\nPHP Fatal error: strict_types declaration must be the very first statement in the script\n```\n\n不能用 block 形式：\n\n```php\ndeclare(strict_types=1) { ... }   // ❌ must not use block mode\n```\n\ndeclare 行**只**作用于当前文件，不会污染 require / include 进来的脚本 —— 这正是允许逐文件渐进迁移的设计。"},{"title":"Git `--allow-empty`：init repo 先打个空 commit 占位","slug":"posts/210712-git-allow-empty-init-commit","date":"2021-07-12","tags":["git"],"body":"```bash\ngit commit -m \"init repo\" --allow-empty\n```\n\n第一个 commit 是 git history 的根。`git rebase -i HEAD~N` 默认从**根之后**开始，**根 commit 不在 todo list 里** —— 除非显式 `git rebase --root -i`（操作复杂度陡增）。先放一个空 init commit 占位，后面所有内容性 commit 都可以正常 rebase / squash / reorder。\n\n`--allow-empty` 的其他真实场景：\n\n- CI 只在 push 时触发但 working tree 没改 —— 强推一个空 commit 触发流水线\n- 给一批工作起点打锚点（`feat: kick off auth refactor`）方便后续 `git log --grep`"},{"title":"SQL AND / OR 执行优先级","slug":"posts/210129-and-or-precedence","date":"2021-01-29","tags":["mysql"],"body":"`AND` 优先级高于 `OR`，所有 SQL 方言通用：\n\n```sql\nSELECT id FROM t WHERE c1 OR c2 AND c3;\n\n-- 实际等价于（AND 先绑定）：\nSELECT id FROM t WHERE c1 OR (c2 AND c3);\n\n-- 不是这样（很多人下意识这么想）：\nSELECT id FROM t WHERE (c1 OR c2) AND c3;\n```\n\n口诀：`AND` 看成 `*`，`OR` 看成 `+`，乘号先算。\n\nBug 不会报错——query 跑成功，只是结果错——所以 `WHERE` 里只要 `AND` 和 `OR` 混用，**永远写括号**，别赌读者（或几个月后的自己）记得优先级。"},{"title":"MySQL DELETE 子查询自引 1093 错误","slug":"posts/210129-delete-self-reference-1093","date":"2021-01-29","tags":["mysql"],"body":"想删一张表里的重复数据，按 `name` 分组保留 `MIN(id)` 那条：\n\n```sql\nDELETE FROM student\nWHERE id NOT IN (SELECT MIN(id) FROM student GROUP BY `name`);\n```\n\n直接跑会报：\n\n```bash\nERROR 1093 (HY000): You can't specify target table 'student' for update in FROM clause\n```\n\nMySQL 不允许在 `DELETE` / `UPDATE` 的子查询里**直接引用目标表**——执行时一边扫一边改会让结果不确定，干脆禁掉。\n\n绕开的办法：再套一层 derived table，强制 MySQL 把子查询**物化成临时表**后再用，与目标表解耦：\n\n```sql\nDELETE FROM student\nWHERE id NOT IN (\n  SELECT id FROM (\n    SELECT MIN(id) AS id FROM student GROUP BY `name`\n  ) tmp                                              -- 关键：再包一层\n);\n```\n\nMySQL 8.0+ 用 CTE 写更清晰，CTE 同样会被物化，所以一样能绕开 1093：\n\n```sql\nWITH keep AS (SELECT MIN(id) AS id FROM student GROUP BY `name`)\nDELETE FROM student WHERE id NOT IN (SELECT id FROM keep);\n```\n\nPostgreSQL / SQL Server 没这个限制，直接写第一种形式就行。"},{"title":"MySQL FOR UPDATE / FOR SHARE 真实加锁范围","slug":"posts/210129-for-update-lock-scope","date":"2021-01-29","tags":["mysql"],"body":"`SELECT ... FOR UPDATE` 和 `FOR SHARE`（8.0.1 之前叫 `LOCK IN SHARE MODE`）加的锁经常**比写 SQL 时以为的大一圈**。规则不复杂，但每条单独看都反直觉。\n\n## S 锁 vs X 锁\n\n|        | 已有 S | 已有 X |\n| ------ | ------ | ------ |\n| 申请 S | ✅     | ❌     |\n| 申请 X | ❌     | ❌     |\n\n- `FOR SHARE` = S 锁，多事务可同时拿\n- `FOR UPDATE` / `INSERT` / `UPDATE` / `DELETE` = X 锁，互斥\n\n死锁陷阱：两个事务都拿了同一行 S 锁，**任何一方想升级成 X 锁都会死锁**——必须先等对方释放。所以 `FOR SHARE` 后立刻 `UPDATE` 是高危模式。\n\n## 锁加在索引上，不是行上\n\n```sql\nSELECT * FROM t WHERE non_indexed_col = 'x' FOR UPDATE;\n```\n\nWHERE 列没索引时 InnoDB 会**对扫描过的所有索引项加锁**——走全表扫描就等于锁全表。EXPLAIN 看到 `type=ALL` 配上 `FOR UPDATE` 基本就是事故现场。\n\n确保 WHERE 列有索引，且 EXPLAIN `type` 不是 `ALL`，加锁才能精准。\n\n## REPEATABLE READ 下 next-key lock 把 gap 也锁了\n\nMySQL 默认隔离级别 REPEATABLE READ，InnoDB 此时用 **next-key lock = record lock + gap lock**：\n\n```sql\n-- 表里现有 id: 1, 5, 10\nSELECT * FROM t WHERE id BETWEEN 3 AND 8 FOR UPDATE;\n-- 锁 id=5（record lock）+ (1,5)、(5,10) 区间（gap lock）\n-- 另一事务 INSERT id=4 / id=7 全部阻塞\n```\n\n更反直觉的是**没匹配上的查询也加 gap 锁**：\n\n```sql\n-- 表里现有 id: 1, 5, 10\nSELECT * FROM t WHERE id = 7 FOR UPDATE;\n-- 没行可锁，但锁住 (5,10) 区间，INSERT id=6/7/8/9 全部阻塞\n```\n\n唯一索引 + 等值匹配**且匹配到**——只加 record lock 不加 gap lock。这是少数能精准加锁的场景。\n\n## READ COMMITTED 关掉 gap 锁\n\n会话或事务级改成 RC，**gap lock 全部消失，只剩 record lock**：\n\n```sql\nSET SESSION TRANSACTION ISOLATION LEVEL READ COMMITTED;\n```\n\n代价是放弃可重复读、接受 phantom row。在高并发 OLTP 场景里去 gap lock 能显著降低死锁率，很多 ORM 默认推荐 RC。\n\n## FOR SHARE 在 8.0+ 多的 NOWAIT / SKIP LOCKED\n\n默认拿不到锁会等 `innodb_lock_wait_timeout`（50s），8.0+ 加了两个修饰符——`LOCK IN SHARE MODE` 不支持，`FOR SHARE` 才有：\n\n```sql\nSELECT * FROM t WHERE id = 5 FOR UPDATE NOWAIT;\n-- 拿不到立刻报 ER_LOCK_NOWAIT，不等\n\nSELECT * FROM t WHERE status = 'pending' ORDER BY id FOR UPDATE SKIP LOCKED LIMIT 1;\n-- 跳过被锁的行\n```\n\n`SKIP LOCKED` 是写消息队列 / worker 抢任务的标准模式——多个 worker 同时跑这条 SQL，自动每人拿到不同任务，零竞争零冲突。比 `FOR UPDATE` + 应用层重试简单一个数量级。"},{"title":"MySQL 看优化器决策的三件套","slug":"posts/210129-optimizer-trace","date":"2021-01-29","tags":["mysql"],"body":"想知道 MySQL 内部怎么处理你的 SQL，按\"看的层次\"挑工具：\n\n| 工具                            | 看到什么                                                | 适用场景                               |\n| ------------------------------- | ------------------------------------------------------- | -------------------------------------- |\n| `EXPLAIN`                       | 物理执行计划（join 顺序、用了哪个 index、估算扫描行数） | 日常 SQL 优化第一站                    |\n| `SHOW WARNINGS` after `EXPLAIN` | MySQL 重写 / 改写后的 SQL                               | 联表、视图、子查询场景，看实际跑的版本 |\n| `OPTIMIZER_TRACE`               | 优化器完整决策过程（cost 估算、为什么没选某 index）     | 选 index 反常识时排查                  |\n\n## EXPLAIN + SHOW WARNINGS\n\n```sql\nEXPLAIN SELECT * FROM `order` o JOIN user u ON o.uid = u.id WHERE u.email = 'a@b.com';\nSHOW WARNINGS;\n```\n\n`SHOW WARNINGS` 里 `Note 1003` 行就是 MySQL 改写后的 SQL——优化器可能调换 join 顺序、把外连接转内连接、把子查询折成 join。看了重写版才知道**实际跑的是什么**，原 SQL 已经不重要了。\n\n## optimizer_trace\n\n```sql\nSET optimizer_trace = 'enabled=on';\nSELECT ... ;                                         -- 你要分析的 SQL\nSELECT * FROM information_schema.OPTIMIZER_TRACE;\nSET optimizer_trace = 'enabled=off';\n```\n\n返回的 `TRACE` 列是一大坨 JSON，覆盖三个阶段：\n\n- **`join_preparation`** — SQL 解析、视图展开、子查询改写\n- **`join_optimization`** — 候选 index、各 index 的估算 cost、最终选谁\n- **`join_execution`** — 执行细节\n\n最常看 `join_optimization`，里面 `range_analysis` / `chosen_range_access_summary` 直接告诉你\"为什么没选 `idx_email`\"——通常是 `rows_estimation` 估算 cost 比全表扫还高，或者 join 顺序变了导致 index 不可用。\n\ntrace 是 session 级别，默认只保留最近一次，且开启后所有 query 都会被 trace（有性能开销），用完立刻关。"},{"title":"MySQL VARCHAR `=` 忽略尾部空格（PAD SPACE）","slug":"posts/201128-varchar-trailing-space","date":"2020-11-28","updated":"2026-05-20","tags":["mysql"],"body":"`WHERE uid = 'abc'` 命中 `'abc '`（带尾部空格）的行，UNIQUE 索引把 `'abc'` 和 `'abc '` 视为重复 —— 这是 collation 的 _PAD SPACE_ 属性决定的：比较时两侧尾部空格当作不存在。**前导空格保留，尾部空格忽略**，是 SQL 标准行为。\n\n```sql\nCREATE TABLE t (\n  uid VARCHAR(10) COLLATE utf8mb4_unicode_ci PRIMARY KEY\n);\nINSERT INTO t VALUES ('abc '), (' abc');\n\nSELECT * FROM t WHERE uid = 'abc';     -- 命中 'abc '\nSELECT * FROM t WHERE uid = ' abc';    -- 命中 ' abc'\nINSERT INTO t VALUES ('abc');           -- ERROR 1062 Duplicate\nINSERT INTO t VALUES ('abc   ');        -- ERROR 1062 Duplicate\n```\n\n## 8.0 默认 collation 已经改为 NO PAD\n\n跨版本最容易翻车的点：\n\n| collation            | PAD 属性   | 默认版本   |\n| -------------------- | ---------- | ---------- |\n| `utf8mb4_general_ci` | PAD SPACE  | 5.7        |\n| `utf8mb4_unicode_ci` | PAD SPACE  | (从未默认) |\n| `utf8mb4_0900_ai_ci` | **NO PAD** | **8.0**    |\n| `utf8mb4_bin`        | NO PAD     | —          |\n\n**8.0 默认建表的 `=` 比较尾部空格敏感**，上面这段 SQL 用 8.0 默认 collation 不会产生 duplicate —— 和 5.7 完全反着来。查当前默认的 PAD 属性：\n\n```sql\nSHOW COLLATION WHERE Charset = 'utf8mb4' AND `Default` = 'Yes';\n-- 看 Pad_attribute 列\n```\n\n## 三条解决路径\n\n**根治** —— 换成 NO PAD collation：\n\n```sql\nALTER TABLE t CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;\n```\n\n迁移前先 `SELECT * FROM t WHERE uid <> TRIM(uid)` 扫一遍尾部带空格的脏数据。\n\n**临时绕** —— 单次查询加 `COLLATE` 子句：\n\n```sql\nSELECT * FROM t WHERE uid = 'abc' COLLATE utf8mb4_bin;\n```\n\n比老写法 `WHERE uid = BINARY 'abc'` 现代，保留字符集语义。\n\n**改用 `LIKE`** —— `LIKE` 是逐字符模式匹配，**不走 collation 的 PAD 规则**：\n\n```sql\nSELECT * FROM t WHERE uid LIKE 'abc';    -- 0 行\nSELECT * FROM t WHERE uid LIKE 'abc ';   -- 命中 'abc '\n```\n\n`=` 和 `LIKE` 在尾部空格上的语义差异根因在此。\n\n## 经典攻击：SQL Constraint Truncation\n\n非严格 mode + PAD SPACE 联手能绕过登录鉴权：注册超长用户名 `'admin     1'`（11 字符，超出 `VARCHAR(10)`）→ 数据库截断成 `'admin     '` → 登录时 PAD SPACE 让 `'admin' = 'admin     '` 等值，匹配到攻击者那行，应用层一看 `username = admin` 就当真 admin 处理 → 越权。\n\n```sql\nSET sql_mode = '';   -- 必须关掉严格模式才能复现\n\nCREATE TABLE users (\n  username VARCHAR(10) COLLATE utf8mb4_unicode_ci PRIMARY KEY,\n  password VARCHAR(64)\n);\nINSERT INTO users VALUES ('admin', 'real_pwd');\nINSERT INTO users VALUES ('admin     1', 'pwn');  -- 实际写入 'admin     '\n\nSELECT * FROM users WHERE username = 'admin' AND password = 'pwn';\n-- 命中攻击者那行：(admin     , pwn)\n```\n\n5.7+ 默认 `sql_mode` 含 `STRICT_TRANS_TABLES`（超长直接报错）、8.0 默认 NO PAD（等值不成立），两道默认都封死了这条路径 —— 主要是老库审计时查 `sql_mode` 和表 collation。\n\n## CHAR 是另一套机制\n\n`CHAR(N)` 存储时空格补齐到 N 位，读取时剥掉尾部空格（除非 SQL mode 打开 `PAD_CHAR_TO_FULL_LENGTH`）。CHAR 上「找不到尾部空格」是**存储层**的事，VARCHAR 上是 **collation 层**的事 —— 同样的症状，两套机制，两套修法。\n\n参考 [CHAR/VARCHAR 官方文档 trailing-space 章节](https://dev.mysql.com/doc/refman/8.0/en/char.html)、[Collation Pad Attributes](https://dev.mysql.com/doc/refman/8.0/en/charset-binary-collations.html#charset-binary-collations-trailing-space)。"},{"title":"脱离 Laravel 跑 illuminate/validation","slug":"posts/200911-illuminate-validation-standalone","date":"2020-09-11","tags":["laravel"],"body":"[illuminate 组件](https://github.com/illuminate)都是独立 composer 包，`illuminate/validation` 在非 Laravel 项目里也能跑——手动把 `FileLoader → Translator → Factory` 三件套接起来即可。\n\n## 安装\n\n```bash\ncomposer require illuminate/validation illuminate/translation illuminate/filesystem\n```\n\n> `illuminate/filesystem` 不是 `illuminate/validation` 的依赖，用 `FileLoader` 必须单独装。\n\n## 准备语言文件\n\n从 [Laravel-Lang/lang](https://github.com/Laravel-Lang/lang) 复制要用的语言文件到本地，例如 `lang/zh-CN/validation.php`。\n\n## 装配\n\n```php\nuse Illuminate\\Filesystem\\Filesystem;\nuse Illuminate\\Translation\\FileLoader;\nuse Illuminate\\Translation\\Translator;\nuse Illuminate\\Validation\\Factory;\n\n$loader = new FileLoader(new Filesystem(), __DIR__ . '/../lang');\n$translator = new Translator($loader, 'zh-CN');\n$validator = new Factory($translator);\n```\n\n## 用\n\n```php\n$v = $validator->make(\n    ['name' => ''],\n    ['name' => 'required|string|min:2'],\n);\n\nif ($v->fails()) {\n    print_r($v->errors()->all());\n}\n```\n\n完整规则参考 Laravel 文档的 [Manually Creating Validators](https://laravel.com/docs/validation#manually-creating-validators)。"},{"title":"Redis 按 pattern 批量删除 key","slug":"posts/200817-redis-delete-keys-by-pattern","date":"2020-08-17","tags":["redis"],"body":"`DEL` 不接受 pattern，要按前缀清一批 key 得绕一下。最常见是 `EVAL` 里用 `KEYS` 拿全集再分块 `DEL`：\n\n```bash\nredis-cli EVAL \"local keys = redis.call('keys', ARGV[1])\nfor i=1,#keys,5000 do\n  redis.call('del', unpack(keys, i, math.min(i+4999, #keys)))\nend\nreturn #keys\" 0 'prefix:*'\n```\n\n分块是因为 Lua 的 `unpack` 在几千个参数后会撞 stack 上限，单次 `DEL unpack(keys)` 在大集合下会直接报错。\n\n但 `KEYS` 本身是 O(N) 全表扫，**会阻塞 Redis 主线程**直到扫完整个 keyspace——线上库千万别这么干。生产环境用客户端侧 `SCAN`，游标式增量取，单次 `DEL` 控量：\n\n```bash\nredis-cli --scan --pattern 'prefix:*' | xargs -L 1000 redis-cli DEL\n```\n\n注意 `KEYS` / `SCAN` 的 pattern 是 glob（`*` `?` `[abc]`），不是 regex。\n\n[Stack Overflow 原帖](https://stackoverflow.com/questions/4006324/how-to-atomically-delete-keys-matching-a-pattern-using-redis)"},{"title":"MySQL JSON 列建索引","slug":"posts/200814-mysql-json-indexing","date":"2020-08-14","tags":["mysql"],"body":"MySQL 5.7+ 支持原生 [`JSON` 数据类型](https://dev.mysql.com/doc/refman/8.0/en/json.html)。两个坑：\n\n- `JSON` 列**不能有 non-NULL 默认值**——要么 nullable，要么应用层填\n- **不能直接在 JSON 字段上建索引**——要靠 [Generated Column](https://dev.mysql.com/doc/refman/8.0/en/create-table-generated-columns.html) 包一层\n\n## 用 Generated Column 索引 JSON 字段\n\n把要查的 JSON 路径抽成一个虚拟列，再在虚拟列上建索引：\n\n```sql\nCREATE TABLE players (\n  id               INT UNSIGNED NOT NULL,\n  player_and_games JSON NOT NULL,\n  name_virtual     VARCHAR(20) GENERATED ALWAYS AS (player_and_games ->> '$.name') NOT NULL,\n  PRIMARY KEY (id),\n  KEY idx_name (name_virtual)\n);\n```\n\n`->>` 是 `->` + `JSON_UNQUOTE` 的简写——直接拿字符串而不是 quoted JSON value。\n\nVirtual 列只存元数据不落盘（默认），Stored 列落盘到磁盘。索引 JSON 字段几乎都用 Virtual，省空间且没性能差。\n\n## 验证索引命中\n\n```sql\nEXPLAIN SELECT * FROM players WHERE name_virtual = 'Sally';\n-- key: idx_name\n```\n\n参考 [MySQL 5.7 新特性 JSON](https://www.lnmp.cn/mysql-57-new-features-json.html) · [如何索引 JSON 字段 | mysql.taobao.org](http://mysql.taobao.org/monthly/2017/12/09/) · [JSON 函数参考](https://dev.mysql.com/doc/refman/8.0/en/json-function-reference.html)"},{"title":"Composer vendor 提交进 git 的 submodule 陷阱","slug":"posts/200810-composer-vendor-git-submodule-trap","date":"2020-08-10","updated":"2026-05-20","tags":["php"],"body":"Composer 官方 FAQ 答案是**别 commit `vendor/`**：仓库膨胀、依赖历史在你 repo 里重演一遍、`vendor/<pkg>/.git` 会被 git 当 submodule。最佳实践是 CI / 部署脚本里跑 `composer install`。\n\n但有些场景必须把 `vendor/` 进 git —— 离线 / 无 composer 的生产机、内网部署、国内网络拉国外包不稳 —— 这时 submodule 这条会静默出事：git 不报错，部署后才发现包是空的。\n\n## 静默踩坑\n\n`composer install` 在两种情况下会留下 `vendor/<pkg>/.git`：\n\n- 包没发 dist tarball，只能从 git 拉 source\n- 显式 `--prefer-source`，或 `composer.json` 配了 `\"preferred-install\": \"source\"`\n\n`git add vendor/` 看到子目录里有 `.git`，不会把里面的代码加进来，**而是记一条 gitlink**。git index 里每个条目都带一个类 Unix 八进制 mode —— `100644` 普通文件、`100755` 可执行、`120000` 符号链接、**`160000` gitlink**（指向另一个 git 仓库的某个 commit，即 submodule 引用）。`git add` 撞到带 `.git` 的子目录就自动选 `160000`，记一条 `<sha> vendor/foo`，里面文件一行不进。\n\n`git commit` 通过，`git push` 通过，部署机 `git pull` 拉出来 —— 那个包目录是空的，运行时 `class not found`。报错栈帧跟 composer 无关，定位常要绕半天。\n\n两个诊断信号：\n\n```bash\ngit ls-files -s | grep 160000\n# 160000 d4e5f6... 0 vendor/foo\n#  ^ mode               ^ 被当 submodule 的路径\n\ngit diff --cached vendor/foo\n# new file mode 160000\n# Subproject commit d4e5f6...\n#                   ^ 只 commit 引用，没有具体文件 diff\n```\n\n## 正确姿势\n\n要 commit `vendor/` 就显式走 dist：\n\n```bash\ncomposer install --prefer-dist\n```\n\n或写进 `composer.json`，所有人共享：\n\n```json\n{\n  \"config\": {\n    \"preferred-install\": \"dist\"\n  }\n}\n```\n\n再加一条兜底 `.gitignore`，万一某个包没 dist 又被 fallback 到 source 也不会变 submodule：\n\n```gitignore\n/vendor/**/.git\n```\n\n## 已经踩了\n\nvendor 已经以 gitlink 形式 commit 进去了，清掉重 add：\n\n```bash\ngit rm -r --cached vendor\necho '/vendor/**/.git' >> .gitignore\ngit add vendor .gitignore\ngit commit -m \"track vendor as files, not submodules\"\n```\n\n`--cached` 只动 index 不删工作区文件，关键参数 —— 丢了会把磁盘上的 vendor 一起删掉。\n\nSee [Composer FAQ: Should I commit the dependencies in my vendor directory?](https://getcomposer.org/doc/faqs/should-i-commit-the-dependencies-in-my-vendor-directory.md)."},{"title":"TCP 三次握手 / 四次挥手","slug":"posts/200629-tcp-handshake","date":"2020-06-29","tags":["network","tcp"],"body":"客户端 A、服务器 B，初始序号 `seq`，确认号 `ack`。\n\n## 三次握手（建立连接）\n\n初始：B 监听，A 打开。\n\n- A → B：`seq = x`（A 发连接请求，进入 SYN-SENT）\n- B → A：`ack = x+1, seq = y`（B 确认，进入 SYN-RCVD）\n- A → B：`ack = y+1`（A 再确认，进入 ESTABLISHED）\n\nB 收到 A 的确认后也进入 ESTABLISHED。\n\n**为什么三次？** 防止失效的连接请求突然传到服务器，让服务器误以为要建连接。\n\n## 四次挥手（释放连接）\n\n- A → B：`seq = u`（A 发释放报文，进入 FIN-WAIT-1）\n- B → A：`ack = u+1, seq = v`（B 确认，半关闭，B 还能发数据，进入 CLOSE-WAIT）\n- B → A：`ack = u+1, seq = w`（B 重发确认号，进入 LAST-ACK）\n- A → B：`ack = w+1, seq = u+1`（A 确认，进入 TIME-WAIT）\n\n经过 `2MSL` 后 A 才进入 CLOSED。\n\n**为什么 TIME-WAIT 要等 2MSL？**\n\n- 保证 A 发的最后一个 ACK 能到达 B（否则 B 重传 FIN）\n- 让本次连接的失效报文在网络中消失，不污染下一次连接\n\n## 抓包验证\n\n```bash\nsudo tcpdump -i en0 host www.qq.com and tcp -S -c 50\n```\n\n![tcpdump 抓包分析握手过程](https://user-images.githubusercontent.com/9289792/87918568-3377e280-caa9-11ea-831a-95000e308ad8.png)"},{"title":"tcpdump 速查","slug":"posts/200629-tcpdump-cheatsheet","date":"2020-06-29","tags":["network"],"body":"抓 HTTP 流量最常用的一行：\n\n```bash\nsudo tcpdump host httpforever.com and tcp port 80 -A -nn\n```\n\n完整文档 `man tcpdump`，下面只列高频项。\n\n## 常用参数\n\n| 参数              | 作用                                                     |\n| ----------------- | -------------------------------------------------------- |\n| `-i <iface>`      | 指定接口（不指定取第一个可用；`-i any` 监听所有）        |\n| `-nn`             | 不解析主机名和端口名                                     |\n| `-v / -vv / -vvv` | 详细级别                                                 |\n| `-c <num>`        | 捕获指定数量后退出                                       |\n| `-s <len>`        | 抓取长度（0 = 全包；新版默认 262144 bytes）              |\n| `-w <file>`       | 写入 pcap 文件                                           |\n| `-r <file>`       | 读取 pcap 文件离线解析                                   |\n| `-A`              | 以 ASCII 打印 payload（截 HTTP：`tcpdump -nSA port 80`） |\n| `-X`              | 十六进制 + ASCII 打印（不要和 `-A` 一起用）              |\n| `-S`              | 显示绝对序列号而不是相对编号                             |\n| `-tttt`           | 打印完整日期                                             |\n| `-e`              | 显示以太网头（MAC 地址）                                 |\n\n## BPF 过滤语法\n\n```text\n[proto] [src|dst] [host|net|port] <对象> [逻辑运算]\n\ntcp and dst port 80 and src net 10.0.0.0/8\n```\n\n- 协议：`ip` `tcp` `udp` `icmp` `arp` `vlan` `ip6` ...\n- 方向：`src` `dst` `src or dst` `src and dst`\n- 对象：`host 10.0.0.1` / `net 192.168.0.0/16` / `port 443`\n- 逻辑：`and` (`&&`) / `or` (`||`) / `not` (`!`) / `( )`\n\n```bash\nsudo tcpdump -i en0 host 192.168.50.1          # 源或目的\nsudo tcpdump -i en0 src host 192.168.50.1      # 源\nsudo tcpdump -i en0 dst host 192.168.50.1      # 目的\nsudo tcpdump -i en0 port 8080                  # 端口\nsudo tcpdump -i en0 net 192.168                # 网段\nsudo tcpdump -i en0 tcp                        # 协议\n```\n\n## 输出格式\n\n```text\n14:24:12.196582 IP 192.168.91.29.56930 > 146.190.62.39.http: Flags [SEW], seq 193726297, win 65535, options [...], length 0\n```\n\n时间戳 / 协议层 / `源.端口 > 目标.端口` / TCP 标志位 / `seq` `ack` `win` 等字段 / 报文长度。\n\n## TCP Flags\n\n| TCP Flag | 缩写 | 含义                 |\n| -------- | ---- | -------------------- |\n| SYN      | S    | 会话建立请求         |\n| ACK      | A    | 确认发送方的数据     |\n| FIN      | F    | 终止标识             |\n| RESET    | R    | 立即中止连接         |\n| PUSH     | P    | 立即推送数据         |\n| URGENT   | U    | 优先于其他数据       |\n| NONE     | `.`  | 占位符，通常用于 ACK |\n\n参考 [tcpdump Flags](https://amits-notes.readthedocs.io/en/latest/networking/tcpdump.html#id2)。\n\n## 接口列表\n\n```bash\nsudo tcpdump -D\n# 1.en0 [Up, Running]\n# 2.p2p0 [Up, Running]\n# ...\n```\n\nmacOS 下监听适配器必须 root。\n\n## 实例\n\n抓 eth1 上目的地址是 192.168.1.254 或 192.168.1.200、端口 80 的 TCP：\n\n```bash\nsudo tcpdump -i eth1 '((tcp) and (port 80) and ((dst host 192.168.1.254) or (dst host 192.168.1.200)))'\n```\n\n只抓 SYN：\n\n```bash\nsudo tcpdump -i eth1 'tcp[tcpflags] = tcp-syn'\n```\n\n抓 SYN + ACK：\n\n```bash\nsudo tcpdump -i eth1 'tcp[tcpflags] & tcp-syn != 0 and tcp[tcpflags] & tcp-ack != 0'\n```\n\n抓 DNS 请求：\n\n```bash\nsudo tcpdump -i en0 udp dst port 53\n```\n\n流量大时用 `-c` 限制数量：\n\n```bash\nsudo time tcpdump -nn -i en0 'tcp[tcpflags] = tcp-syn' -c 10000 > /dev/null\n```\n\n实时抓 8000 端口的 GET 包写入文件：\n\n```bash\nsudo tcpdump -i eth0 '((port 8000) and (tcp[(tcp[12]>>2):4]=0x47455420))' -nnAl -w /tmp/GET.log\n```"},{"title":"PhpStorm + Xdebug 容器联调，3 个名字必须对齐","slug":"posts/200526-phpstorm-xdebug-name-match","date":"2020-05-26","updated":"2026-05-20","tags":["php","phpstorm","xdebug"],"body":"PhpStorm 容器化 Xdebug 跑不通，最常见的根因不是端口、不是网络，而是 3 个魔法字符串没对齐 —— 错位时 IDE 静默忽略，没有任何报错。挨个对完，剩下的就是常规网络问题。\n\n## 前置：mode + 触发方式\n\nXdebug 3 默认完全 disabled，要先开 mode；然后选 _trigger_ 还是 _always_：\n\n```ini\nxdebug.mode=debug\nxdebug.start_with_request=trigger    ; 推荐 dev 默认\n```\n\n- `trigger`：只有请求带 `XDEBUG_SESSION` cookie / GET / POST / env 时才挂 debug；其他请求零开销。HTTP 场景永远选这个\n- `yes`：所有请求都进 debug session。每个请求都要给 IDE 发 connect，开发体感慢；只在 CLI 一次性脚本场景用（见最后一节）\n\n## 1. `serverName` ↔ Servers > Name\n\n容器内 PHP 通过环境变量告诉 Xdebug \"我是哪个项目\"：\n\n```bash\nPHP_IDE_CONFIG=serverName=laradock\n```\n\n`Preferences > PHP > Servers` 里那条 Server 的 **Name** 字段必须**完全等于** `serverName=` 的值（这里是 `laradock`）。匹配上之后 PhpStorm 才会用这条 Server 上配的 _path mapping_ 把容器里的 `/var/www/...` 翻译回宿主项目根，断点才落在正确文件上。\n\n**失败模式**：debug session 建立成功（右下角图标变绿、Variables 面板出现），但断点永远不停 —— path mapping 找不到对应 Server，PhpStorm 不知道容器路径对应宿主哪个文件。\n\n## 2. `xdebug.idekey` ↔ `XDEBUG_SESSION` value ↔ Run Configuration IDE key\n\n三处的 IDE key 必须一致，默认值约定俗成是 `PHPSTORM`：\n\n```ini\nxdebug.idekey=PHPSTORM\n```\n\n- 浏览器：Xdebug helper 扩展给请求加 `Cookie: XDEBUG_SESSION=PHPSTORM`\n- CLI：`XDEBUG_SESSION=PHPSTORM php some-script.php`\n- PhpStorm：`Run > Edit Configurations > PHP Remote Debug` 里 IDE key 填 `PHPSTORM`\n\ncookie / env 的 **value** 同时承担两个角色 —— 触发 `start_with_request=trigger` 模式启动 session，且把 value 当作本次 session 的 IDE key 上报给 IDE。\n\n**失败模式**：连接建立、断点也命中，但 PhpStorm 把 session 路由给错的 Run Configuration —— 看不到该 config 里配的环境变量、看不到自定义 evaluator。多人共享一台 Xdebug proxy 时尤其容易撞。\n\n## 3. `xdebug.client_host` ↔ 容器回连宿主的地址\n\nXdebug 是 _reverse connection_：容器里的 PHP 主动回连 IDE，不是 IDE 拨进容器。所以 `xdebug.client_host` 必须能从容器解析到宿主 IP：\n\n```ini\nxdebug.client_host=host.docker.internal   ; Mac / Win / Linux 20.10+\nxdebug.client_port=9003\n```\n\n跨平台统一写法（`dockerhost` 别名、`host-gateway` magic）。\n\n**失败模式**：容器里 `tail -f` Xdebug 日志能看到 `Could not connect to client. Tried: host.docker.internal:9003` —— 这条是少数不静默的失败，因为 Xdebug 自己在抱怨连不上。\n\n## CLI / Artisan 用 `-d` 覆盖\n\nPHP CLI 走的不是 fpm，加载的是 `/etc/php/X.Y/cli/conf.d/*.ini` 而不是 fpm 的 ini。fpm 那边配好 Xdebug、CLI 仍然没动静是常见盲区。两种解法：\n\n- 把同样的 `xdebug.*` 配到 `cli/conf.d/20-xdebug.ini`，CLI 全局开（开发机 OK，生产别这么干）\n- 用 `php -d` 临时覆盖，只在需要时调试某条命令：\n\n```bash\nalias artisandebug='php \\\n  -dxdebug.mode=debug \\\n  -dxdebug.client_host=host.docker.internal \\\n  -dxdebug.client_port=9003 \\\n  -dxdebug.start_with_request=yes \\\n  artisan'\n```\n\nCLI 场景用 `start_with_request=yes` 是对的 —— 一次性进程，没机会注入 trigger，进来就要挂 debug。然后 `artisandebug migrate:status` 跑起来就能停断点。\n\n## 排查顺序\n\n跑不通时按这个顺序查，每一步都是上一步的前提：\n\n1. 容器里 `php -i | grep xdebug` 确认 `mode=debug`、`idekey`、`client_host` 都加载（CLI 调试要 `php-cli` 而不是 fpm 的输出）\n2. PhpStorm 右下角点过 _Start Listening for PHP Debug Connections_（电话图标变绿）\n3. 三对名字（serverName / idekey / client_host）逐一对齐\n4. Server 里 path mapping 设了容器 ↔ 宿主路径\n\n排到 4 还不行，看 `Help > Show Log in Finder` 的 `idea.log`，搜 `xdebug` —— 99% 能直接定位到具体 mismatch。"},{"title":"CORS：带凭证跨域时 Allow-Origin 不能用 *","slug":"posts/200509-cors-wildcard-origin-credentials","date":"2020-05-09","tags":["network"],"body":"带 cookie 的跨域请求（`fetch(..., { credentials: 'include' })`、`xhr.withCredentials = true`）触发浏览器的「凭证模式」，硬性四条死规矩：\n\n1. `Access-Control-Allow-Origin` **必须是具体 origin**，禁止 `*`\n2. 必须返回 `Access-Control-Allow-Credentials: true`\n3. 必须加 `Vary: Origin` 防缓存错乱\n4. 预检 `OPTIONS` 也要重复以上三条\n\nFirefox 报：\n\n> Credential is not supported if the CORS header 'Access-Control-Allow-Origin' is '\\*'\n\nChrome 报：\n\n> The value of the 'Access-Control-Allow-Origin' header in the response must not be the wildcard '\\*' when the request's credentials mode is 'include'\n\n## 为什么禁用 `*` + 凭证\n\n如果允许 `*` 同时带 cookie：攻击者站点 `https://hack.com` 直接请求 `https://api.example.com`，**自动带上你已登录的 cookie**，服务端无法区分调用方，敏感数据全开。规范因此硬性禁止——这是浏览器侧的强制 CSRF 防线。\n\n`Access-Control-Allow-Credentials: true` 是服务端的**显式授权开关**，控制两件事：跨域请求**能不能带凭证发出**、跨域响应**能不能把 `Set-Cookie` 写入当前域**。前端 `credentials:include` + 服务端 `Allow-Credentials: true` 是**双向确认**，少一边浏览器都不携带凭证、也不写入 cookie。\n\n## 服务端正确返回\n\n读取请求头 `Origin`，比对白名单，**动态回写**当前 origin（不是写死一个）：\n\n```http\nAccess-Control-Allow-Origin: https://app.example.com\nAccess-Control-Allow-Credentials: true\nVary: Origin\n```\n\nNginx 关键片段：\n\n```nginx\nif ($http_origin ~* ^https://(app|admin)\\.example\\.com$) {\n    add_header Access-Control-Allow-Origin $http_origin always;\n    add_header Access-Control-Allow-Credentials true always;\n    add_header Vary Origin always;\n}\n```\n\n## `Vary: Origin` 不可省\n\nCDN / 反代按 URL 缓存响应。第一次 `https://a.com` 请求命中后，缓存里那条响应携带 `Access-Control-Allow-Origin: https://a.com`。下一次 `https://b.com` 请求同一 URL，缓存直接返回——浏览器拿到不匹配的 `Allow-Origin`，跨域失败。\n\n`Vary: Origin` 告诉缓存层：把 `Origin` 也算进 cache key，不同 origin 各存各的。预检请求建议扩展为：\n\n```http\nVary: Origin, Access-Control-Request-Method, Access-Control-Request-Headers\n```\n\n## 常见踩坑\n\n- 配了具体 Origin，忘了 `Allow-Credentials: true` → 能过 CORS，但 cookie 发不上、`Set-Cookie` 也存不下\n- 多域名白名单写死了一个 origin → 其余域名全跨域失败，必须动态回传当前请求的 `Origin`\n- 同一响应出现多个 `Access-Control-Allow-Origin`（框架 + Nginx 重复添加）→ 浏览器报错\n- 本地开发 `*` 一切正常，上线开 credentials 立刻崩\n- 预检 `OPTIONS` 偷懒返回 `*` → 主请求过不了\n\n参考：[MDN: CORS — Requests with credentials](https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS#requests_with_credentials)"},{"title":"vim 终端粘贴自动缩进错乱用 :set paste","slug":"posts/200428-vim-paste-indent","date":"2020-04-28","tags":["vim"],"body":"从浏览器 / 别的终端 app 粘贴一段已缩进的代码到终端 vim，每行缩进会层层叠加变成阶梯——因为终端 vim 收不到\"这是粘贴\"的信号，把每个字符当成键盘输入处理，于是每按一次回车就按上一行结尾的缩进起步。\n\n粘贴前后切回来：\n\n```vim\n:set paste\n\" 粘贴...\n:set nopaste\n```\n\n或者一行 toggle：`:set invpaste`。\n\nGUI vim（gvim / MacVim）不需要——它能直接区分粘贴事件和键盘输入。Neovim 在多数现代终端也能自动识别 bracketed paste，可以不管。\n\n[stackoverflow](https://stackoverflow.com/questions/2514445/turning-off-auto-indent-when-pasting-text-into-vim/38258720#38258720)"},{"title":"vim 全选 / 全复制 / 全删全文","slug":"posts/200428-vim-select-all","date":"2020-04-28","tags":["vim"],"body":"`gg` 跳首行、`G` 跳末行，前后一夹就是\"全文\"。组合公式 `gg + 动作 + G`：\n\n```vim\nggvG   \" 字符级全选（高亮）\nggVG   \" 行级全选（整行高亮）\nggyG   \" 全文复制到 0 号寄存器\nggdG   \" 全文删除（等价于 dG，因为光标已在首行）\n```\n\n记住套路本身比记单条命令值——同样的夹击思想能拼出\"从光标到末行替换\"`c`、\"格式化全文\" `gg=G` 等任意操作。"},{"title":"PHP GD 文字对齐：imagettfbbox 8 元素 + 坐标系是相对 baseline","slug":"posts/200416-php-gd-text-bbox-alignment","date":"2020-04-16","tags":["php"],"body":"GD 写文字最容易踩的坑：`imagettftext($img, $size, $angle, $x, $y, ...)` 里的 `$y` **不是文字顶部、也不是中线**，而是 _baseline_——拉丁字母 `Hxg` 中 `H` 的脚底。所以 `imagettftext($img, 12, 0, 0, 0, ...)` 写出来的字会**完全跑到画布上方**消失。\n\n`imagettfbbox` 返回的也是 baseline 坐标系下的 8 元素：\n\n```text\n索引   含义        典型值（angle=0）\n[0]   左下 x        0\n[1]   左下 y       -1     ← baseline 之下 1 px\n[2]   右下 x      198\n[3]   右下 y       -1\n[4]   右上 x      198\n[5]   右上 y      -20    ← baseline 之上 20 px（负值在上）\n[6]   左上 x        0\n[7]   左上 y      -20\n```\n\n**y 轴在 baseline 下为正、之上为负**——和 GD 全局图像坐标（左上角原点、向下增长）相反。这是混淆的根源。\n\n## 实用算法\n\n文字宽度：`$bbox[2] - $bbox[0]`；高度：`$bbox[1] - $bbox[7]`。\n\n**右对齐到 `$rightX`**：\n\n```php\n$bbox = imagettfbbox($size, 0, $font, $text);\n$x = $rightX - ($bbox[2] - $bbox[0]);\nimagettftext($img, $size, 0, $x, $y, $color, $font, $text);\n```\n\n**水平居中到 `$centerX`**：\n\n```php\n$x = $centerX - intval(($bbox[2] - $bbox[0]) / 2) - $bbox[0];\n```\n\n减 `$bbox[0]` 是因为有些字体首字符左缘会突出到 x=0 之前（`$bbox[0]` 为负），不补回来会左偏。\n\n**垂直贴到顶边 `$topY`**（让文字最高点对齐 `$topY`）：\n\n```php\n$y = $topY - $bbox[7];   // bbox[7] 是负的，等价 topY + |bbox[7]|\n```\n\n## 隐藏坑\n\n- **descender 不算 bounding box**：g/p/q/y 的下伸尾巴在 bbox 外，多行排版按 bbox 算行高会让下行字头部蹭到上行尾巴。靠 `$size * 1.2` 这种经验系数加行距更稳\n- **`$angle` 旋转的是文字，不是 bbox 坐标系含义**——bbox 永远按\"如果这段文字是水平正向的\"来给点，旋转后想知道屏幕坐标得自己用 `$angle` 套旋转矩阵\n- **PHP 8.0 起 `imagettfbbox` / `imagettftext` 是 `imageftbbox` / `imagefttext` 的别名**——行为一致，新代码两套都行\n\n字体文件路径必须是真实可读的 `.ttf` / `.otf`，相对路径默认相对 CWD 不是 script 目录——线上跑用 `__DIR__ . '/font.ttf'` 兜底。"},{"title":"Composer `--ignore-platform-reqs` 是安静的 bug 触发器","slug":"posts/200415-composer-ignore-platform-reqs-pitfall","date":"2020-04-15","tags":["php"],"body":"经典踩坑：本地 PHP 7.1，要装的包声明需要 PHP 7.4，`composer require` 直接拒：\n\n```text\nYour requirements could not be resolved to an installable set of packages.\n- hellogerard/jobby requires php >=7.4\n```\n\nGoogle 第一条答案是加 `--ignore-platform-reqs`：\n\n```bash\ncomposer require hellogerard/jobby --ignore-platform-reqs\n```\n\n报错没了，包装上了，看起来一切正常 —— 直到运行时撞上 `PHP Fatal error: Cannot use null coalescing assignment operator (??=)` 之类，因为这是 PHP 7.4 才有的语法。\n\n`--ignore-platform-reqs` 字面就是\"不检查平台依赖\"，它把 `composer.json` 里 `\"require\": { \"php\": \">=7.4\" }` 这条约束跳过去 —— composer 不知道这个包用了 7.4 才有的 syntax / API，硬装下去，运行才炸。**安装时无错、运行时无关栈帧、`composer.lock` 还带着错版本 —— 是教科书级的静默 bug**。\n\n正解：用目标 PHP 版本的 binary 跑 composer。Composer 本身是个 PHP 脚本，跟着哪个 PHP 走就按哪个 PHP 检查约束：\n\n```bash\nwhich composer\n# /usr/local/bin/composer\n\n# 用 7.4 解释器跑 composer\n/usr/local/opt/php@7.4/bin/php /usr/local/bin/composer require hellogerard/jobby\n\n# 内存不够时临时关掉 memory_limit\n/usr/local/opt/php@7.4/bin/php -d memory_limit=-1 /usr/local/bin/composer update -vvv\n```\n\n如果之前误用 `--ignore-platform-reqs` 装过，`composer.lock` 已经被污染，要 `composer update --lock` 重生成约束树。"},{"title":"PHP CLI 排查速查：`--ri` / `--re` / `php -a` 常被忽略","slug":"posts/200415-php-cli-introspection","date":"2020-04-15","tags":["php"],"body":"`php` 的 CLI 选项里有几个 flag 大多数人不知道，能省一堆 `phpinfo() | grep`。\n\n**`php --ri <extension>`** —— 看某个扩展的当前有效配置（合并所有 ini 之后的运行时值，不是源 ini 文件内容）：\n\n```bash\nphp --ri gd\nphp --ri opcache\n```\n\n比 `php -i | grep -A 10 gd` 准 —— `--ri` 给的是 PHP 真正认到的值。\n\n**`php --re <extension>`** —— 反射列出扩展提供的所有函数 / 类 / 常量，顺便确认装没装：\n\n```bash\nphp --re decimal\n# 没装时输出：Exception: Extension decimal does not exist\n```\n\n**`php -a`** —— REPL 交互模式，带 tab 补全（函数 / 类 / 常量名），比 `php -r '...'` 拼字符串方便：\n\n```bash\nphp -a\nphp > date('Y-m-d', 1234567890);\n```\n\n需要 PHP build 时带 readline，Homebrew 装的 `php` 默认有。\n\n**`php-fpm -tt`** —— FPM 配置语法检查 + dump 解析后的最终值（双 `t`，单 `t` 只验语法不 dump）：\n\n```bash\nphp-fpm -tt 2>&1 | grep memory_limit\n```\n\n其他常用一行：\n\n```bash\nphp --ini                                                  # ini 文件路径\nphp -i | grep -E \"Loaded Configuration|Scan this dir\"      # ini 加载顺序\nphp -m                                                     # 已加载模块\nphp -r \"echo ini_get('memory_limit');\"                     # 看任意 ini 值\nphp -r \"phpinfo();\" | grep configure                       # 编译参数\nphp -r \"print_r(openssl_get_cert_locations());\"            # openssl cert 路径\n```"},{"title":"MySQL Illegal mix of collations","slug":"posts/200414-illegal-mix-of-collations","date":"2020-04-14","tags":["mysql"],"body":"```bash\nIllegal mix of collations (utf8mb4_general_ci,IMPLICIT) and (utf8mb4_unicode_ci,IMPLICIT) for operation\n```\n\n`utf8mb4_general_ci` 和 `utf8mb4_unicode_ci` 两列直接做 JOIN / 比较时报错——MySQL 不会自动转换 collation。\n\n## 解决\n\n**根治：统一字段 collation**。MySQL 8.0+ 默认 `utf8mb4_0900_ai_ci`（基于 Unicode 9.0），5.7 默认 `utf8mb4_general_ci`，老库迁 8.0 时 collation 不会自动改，要小心遗留表。\n\n**临时绕：SQL 里 `COLLATE` 显式转**：\n\n```sql\nSELECT * FROM a JOIN b ON a.name = b.name COLLATE utf8mb4_unicode_ci;\n```\n\n## 三种 collation\n\n|                      | 排序依据        | 性能         | 用途                     |\n| -------------------- | --------------- | ------------ | ------------------------ |\n| `utf8mb4_general_ci` | 简化 ASCII 规则 | 最快         | 5.7 老库默认，新库不推荐 |\n| `utf8mb4_unicode_ci` | Unicode 4.0     | 略慢         | 5.7 推荐                 |\n| `utf8mb4_0900_ai_ci` | Unicode 9.0     | ≈ unicode_ci | 8.0+ 默认                |\n\n性能差距在绝大多数业务里可忽略，**同库统一**比选哪个更重要。\n\n参考 [Illegal mix of collations | 少年阿斌](https://www.cnblogs.com/wqbin/p/11852376.html)。"},{"title":"按作者统计 git 贡献量","slug":"posts/191203-author-stats","date":"2019-12-03","tags":["git"],"body":"只看 commit 次数（git 内置，0 依赖）：\n\n```bash\ngit shortlog -sn --no-merges\n```\n\n要带行数的版本，用 `--numstat` 给出每文件 `+N -N filename`，再用 `--pretty=format` 把作者名作定界符让 awk 聚合：\n\n```bash\ngit log --numstat --pretty='COMMIT %aN' \\\n  | awk '/^COMMIT / { a = substr($0, 8); next }\n         /^[0-9]/   { ins[a] += $1; del[a] += $2 }\n         END        { for (k in ins) printf \"%s\\t+%d -%d\\n\", k, ins[k], del[k] }'\n```\n\n`%aN` 会走 `.mailmap`（同一人不同邮箱自动合并）。`^[0-9]` 自动跳过 binary 文件（numstat 对 binary 输出 `- - file`）。加 `--author=\"...\"` 限单人；加 `--since=\"2024-01-01\"` 限时段。\n\n**行数易被 reformatting / rename / 生成代码放大，跟 commit 数对照看比单看任一项准。**"},{"title":"CRLF will be replaced by LF","slug":"posts/191122-crlf-warning","date":"2019-11-22","tags":["git"],"body":"```bash\n$ git add .\nwarning: CRLF will be replaced by LF in X.\n```\n\n文件含 CRLF 行尾，git 按当前 `core.autocrlf` / `.gitattributes` 配置正在把它改成 LF。**只是 warning 不是 error，提交不会被拦。**\n\n跨平台团队（Mac/Linux + Windows）想把行尾问题从 PR review 里彻底抹掉，仓库根放一份 `.gitattributes`：\n\n```plaintext\n* text=auto\n*.sh text eol=lf\n*.bat text eol=crlf\n```\n\n老仓库加这份配置后，跑 `git add --renormalize . && git commit` 让已入库的文件按新规则重写一遍——否则规则只对未来的修改生效。\n\n`* text=auto` 一行覆盖 90% 场景。`*.sh text eol=lf` 是经典救场——Windows 同事 checkout 后行尾被换成 CRLF，`bash deploy.sh` 直接报 `bad interpreter`；`*.bat` 是对称情况。完整规则见 [GitHub Docs: Configuring Git to handle line endings](https://docs.github.com/en/get-started/getting-started-with-git/configuring-git-to-handle-line-endings)。"},{"title":"Git commit message 规范：Conventional Commits + 工具链分工","slug":"posts/190604-git-commit-conventional-tools","date":"2019-06-04","tags":["git"],"body":"约定本身就是 [Conventional Commits](https://www.conventionalcommits.org/) 的 Angular 变体：\n\n```text\n<type>(<scope>): <subject>\n\n<body>\n\n<footer>\n```\n\n`type` 限定 7 个：`feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore`。`scope` 标受影响模块（`auth`、`api`、`db`），项目内自定。`subject` 三条硬规则：≤ 50 字符、动词原形开头（`change` 不是 `changed`）、首字母小写、不加句号。\n\n约定的价值不在\"整洁\" —— `type` 是给机器读的：`feat` 触发 minor bump、`fix` 触发 patch、`BREAKING CHANGE` 触发 major，CHANGELOG 也按 type 分组。光人手写不行，工具链分两段 —— 本地\"写 + 验\"、远端\"发布\"。\n\n本地两件套：\n\n| 阶段 | 工具                                                                                                                                                                         | 职责                                                                                                    |\n| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |\n| 写   | [commitizen](https://github.com/commitizen/cz-cli) + [@commitlint/cz-commitlint](https://github.com/conventional-changelog/commitlint/tree/master/@commitlint/cz-commitlint) | `git cz` 交互式生成 message，prompt 直接读 commitlint 规则 —— 不用像 `cz-customizable` 那样维护两份配置 |\n| 验   | [commitlint](https://github.com/conventional-changelog/commitlint) + [husky](https://github.com/typicode/husky)                                                              | git hook 拦截不合规 message —— commitizen 是引导、commitlint 是底线                                     |\n\n装一次：\n\n```bash\nnpm i -D commitizen @commitlint/cli @commitlint/config-conventional @commitlint/cz-commitlint\necho \"module.exports = { extends: ['@commitlint/config-conventional'] };\" > commitlint.config.js\n```\n\n`package.json` 把 commitizen 指到 commitlint adapter，规则就只剩 `commitlint.config.js` 一份：\n\n```json\n{\n  \"scripts\": { \"commit\": \"git-cz\" },\n  \"config\": {\n    \"commitizen\": { \"path\": \"@commitlint/cz-commitlint\" }\n  }\n}\n```\n\nhusky v9 起 `husky add` / `husky install` 都没了，改 `init` + 手写 hook 文件：\n\n```bash\nnpm i -D husky\nnpx husky init\necho 'npx --no -- commitlint --edit \"$1\"' > .husky/commit-msg\n```\n\n发布工具按场景四选一：\n\n| 场景                                     | 工具                                                                                 |\n| ---------------------------------------- | ------------------------------------------------------------------------------------ |\n| GitHub 项目，PR-based release flow       | [release-please](https://github.com/googleapis/release-please)                       |\n| CI 全自动 `npm publish` + GitHub Release | [semantic-release](https://github.com/semantic-release/semantic-release)             |\n| 自管 tag、本地跑 release                 | [commit-and-tag-version](https://github.com/absolute-version/commit-and-tag-version) |\n| Monorepo，多包独立版本号                 | [changesets](https://github.com/changesets/changesets)                               |\n\n口诀：\n\n- **release-please** —— Google 维护，把\"待发布\"维持成一条长期 PR，merge 即发版。GitHub Actions 装一次就不再管，新项目首选。\n- **semantic-release** —— push 到 main 立即发版，最激进；适合 SDK / 工具库这种发版没人审的场景。\n- **commit-and-tag-version** —— [standard-version 2022-05-15 弃用](https://github.com/conventional-changelog/standard-version/issues/919)后的社区 drop-in fork，旧配置原样可用；适合不想给 CI 开发布权限、自己 `npm publish` 的项目。\n- **changesets** —— monorepo 默认选项（pnpm / turborepo 生态主流）。每个 PR 自带一份 markdown changeset 文件，发版时按包聚合 —— 比 conventional commits 派系更适合\"一次 PR 改多包但版本号要各自 bump\"。"},{"title":"PHP strpos 找到时返回 0","slug":"posts/190410-strpos-returns-zero","date":"2019-04-10","tags":["php"],"body":"`strpos($haystack, $needle): int|false` 找不到时返回 `false`，**找到时返回位置 int**——位置 `0` 在 `if` 里跟 `false` 等价，会被吞掉。\n\n```php\n$s = 'abc';\n\nif (strpos($s, 'a')) {            // 不输出，0 被当 false\n    echo 'found';\n}\n\nif (strpos($s, 'a') !== false) {  // 正解\n    echo 'found';\n}\n```\n\nPHP 8.0+ 直接用 `str_contains($haystack, $needle): bool`，没有这个坑。\n\n<https://www.php.net/manual/zh/function.strpos.php>"},{"title":"PhpStorm \"Multiple definitions exist for class\" 警告","slug":"posts/180505-phpstorm-multiple-class-definitions","date":"2018-05-05","tags":["php","phpstorm"],"body":"PhpStorm 报：\n\n```bash\nMultiple definitions exist for class Foo\\Bar\n```\n\n自动跳转 / 补全在两个文件之间反复横跳 —— 同一份代码被索引了两遍。\n\n最常见场景：本地 dev 一个 package（`packages/foo/`），它又被 Composer path repository 或 symlink 挂进 `vendor/foo/`，PhpStorm 把两份都索引了。\n\n`Cmd + O` 输入类名看候选列表，确认是哪两个文件，然后保留 source-of-truth 那份，另一份处理：\n\n- 项目内的副本：右键 dir → `Mark Directory as > Excluded`\n- 系统级 Include Path 里的：`Settings > Languages & Frameworks > PHP > Include Path` 删条目\n\nPhpStorm 会增量重建索引，不必走 `File > Invalidate Caches`。"},{"title":"PhpStorm 新建文件突然没有语法高亮","slug":"posts/180505-phpstorm-no-syntax-highlight","date":"2018-05-05","tags":["php","phpstorm"],"body":"新建 `foo.php` 突然没语法高亮，删了重建还是没有，扩展名也没错 —— 大概率是某个 pattern 被误加进了 `Text` 文件类型的白名单。最常见的源头是某次手滑点了 _Override File Type → Text_，PhpStorm 就把这个 pattern 永久记下了。\n\n修复：\n\n```text\nSettings > Editor > File Types > Recognized File Types > Text > Registered Patterns\n```\n\n删掉多余的 `*.php` / `foo.php` 即可。改动立刻生效，文件不用重开。"},{"title":"PhpStorm 选中文本后输引号自动包裹","slug":"posts/180505-phpstorm-surround-on-typing","date":"2018-05-05","tags":["php","phpstorm"],"body":"很多编辑器默认开启的 \"选中 + 输引号 = 自动包裹\" 在 PhpStorm 里默认**关**。开启路径：\n\n```text\nSettings > Editor > General > Smart Keys > Surround selection on typing quote or brace\n```\n\n勾上后，选中一个 word 再敲 `'` `\"` `(` `[` `{` 都会用对应符号把它包起来 —— 加日志、加翻译函数、试不同表达式优先级时省一大堆光标移动。"},{"title":"Mac 上录制和编辑 GIF","slug":"posts/171123-mac-record-edit-gif","date":"2017-11-23","tags":["media"],"body":"## 录制\n\n[LICEcap](https://www.cockos.com/licecap/) 小巧好用。\n\n## 编辑\n\nMac 自带「预览」可以删 GIF 帧，但**保存后循环属性丢了，只播放一次**。改用 Photoshop：\n\n1. 打开 GIF，`窗口 → 时间轴` 编辑各帧\n2. `文件 → 导出 → 存储为 Web 所用格式`\n3. 在「循环选项」里选 **永久**，再 `存储`\n\n同一个面板里改「图像大小」可以顺手压缩。"},{"title":"GitHub README 图片报 Content length exceeded","slug":"posts/171122-camo-image-limit","date":"2017-11-22","tags":["github"],"body":"GitHub 把 README / issue / wiki 里所有外链图都走 [Camo](https://github.com/atmos/camo) 匿名代理（隐藏访问者 IP 和 referer），代理默认 5 MB 上限。超过的图加载失败，`alt` 文字被替换成 `Content length exceeded`。\n\n5 MB 写死在 [`CAMO_LENGTH_LIMIT`](https://github.com/atmos/camo/blob/master/server.coffee#L25)：\n\n```coffee\ncontent_length_limit = parseInt(process.env.CAMO_LENGTH_LIMIT || 5242880, 10)\n```\n\n`5242880 / 1024 / 1024 = 5`。压缩或裁剪到 5 MB 以下是唯一办法——GitHub 强制代理，自托管也绕不开。"},{"title":"git checkout --ours / --theirs","slug":"posts/170830-git-checkout-ours-theirs","date":"2017-08-30","tags":["git"],"body":"冲突里某个文件不想逐行 merge，整文件保留一边即可。批量加 `.`：\n\n```bash\ngit checkout --ours .    # 保留\"当前视角\"那边，丢另一边\ngit checkout --theirs .  # 反之\ngit add .\n```\n\n**坑：`--ours` 在 merge 和 rebase 下指向相反的分支。**\n\n| 操作                  | `--ours`                  | `--theirs`           |\n| --------------------- | ------------------------- | -------------------- |\n| `git merge feature`   | 当前分支 (`HEAD`)         | 被合入的 `feature`   |\n| `git rebase upstream` | `upstream`（rebase 落点） | 你正在 rebase 的分支 |\n\n`rebase` 把你的提交逐个\"重放\"到 upstream 上，每一步 `HEAD` 都是 upstream，所以 `--ours` 指 upstream，反直觉但自洽。\n\nSource: <https://bitmingw.com/2017/02/16/git-merge-rebase-ours-and-theirs/>"},{"title":"this / self / parent / static 在 PHP 类内部的区别","slug":"posts/170525-this-self-parent-static","date":"2017-05-25","tags":["php"],"body":"类内部访问成员有 4 个关键字，差别在「指向哪个类」与「是否跟着子类走」（多态 / late static binding）：\n\n| 关键字     | 指向                                                  | LSB | 静态方法可用              |\n| ---------- | ----------------------------------------------------- | --- | ------------------------- |\n| `self::`   | **写它的那个类**（词法）                              | 否  | ✓                         |\n| `parent::` | `self` 的父类                                         | 否  | ✓                         |\n| `static::` | **实际调用时的类**（_late static binding_，PHP 5.3+） | ✓   | ✓                         |\n| `$this->`  | 当前实例所属的类                                      | ✓   | ✗（静态方法里无 `$this`） |\n\n`self::` 和 `static::` 的差别只在被子类继承后才显现：\n\n```php\nclass A\n{\n    public static function newSelf()   { return new self();   }\n    public static function newStatic() { return new static(); }\n}\n\nclass B extends A {}\n\nvar_dump(B::newSelf());   // object(A) — self 锁死在写它的 A 里\nvar_dump(B::newStatic()); // object(B) — static 跟着调用方\n```\n\n口诀：工厂方法要返回子类型 → `static::`；要锁定当前类不被覆盖 → `self::`。\n\n[Stack Overflow: When to use self over $this?](https://stackoverflow.com/questions/151969/when-to-use-self-over-this)"},{"title":"CGI vs FastCGI vs PHP-CGI vs PHP-FPM","slug":"posts/170421-cgi-fastcgi-phpcgi-phpfpm","date":"2017-04-21","tags":["php"],"body":"PHP 处理 web 请求要解决两件事——和 web server 通信的**协议**、PHP 自己的**进程管理**。这四个名词分两组踩在这两件事上：协议层是 CGI / FastCGI，PHP 实现是 PHP-CGI / PHP-FPM。\n\n## 协议层\n\n|          | CGI                         | FastCGI                    |\n| -------- | --------------------------- | -------------------------- |\n| 进程模型 | 每个请求 fork-and-exec 一次 | master + 长驻 worker pool  |\n| 启动开销 | 每次都加载解释器            | 解释器常驻内存             |\n| 数据流   | 环境变量 + stdin/stdout     | 二进制协议复用 socket 连接 |\n| 现状     | 历史遗留                    | 现代 web 标准              |\n\nCGI 慢在反复 fork 和解释器反复初始化。FastCGI 把解释器留在内存，请求过来直接派给空闲 worker。\n\n## PHP 实现\n\n|              | PHP-CGI                    | PHP-FPM                                     |\n| ------------ | -------------------------- | ------------------------------------------- |\n| 性质         | PHP 的 CGI binary          | PHP 官方 FastCGI 进程管理器                 |\n| 进程管理     | 无                         | master + 动态 worker pool                   |\n| 改 `php.ini` | 必须杀进程重启，PHP 会停服 | `kill -USR2` 平滑 reload，请求不中断        |\n| 现状         | 淘汰                       | 主流（Laravel / WordPress / 大多数 PHP 栈） |\n\nPHP-FPM = PHP 解释器 + FastCGI 协议实现 + 进程管理。三件事的合体，不是单独某个组件。\n\n## 实战：nginx + php-fpm\n\nnginx 把 `.php` 请求按 FastCGI 协议丢给 php-fpm 的 socket：\n\n```nginx\nlocation ~ \\.php$ {\n    fastcgi_pass   unix:/run/php/php8.2-fpm.sock;\n    fastcgi_index  index.php;\n    fastcgi_param  SCRIPT_FILENAME $document_root$fastcgi_script_name;\n    include        fastcgi_params;\n}\n```\n\nphp-fpm 那头按 pool 配置 worker 数（`pm.max_children` 等），nginx 不感知 PHP 进程。\n\n## 怎么选\n\n现代栈一律 **nginx + php-fpm**，CGI / PHP-CGI 了解概念即可，生产不用。\n\n参考：[搞不清 FastCGI 与 PHP-fpm 之间是什么关系](https://segmentfault.com/q/1010000000256516) · [CGI / FastCGI / PHP-CGI / PHP-FPM 是什么](http://www.mike.org.cn/articles/what-is-cgi-fastcgi-php-fpm-spawn-fcgi/)"},{"title":"MySQL 取每组最大行：子查询 ORDER BY 不生效","slug":"posts/170420-group-by-order-by-subquery-trap","date":"2017-04-20","tags":["mysql"],"body":"经典需求：每个用户按某字段取最大那一行。MySQL 5.6 时代流传一个\"民间偏方\"——子查询先 ORDER BY 倒序、外层 GROUP BY 拿\"第一条\"：\n\n```sql\n-- 5.6 上看似生效，5.7 起失效\nSELECT * FROM (\n  SELECT id, no, name FROM testorder ORDER BY no DESC\n) a GROUP BY a.name;\n```\n\n5.6 上巧合返回每组 `no` 最大的那一行；5.7 默认 `sql_mode=ONLY_FULL_GROUP_BY` 直接拒；即便关掉 ONLY_FULL_GROUP_BY，返回的也是按物理存储顺序的\"任意一行\"，**ORDER BY 被忽略**。\n\n根因不是 MySQL 的 bug，是 SQL 标准本来就这么定义：\n\n> A \"table\" (and a subquery in the FROM clause too) is — according to the SQL standard — an unordered set of rows.\n\nFROM 后的子查询返回的是**集合**不是**序列**。子查询里写 ORDER BY，优化器允许直接丢弃。GROUP BY 选哪一行也是自由的，更不受外层 ORDER BY 影响——5.6 那种\"先排序再分组取第一\"是优化器实现细节，不是契约。\n\n正确写法是 _self-join_：先聚合算出每组最大值，再回表取整行。\n\n```sql\nSELECT a.id, a.no, a.name\nFROM testorder a\nINNER JOIN (\n  SELECT name, MAX(no) AS no FROM testorder GROUP BY name\n) b ON a.name = b.name AND a.no = b.no;\n```\n\nMySQL 8.0+ 改用 _window function_ 更直观：\n\n```sql\nSELECT id, no, name FROM (\n  SELECT id, no, name,\n         ROW_NUMBER() OVER (PARTITION BY name ORDER BY no DESC) AS rn\n  FROM testorder\n) t WHERE rn = 1;\n```\n\n`ANY_VALUE(col)` 只是绕开 ONLY*FULL_GROUP_BY 的报错，**不解决\"取哪一行\"的不确定性**——MySQL 文档原话：服务器自由选择，结果 \\_indeterminate*。别拿它当\"5.7 兼容补丁\"用。"},{"title":"MySQL 5.7 默认 sql_mode 多了 6 项：升级踩坑清单","slug":"posts/170420-sql-mode-defaults-57","date":"2017-04-20","tags":["mysql"],"body":"5.6 → 5.7 升级最容易踩的坑：默认 `sql_mode` 从只有 `NO_ENGINE_SUBSTITUTION` 一项变成 7 项。多出来的每一条都可能让\"5.6 跑得好好的 SQL\"在 5.7 直接报错。\n\n```sql\nSELECT @@SQL_MODE;\n-- 5.6: NO_ENGINE_SUBSTITUTION\n-- 5.7: ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,\n--      NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_AUTO_CREATE_USER,\n--      NO_ENGINE_SUBSTITUTION\n```\n\n新增 6 项各自封禁的\"老 MySQL 容忍行为\"：\n\n- **`ONLY_FULL_GROUP_BY`**：SELECT 引用了 GROUP BY 外的非聚合列直接拒（5.7.5 起还能识别 _functional dependency_：主键 + 全字段唯一确定的列免检）。绕开用 `ANY_VALUE(col)`，但要清楚它不解决\"取哪一行不确定\"的问题\n- **`STRICT_TRANS_TABLES`**：事务表上 INSERT/UPDATE 拿到非法值（字符串超长被截断、无符号字段写负数、NOT NULL 字段无默认值且未提供）直接报错而不是静默修正\n- **`NO_ZERO_IN_DATE`** + **`NO_ZERO_DATE`**：禁 `2017-00-15`、`0000-00-00` 这种\"日期占位符\"。老业务把 `0000-00-00` 当 NULL 用的全部踩雷\n- **`ERROR_FOR_DIVISION_BY_ZERO`**：INSERT/UPDATE 中除零报错（之前默认返 NULL）\n- **`NO_AUTO_CREATE_USER`**：`GRANT ... TO 'user'` 不再隐式创建用户。8.0 起这条被移除——`CREATE USER` 是唯一合法路径\n\n升级前的 checklist：\n\n```sql\n-- 1. 测试库上跑生产读流量，盯 ONLY_FULL_GROUP_BY 报错\n-- 2. 写流量看 STRICT_TRANS_TABLES、NO_ZERO_DATE 报错\n-- 3. 应急可临时降级，但只是续命，不是解法\nSET GLOBAL sql_mode = 'NO_ENGINE_SUBSTITUTION';\n```\n\n主备复制场景额外注意：**先升级备库再升主库**——主库以高 sql_mode 写入的 binlog，老 sql_mode 的备库未必能 replay。"},{"title":"PHP empty('0.0') 不是空","slug":"posts/170309-empty-zero-string","date":"2017-03-09","tags":["php"],"body":"PHP 的 `empty()` 把 `'0'`（字符串零）当空，但 `'0.0'`（字符串小数零）当**非空**。DB 里把 decimal 字段存成 string 时会咬人——查到 `'0.00'` 误以为有值。\n\n```php\nempty('0');           // true\nempty('0.0');         // false ← 反直觉\nempty((float) '0.0'); // true\n```\n\n判断数值前先 cast：`empty((float) $value)`。\n\n<https://www.php.net/manual/zh/function.empty.php>"},{"title":"MySQL LIMIT offset 深分页慢：deferred join 与 keyset","slug":"posts/160918-limit-offset-deep-pagination","date":"2016-09-18","tags":["mysql"],"body":"`LIMIT 100000, 10` 不是\"取 10 行\"，是\"取 100010 行扔掉前 100000 行\"。执行器没法靠索引跳过 N 行，必须物化前 100000 行（含回表读所有 SELECT 列）数着扔。**真正代价是回表读了 10 万行宽数据**，跟最终返回多少无关。\n\n## 方案 1：deferred join\n\n逃不开 OFFSET 时（\"跳到第 N 页\"按钮），让 OFFSET 只在窄覆盖索引上扫：\n\n```sql\n-- 慢：100010 次回表\nSELECT * FROM t1 WHERE ftype = 1 ORDER BY id DESC LIMIT 100000, 10;\n\n-- 快：100010 次只走索引，10 次回表\nSELECT t1.* FROM t1\nINNER JOIN (\n  SELECT id FROM t1 WHERE ftype = 1 ORDER BY id DESC LIMIT 100000, 10\n) t2 USING (id);\n```\n\n`ftype` 上有二级索引即可——InnoDB 二级索引叶子隐式追加主键，`(ftype)` 等价 `(ftype, id)`，反向扫满足 `ORDER BY id DESC`，`SELECT id` 已覆盖，全程不回表。`EXPLAIN` 看子查询 `Extra` 必须是 `Using index`。\n\n## 方案 2：keyset pagination\n\n放弃页码，用上一页最后一行的 id 当游标：\n\n```sql\nSELECT * FROM t1 WHERE ftype = 1 AND id < ? ORDER BY id DESC LIMIT 10;\n```\n\n每页 O(页大小) 而非 O(offset)，第 1000 页和第 1 页一样快。**排序列非唯一**（如 `created_at` 有重复）需复合游标避免跳行：`WHERE (created_at, id) < (?, ?) ORDER BY created_at DESC, id DESC`。\n\n## 怎么选\n\n| 场景                            | 用哪个        |\n| ------------------------------- | ------------- |\n| 无限滚动 / \"下一页\" / API 游标  | keyset        |\n| \"跳到第 N 页\"硬需求（后台列表） | deferred join |"},{"title":"MySQL 客户端提示符自定义","slug":"posts/160731-client-prompt-customization","date":"2016-07-31","tags":["mysql"],"body":"默认的 `mysql>` 提示符不显示当前用户 / host / 数据库——切了几次连接后 `DROP TABLE` 错库是真实事故。把提示符换成 `(user@host) [db]>` 是 5 秒杜绝这类事故的方式。\n\n四种设法，**作用域和反斜杠规则不同**：\n\n| 方法                        | 作用域              | 反斜杠      |\n| --------------------------- | ------------------- | ----------- |\n| `mysql --prompt=\"...\"` CLI  | 仅当前连接          | 单 `\\`      |\n| 连接后输入 `prompt ...`     | 仅当前连接          | 单 `\\`      |\n| `MYSQL_PS1` 环境变量        | 当前 shell 所有连接 | 单 `\\`      |\n| `~/.my.cnf` 的 `[mysql]` 段 | 当前用户所有连接    | **双 `\\\\`** |\n\n推荐永久方案落 `~/.my.cnf`：\n\n```ini\n[mysql]\nprompt=\"(\\\\u@\\\\h) [\\\\d]>\\\\_ \"\n```\n\n<!-- prettier-ignore-start -->\n<!-- markdownlint-disable-next-line no-space-in-code -->\n效果：`(root@127.0.0.1) [mydb]> `。\n<!-- prettier-ignore-end -->\n\n## 为什么 `my.cnf` 必须双反斜杠\n\noption 文件解析器**自己有一套反斜杠转义**（`\\s` → 空格、`\\n` → 换行、`\\t` → tab），跟 prompt 转义序列正面冲撞。写单 `\\s` 会被 option 解析层先吃掉变成空格，根本到不了 prompt 解析层，秒数字段就没了。双 `\\\\` 是先把 option 层逃掉，让真正的 `\\s` 落到 prompt 层。\n\n其他三种方式（CLI / 交互 `prompt` / 环境变量）不经 option 解析，单 `\\` 即可。混淆这条是从配置文件抄到命令行（或反过来）调不通的高频原因。\n\n## 常用转义序列\n\n- `\\u` 当前用户 / `\\h` 服务器 / `\\d` 当前数据库\n- `\\U` 完整 `user@host`\n- `\\D` 完整日期 / `\\R` 24 时制 / `\\m` 分 / `\\s` 秒\n- `\\T` 在事务块中显示 `*`（活在未提交事务里很容易忘）\n- `\\c` 自增的语句计数器\n- `\\_` 一个空格（结尾留空白比裸空格稳）\n\n完整列表见 [MySQL 8.4 mysql Client Commands](https://dev.mysql.com/doc/refman/8.4/en/mysql-commands.html)。"},{"title":"MySQL 查重去重：HAVING + JOIN 子查询","slug":"posts/160725-find-and-dedupe-with-having","date":"2016-07-25","tags":["mysql"],"body":"按某列查重——`GROUP BY` 之后用 `HAVING` 筛 `COUNT(*) > 1`：\n\n```sql\nSELECT col_a, COUNT(*)\nFROM t1\nGROUP BY col_a\nHAVING COUNT(*) > 1;\n```\n\n记法：`WHERE` 在聚合之前过滤行、不能引用 `COUNT(*)`；`HAVING` 在聚合之后过滤组、可以。\n\n去重保留每组 `id` 最大的——先把 keep set 算出来当 derived table，再 JOIN 回原表删剩下的：\n\n```sql\nDELETE a\nFROM t1 a\nJOIN (\n  SELECT col_a, MAX(id) AS keep_id\n  FROM t1\n  GROUP BY col_a\n  HAVING COUNT(*) > 1\n) b ON a.col_a = b.col_a\nWHERE a.id < b.keep_id;\n```\n\n不能直接写 `DELETE FROM t1 WHERE id NOT IN (SELECT MAX(id) FROM t1 GROUP BY col_a)`——MySQL 禁止 `DELETE` / `UPDATE` 子查询里直接引目标表（错误 1093）。包一层 derived table 触发物化、与目标表解耦就能绕开，机制详见 `210129-delete-self-reference-1093.md`。\n\n要保留最早一条把 `MAX` → `MIN`、`<` → `>`。"},{"title":"Hero Demo","slug":"posts/060703-hero-demo","date":"2016-07-03","updated":"2016-07-04","tags":["demo"],"body":"The newest note shows up here in full — a 60ch slot above the masonry grid. Everything else is a preview card.\n\nAstro returns collection entries in **disk order**, not date order. Sort first, then split:\n\n```ts\nconst notes = (await getCollection(\"notes\")).sort(\n  (a, b) => b.data.date.valueOf() - a.data.date.valueOf(),\n);\nconst [hero, ...rest] = notes;\n```\n\nThat's the whole trick."},{"title":"你好，今日新知","slug":"posts/060702-nihao-til","date":"2016-07-02","tags":["demo","hello"],"body":"这是一篇用来检验中文排版效果的简短示例：标点、行高、字体回退（Iowan Old Style + 苹方）以及代码块在 CJK 段落中的混排表现。\n\n## 段落与引用\n\n孔子曰：「学而不思则罔，思而不学则殆。」短短一句话点出了学习与思考相辅相成的道理。这一原则放在工程实践里同样成立——只读文档不动手，或只埋头写代码却不复盘，都会陷入低效循环。\n\n> 不积跬步，无以至千里；不积小流，无以成江海。\n>\n> —— 荀子《劝学》\n\n## 列表\n\n- 中英混排：使用 `git log --oneline` 查看提交记录。\n- 嵌套：\n  - 二层项目一\n  - 二层项目二\n- 行内强调：**加粗**、_斜体_、`代码`、~~删除线~~ 都可以正常渲染。\n\n## 表格\n\n| 命令       | 用途         | 备注             |\n| ---------- | ------------ | ---------------- |\n| `pwd`      | 当前路径     | 常用于脚本调试   |\n| `ls -alh`  | 列出文件     | 含隐藏与可读大小 |\n| `du -sh *` | 统计目录大小 | 排查空间占用     |\n\n## 代码\n\n行内代码：在终端运行 `find . -name '.DS_Store' -delete` 即可清理 macOS 缓存文件。\n\n带中文注释的 Bash：\n\n```bash\n# 把当前目录下所有 Markdown 文件按修改时间排序\nls -lt *.md | head -10\n\n# 替换文件中的全角逗号为半角（保留空格）\nsed -i '' 's/，/, /g' *.md\n```\n\nGo 示例——通道传递中文字符串：\n\n```go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    ch := make(chan string, 1)\n    ch <- \"通过通信共享内存，而不是通过共享内存通信\"\n    fmt.Println(<-ch)\n}\n```\n\n## 行内元素\n\n按下 <kbd><kbd>⌘</kbd>+<kbd>Shift</kbd>+<kbd>4</kbd></kbd> 即可截取屏幕区域。\n\n大多数 <mark>蝾螈</mark>都在夜间活动，以昆虫和蠕虫为食。\n\n化学式 H<sub>2</sub>O，毕达哥拉斯定理 a<sup>2</sup> + b<sup>2</sup> = c<sup>2</sup>。"},{"title":"Hello, TIL","slug":"posts/060701-hello-til","date":"2016-07-01","tags":["demo","hello"],"body":"This article offers a sample of basic Markdown syntax that can be used in Hugo content files, also it shows whether basic HTML elements are decorated with CSS in a Hugo theme.\n\n## Headings\n\nThe following HTML `<h1>`—`<h6>` elements represent six levels of section headings. `<h1>` is the highest section level while `<h6>` is the lowest.\n\n<!-- prettier-ignore-start -->\n<!-- markdownlint-disable-next-line single-h1 -->\n# H1\n<!-- prettier-ignore-end -->\n\n## H2\n\n### H3\n\n#### H4\n\n##### H5\n\n###### H6\n\n## Paragraph\n\nXerum, quo qui aut unt expliquam qui dolut labo. Aque venitatiusda cum, voluptionse latur sitiae dolessi aut parist aut dollo enim qui voluptate ma dolestendit peritin re plis aut quas inctum laceat est volestemque commosa as cus endigna tectur, offic to cor sequas etum rerum idem sintibus eiur? Quianimin porecus evelectur, cum que nis nust voloribus ratem aut omnimi, sitatur? Quiatem. Nam, omnis sum am facea corem alique molestrunt et eos evelece arcillit ut aut eos eos nus, sin conecerem erum fuga. Ri oditatquam, ad quibus unda veliamenimin cusam et facea ipsamus es exerum sitate dolores editium rerore eost, temped molorro ratiae volorro te reribus dolorer sperchicium faceata tiustia prat.\n\nItatur? Quiatae cullecum rem ent aut odis in re eossequodi nonsequ idebis ne sapicia is sinveli squiatum, core et que aut hariosam ex eat.\n\n## Blockquotes\n\nThe blockquote element represents content that is quoted from another source, optionally with a citation which must be within a `footer` or `cite` element, and optionally with in-line changes such as annotations and abbreviations.\n\n### Blockquote without attribution\n\n> Tiam, ad mint andaepu dandae nostion secatur sequo quae.\n> **Note** that you can use _Markdown syntax_ within a blockquote.\n\n### Blockquote with attribution\n\n> Don't communicate by sharing memory, share memory by communicating.\n>\n> — <cite>Rob Pike[^1]</cite>\n\n[^1]: The above quote is excerpted from Rob Pike's [talk](https://www.youtube.com/watch?v=PAAkCSZUG1c) during Gopherfest, November 18, 2015.\n\n## Tables\n\nTables aren't part of the core Markdown spec, but Hugo supports them out-of-the-box.\n\n| Name  | Age |\n| ----- | --- |\n| Bob   | 27  |\n| Alice | 23  |\n\n### Inline Markdown within tables\n\n| Italics   | Bold     | Code   |\n| --------- | -------- | ------ |\n| _italics_ | **bold** | `code` |\n\n## List Types\n\n### Ordered List\n\n1. First item\n2. Second item\n3. Third item\n\n### Unordered List\n\n- List item\n- Another item\n- And another item\n\n### Nested Unordered list\n\n- Fruit\n  - Apple\n  - Orange\n  - Banana\n- Dairy\n  - Milk\n  - Cheese\n\n### Nested Ordered list\n\n1. Fruit\n   - Apple\n   - Orange\n   - Banana\n2. Dairy\n   1. Milk\n   2. Cheese\n3. Third item\n   1. Sub One\n   2. Sub Two\n\n## Code\n\n### Inline code\n\nUse single backticks for inline code: `npm install`, `git commit -m \"wip\"`, or for filenames like `package.json`.\n\n### Fenced code blocks\n\nBash — a classic TIL one-liner:\n\n```bash\n# Find and remove every .DS_Store under the current directory\nfind . -name '.DS_Store' -type f -delete\necho \"done\"\n```\n\nJavaScript:\n\n```js\nconst greet = (name) => `Hello, ${name}!`;\nconst names = [\"TIL\", \"world\"];\nnames.forEach((n) => console.log(greet(n)));\n```\n\nGo — pairs with the Rob Pike quote above:\n\n```go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    ch := make(chan string, 1)\n    ch <- \"share memory by communicating\"\n    fmt.Println(<-ch)\n}\n```\n\nJSON:\n\n```json\n{\n  \"name\": \"til\",\n  \"version\": \"0.1.0\",\n  \"private\": true\n}\n```\n\nA block with no language tag — monospace, no highlighting:\n\n```bash\n$ whoami\nzyf\n$ uname -s\nDarwin\n```\n\n## Other Elements — abbr, sub, sup, kbd, mark\n\n<abbr title=\"Graphics Interchange Format\">GIF</abbr> is a bitmap image format.\n\nH<sub>2</sub>O\n\nX<sup>n</sup> + Y<sup>n</sup> = Z<sup>n</sup>\n\nPress <kbd><kbd>CTRL</kbd>+<kbd>ALT</kbd>+<kbd>Delete</kbd></kbd> to end the session.\n\nMost <mark>salamanders</mark> are nocturnal, and hunt for insects, worms, and other small creatures."}]